* Performance improvement plan

Tracking doc for filter/render/dropdown performance work on ShowAllEntityData.

Each implementation step below is its own top-level heading with a TODO/DONE
keyword. Flip TODO -> DONE (and add a short note: what changed, which commit
range / branch) as each step lands. Do not delete completed steps — the
history of what was tried and why is the point of this file.

*The keyword tracks* ~main~, *not effort.* A step reads DONE only when its code
is on ~main~. Several steps below were implemented and measured on a branch
that was never merged; those stay TODO and say so in their own text, so that
"what is true of the script people actually run" is readable straight off the
keywords. As of this revision the DONE set is exactly Steps 3, 4, 8, 18, 19, 21 and 22.

*One canonical numbering.* Three copies of this file drifted into three
incompatible schemes — ~main~, ~perf-steps-1-4~ and
~filter-performance-fix-caa-throughput~ each had a different "Step 6". They are
now reconciled here; see "Step-number provenance" below before trusting any
"Step N" reference in a branch commit message or an old session log.

*Line references* (~:12345~) are only valid for the script version named
alongside them. Re-grep the symbol rather than trusting a number that looks
stale — the userscript is ~74 000 lines and they drift constantly.

** Context

~runFilter()~ currently clones every matching row with ~cloneNode(true)~ on
every keystroke, replaces the tbody, and re-runs the full post-render hook
chain (CAA/EAA artwork, ~initExpandRGsFeature~, ~initCollapsableColumns~,
CDtoc, Picard tagger, nested-~<h2>~ rewiring). The abandoned
~filter-performance-fix~ branch ("Fix C") replaced this with in-place
~display:none~/~display:''~ toggling on live DOM rows, so the hook chain
runs once at initial load instead of once per keystroke, plus
IntersectionObserver-based lazy CAA/EAA loading for large tables
("Pattern 5").

That branch diverged from ~main~ at commit ~0683c23~ and was never merged;
~main~ has since moved ~347 commits ahead (~23k lines changed in the
userscript alone). It cannot be merged cleanly (4 real conflicts, one
~740 lines) and the surrounding systems it touches (CAA/EAA pipeline,
collapsable-columns, disk-load restore) have all changed since. It has been
archived (not deleted) as ~archive/filter-performance-fix-v1-single-table~
for reference — the ideas need a fresh reimplementation against current
~main~, not a merge.

** Findings: is the underlying problem still real?

Yes — re-confirmed against ~main~ at *9.99.1045+2026-09-07*, which is also the
version every line reference in this section was read at:

- ~runFilter()~ (~:42225~) still clones every matching row and fully rebuilds
  the tbody on every keystroke, in both branches.
- The multi-table branch does the same per group, *except for a sort*, which
  since Step 18 touches only the sub-table it re-ordered: ~runFilter()~
  (~:42450~) pushes an empty ~_mbSkipRender~ group for every undisturbed
  index and ~renderGroupedTable()~ (~:49379~) leaves those ~<tbody>~ elements
  alone. Filters still re-render everything.
- No ~IntersectionObserver~ exists anywhere in the file — zero occurrences —
  so lazy CAA/EAA loading (Step 5) was never independently reimplemented.
- The "Show single-table" button (~openSubtableAsSingleTableTab()~) now lets
  any non-overflowing multi-table sub-table be viewed as its own single-table
  snapshot tab, which makes a single-table-only fix cover *more* ground than
  when the old branch was written. Performance work should keep targeting
  single-table mode first, same as the old branch already did (its own WIP
  changelog scoped Fix C to single-table mode only).

*** The dominant, previously-underweighted cost: ~_updateAllColHeaderCounts~

The file's own comments (~:56886-56888~, ~:57587~) document that this
O(rows x columns) per-column unique-value + multi-row-count scan can take
"30–120 s synchronously" for 8 k+ rows — enough to trigger Chrome's "Page
Unresponsive" dialog, which is why it's already deferred rather than fixed at
the root. ~:56694~ puts the same cost concretely for the harness page:
~artist-events~ at 4174 rows × 21 columns is *~88 000 ~getCleanColumnText()~
calls* per full pass.

It runs at the tail of ~initCollapsableColumns()~ (~:56878~), reached from
~_scheduleColHeaderCounts()~ (~:56650~), which today fires from:

| Call site          | Function                                                | Frequency                                                                       |
|--------------------+---------------------------------------------------------+---------------------------------------------------------------------------------|
| ~:42747~           | ~runFilter()~, single-table branch                      | *every keystroke and every sort*                                                |
| ~:49738~           | ~renderGroupedTable()~, reuse branch, per sub-table     | every multi-table render — *except undisturbed groups on a sort, since Step 18* |
| ~:50271~           | ~renderGroupedTable()~, new-table branch, per sub-table | every multi-table render                                                        |
| ~:19699~           | ~syncCollapseButtonsWithColumnVisibility()~             | every column show/hide, page-wide                                               |
| ~:61362~, ~:61377~ | ~initReleaseEventsColumn()~                             | up to 3x in a row post-fetch (what the coalescing token exists for)             |
| ~:46421~           | ~startFetchingProcess()~                                | once per fetch                                                                  |
| ~:73505~           | ~initPicardTaggerColumn()~                              | post-render                                                                     |

Scaled to 5,000 rows that is roughly 19-75 s of background work re-triggered
on most of those actions — larger than the cloneNode/DOM-rebuild cost the old
branch was framed around. Step 3 is the remedy and landed in 9.99.1049; Step
18 had already removed one slice of it (see that step, and Step 3's own note).
Read the call-site table above as the pre-9.99.1049 state it describes — the
same sites still fire, but a scan whose visible row set is unchanged since the
last completed one now costs O(columns) instead of O(rows x columns).

** Ranked areas + estimated impact (5,000-row, ~20-column table)

Estimates are engineering estimates from operation-count analysis (Big-O
comparison, anchored to the one concrete empirical number in the codebase:
the documented 30-120s/8k-rows cost above), not measured browser profiles.
Real DevTools Performance-tab profiling before/after is the actual
acceptance check for each step.

| # | Area                                        | Est. improvement                               |
|---+---------------------------------------------+------------------------------------------------|
| 1 | Filter re-render (Fix C)                    | ~95-99% per-keystroke latency (wide tables)    |
| 2 | Same fix extended to Sort                   | ~90-99% per-sort-click latency                 |
| 3 | Cache ~_updateAllColHeaderCounts~           | ~90%+ on toggle / multi-table triggers         |
| 4 | ~openUniqDrop()~ per-open caching           | ~90-98% on repeat dropdown opens               |
| 5 | Lazy CAA/EAA loading (IntersectionObserver) | ~99% fewer art requests queued at initial load |

** Step-number provenance

Three copies of this file drifted into three incompatible numberings. All of
them agreed on Steps 1-5, and all three then reused 6, 7 and 8 for completely
different work. The canonical scheme below keeps ~perf-steps-1-4~'s 6-14,
because that was the most-developed set *and* because ~main~'s own code and
prose already pointed at it — ~ShowAllEntityData.user.js:56582~ cites "Step 6"
for the order-independent signature, and Step 4's hazard-2 note cites "Step 8"
for the async-column invalidations. Both are correct under this scheme with no
edit. What moved instead was ~caa-throughput~'s 6/7/8 and ~main~'s own Step 6,
which was only one day old.

Read this table before trusting a "Step N" reference in a branch commit
message, a ~debug/*.org~ session log, or a changelog entry written before this
revision.

| Canonical | ~main~ (pre-consolidation) | ~perf-steps-1-4~ | ~filter-performance-fix-caa-throughput~ |
|-----------+----------------------------+------------------+-----------------------------------------|
|       1-5 | 1-5 (same)                 | 1-5 (same)       | 1-5 (same)                              |
|         6 | —                          | *6*              | —                                       |
|         7 | —                          | *7*              | —                                       |
|         8 | cited as "Step 8", no body | *8*              | —                                       |
|      9-14 | —                          | *9-14*           | —                                       |
|        15 | —                          | —                | *6*                                     |
|        16 | —                          | —                | *7*                                     |
|        17 | —                          | —                | *8*                                     |
|        18 | *6* (added 2026-09-07)     | —                | —                                       |
|     19-21 | — (new in this revision)   | —                | —                                       |
|        22 | — (new in this revision)   | —                | —                                       |
|     23-33 | — (new 2026-09-09)         | —                | —                                       |

Consequences worth knowing:

- A ~caa-throughput~ commit message saying "PERFORMANCE.org Step 6" means
  canonical *15*, and "Step 7"/"Step 8" mean *16*/*17*.
- ~tests/live/caa-icon-survives-sort-multi.spec.js~ and
  ~tests/live/registry.org~ referenced ~main~'s old Step 6 and now say *18*.
- The changelog's two references ("PERFORMANCE.org Step 3", "Step 4") are
  unaffected — those numbers never moved — so no changelog entry was
  retroactively edited.
- Steps 12 and 14 (~perf-steps-1-4~) and 15 and 16 (~caa-throughput~) are
  marked DONE on their own branches. They are TODO here because their code is
  not on ~main~; each says so in its own text.

** Findings: the per-keystroke filter cost (2026-09-09)

Traced at *9.99.1049+2026-09-08*, which is the version every line reference in
this section and in Steps 23-33 was read at. Prompted by a report that
"specific things happen at every keystroke" while a filter string is typed.

*It is not the debounce.* ~runFilter()~ is debounced through
~_adaptiveFilterDelay()~ (~:43210~) — a 300 ms floor from
~sa_filter_debounce_delay~, plus 0.05 ms/row above 500 rows, capped at 1000 ms.
On the 4174-row ~artist-events~ fixture the adaptive term is only 184 ms, so the
floor wins and a *burst* of keystrokes already collapses into one pass. The
problem is that one pass costs far more than the work it has to do, so every
typing pause over ~300 ms — and every backspace — pays it again in full.

*** One pass makes five independent walks of the same cells

With ~M~ = matching rows and ~C~ = columns, ~runFilter()~ (~:42388-43190~) does:

| # | Walk                                                | Per-cell cost                      | Reached from                                    |
|---+-----------------------------------------------------+------------------------------------+-------------------------------------------------|
| 1 | ~cloneNode(true)~ + ~_stripTransientCellState(td)~  | *12* ~querySelectorAll~/cell       | ~:42814~ single, ~:42620~ multi                 |
| 2 | ~testRowMatch(clone, ctx)~ + ~highlightText(…, -1)~ | ~normalize()~ + text-node TreeWalk | ~:42854~ → ~:41688~, ~:38622~, ~:38785~         |
| 3 | ~applyStickyColumn~ hover-background snapshot       | style write → ~getComputedStyle~   | ~:19262-19268~, via ~_applyPostRenderRowPasses~ |
| 4 | ~_countLiveDateFlags()~                             | one failing ~querySelectorAll~     | ~:32881~, via ~updateFilterButtonsVisibility~   |
| 5 | ~_updateAllColHeaderCounts~                         | ~getCleanColumnText~ per cell      | ~:57088~, via ~initCollapsableColumns~ ~:42910~ |

Walks 2 and 5 re-extract text that walk 1's own cached ~matchOnly~ pass
(~_rowTextCache~, ~:35148~) already extracted and has sitting in a ~WeakMap~.
Walk 3 is a write-then-read layout-thrash loop. Walk 4 produces nothing at all
on every pageType that carries no live-date flags, which is nearly all of them.

*The five are not equals, and this section first implied they were.* Measured
2026-09-09, walk 1 alone is ~1.05 million ~querySelectorAll~ per filter clear —
97.5% of that phase — against ~27 000 for everything else in it combined. See
"Measured, 2026-09-09" below, and Step 34.

Note walk 2's shape specifically: ~testRowMatch~ runs *twice* per surviving row —
once with ~matchOnly=true~ on the source row, where every read goes through
~_rowTextCache~, and once with ~matchOnly=false~ on the clone, where every read
is a fresh ~getCleanColumnText~. The second call is the expensive one and it is
the one with no cache.

On top of the five: ~applyZebraStriping~, ~normalizeCommentSpans~ and
~initBarcodeHighlight~ (three more O(M) walks, ~:42326-42341~);
~_countLengthMismatchRows~ and ~_pendingEditsGroups~, which walk all *source*
rows and so cost the same 4174 no matter how narrow the filter is;
~initPicardTaggerColumn(true)~ and ~initExpandRGsFeature()~ rebuilding every
interactive cell (see Step 23); and an unconditional ~window.scrollTo()~ forced
reflow (~:43011~).

*The size of walk 3 is not an estimate.*
~tests/snapshots/artist-events/rendered.html~ contains *87 738*
~data-mb-rest-bg~ attributes — 4174 rows x 21 cells plus headers, one per
~getComputedStyle~ call. That baseline is 19 MB and this is most of it.

*** Two of these are defects, not merely inefficiencies

*~initPicardTaggerColumn(true)~ defeats Step 3's cache on every keystroke.* Its
rewire pass ends in ~_invalidateUniqDropDataCacheForTable(table)~ (~:74033~),
which also drops ~_colHeaderCountsCache~ (~:52459~ → ~:52548~). ~runFilter~
calls it every pass (~:42748~ multi, ~:42946~ single);
~sa_enable_picard_tagger~ defaults *true* and the only guard is "does this
tbody contain a ~/release/~ link". So on ~artist-releases~,
~artist-releasegroups~, ~releasegroup-releases~, ~series-releases~ and
~collections-releases~, 9.99.1049's memoization never hits at all. *The
interaction-perf harness cannot see this*: ~artist-events~ has no release
links, so the Picard column is skipped there and every committed number was
captured with this cost absent.

*~_countLiveDateFlags()~ tallies the live tbody, not the source rows.* Its own
JSDoc claims the totals "stay a stable page-wide summary independent of
whatever filter happens to be active" (~:32874-32876~), but ~runFilter~ REMOVES
non-matching rows rather than hiding them. This is the identical bug already
fixed for ~_countLengthMismatchRows~, which now counts ~_msSourceRows()~ for
exactly this reason. Its buttons are created unconditionally in
~mb-global-summary-container~ (~:32629~, asserted by
~tests/fixtures/filter-bar-containers.spec.js~), so the early-out at ~:32919~
never fires and the walk always runs, on every pageType.

*** Measured, 2026-09-09 — and it moved the ordering

Tier 0 landed (=tests/support/domCounters.js= + =capture-pass-cost.js=) and the
first arm is =pass-cost-main-9.99.1049-2026-09-09-petri.json=. Full table and
the reproducibility check in =tests/MEASUREMENTS.org=. Three results, two of
which contradict the estimates above — which is what the tier was for.

*=_stripTransientCellState= dominates, and this section under-ranked it.* Stack
sampling attributes *97.5%* of a filter clear's 1 078 372 =querySelectorAll=
calls to it: roughly *1.05 million*, twelve per cell, against ~27 000 for
everything else in the phase combined. Walk 1 is not one of five comparable
walks, it is the walk. Filed as Step 34, which now leads Tier 1.

*Walk 3's estimate was exactly right.* =applyStickyColumn= costs 87 654
=getComputedStyle= calls — 1.00 per cell to the digit, attributed to its inner
=_apply=. Step 24 stands as written.

*Walk 4 was overestimated ~8x.* Step 25 claimed ~87 700 per pass; the measured
figure is *10 500* on a clear, 3318 on an apply, 98 154 across the initial
render. The 10 500 is 500 x 21 — =renderRowsChunked='s chunk prefix — so
=_countLiveDateFlags= is scanning a PARTIALLY rendered tbody, a second defect
on top of the live-tbody tally the step already files. Both are still worth
fixing; the cost argument for doing it early is not.

*A methodological note worth keeping.* The counts also show deferred work
crossing phase boundaries: a clear records zero =getComputedStyle= while the
phase after it records all 87 654, because the sticky-column pass runs after
the status line settles. On an apply the same pass lands inside the apply
phase, since 158 rows finish first. Any arm comparison has to read the two
phases together.

*** The harness cannot see progressive typing at all

~capture-interaction-perf.js~ types with ~input.pressSequentially(value)~ and no
per-character delay. All fourteen characters land within a few ms, the 300 ms
debounce swallows every one of them, and the metric therefore times *one* filter
pass. Every interaction number in ~tests/MEASUREMENTS.org~ is a single-pass
number, and the symptom this section describes — work repeating as you type —
is outside what any committed measurement observes. Recorded in that file too.

** What is already deferred — the idioms to copy

Four working deferrals already exist. Any new one should follow the closest of
them rather than invent a fifth shape.

| Feature                  | How it defers                                                                           | Reusable as                                      |
|--------------------------+-----------------------------------------------------------------------------------------+--------------------------------------------------|
| Millisecond Length       | Opt-in per page via a header button; fetches lazily on first press; five button states  | the model for any "offer it, don't do it" column |
| CAA/EAA big-image strips | ~sa_caa_pics_initially_collapsed~ defaults *true* — loads nothing until toggled         | threshold-free deferral of a whole subsystem     |
| Auto-resize columns      | ~sa_auto_resize_columns_threshold~ (default 2000 rows) skips it entirely, plus a button | the row-count-threshold idiom                    |
| Unique-values dropdown   | Computed on open, then memoized per (table, column, row set) — Step 4                   | compute-on-demand + cache                        |

Two costs are also already better optimised than they look, and should not be
re-attacked: the prose-clamp ~scrollHeight~/~clientHeight~ reads are batched
into one layout flush per column (~:57752-57757~), and
~_updateAllColHeaderCounts~ is already deferred, sliced per column, coalesced
per table and memoized (Steps 3 and 22).

** Recommended order (2026-09-09)

Estimates below are *operation-count* estimates, on the same footing as the
"Ranked areas" table above — they are to be replaced by counts as each step
lands, not quoted as measurements.

*Why Steps 1/2 are not first.* They measured a net loss on narrow filters
(+53-68%), because keeping 4174 rows live made the unconditional ~scrollTo~ lay
out the full row set (219 ms of forced reflow against ~main~'s 35 ms). Step 30
changes exactly that term and Tier 1 removes the walks Fix C was competing
against, so it deserves re-judging — but only afterwards, and on fresh numbers.

*** DONE Tier 0 — make it verifiable

/Landed 2026-09-09./ Counts settle unambiguously what a timing inside the ~10%
noise band cannot — the same argument Step 19 used for its own count-based
metrics.

Written as "add ~__saTest~ counters", which would have meant editing the
userscript. What landed instruments the DOM APIs from the TEST side instead, so
it is genuinely ~tests/~-only with no version bump, and it needs no cooperation
from the code under test — which also means a step cannot accidentally
invalidate its own instrument:

- ~tests/support/domCounters.js~ — wraps ~getComputedStyle~,
  ~querySelector(All)~, ~cloneNode~, ~createTreeWalker~, ~createElement~,
  ~addEventListener~, ~getBoundingClientRect~ and the four layout-forcing
  getters. Totals plus per-selector, per-tag and per-event breakdowns.
  Attribution comes from stack traces sampled 1-in-200, walking to the first
  NAMED userscript frame — taking the first frame outright bucketed
  ~applyStickyColumn~'s 87 654 calls as "unattributed", because its hot loop is
  an anonymous arrow inside a ~forEach~.
- ~tests/support/capture-pass-cost.js~ — four phases on the ~artist-events~
  fixture. *Never run it alongside* ~capture-interaction-perf.js~: the
  instrument wraps hot DOM APIs, so it makes any timing meaningless.
- ~tests/support/runMetadata.js~ — the machine block and archive-filename
  convention, extracted from ~capture-interaction-perf.js~ so two capture
  scripts cannot drift apart on it.

Reproducibility is measured rather than asserted: 28 of 32 phase counters are
bit-identical across runs, and the four that move are one ~getCleanColumnText~
call's worth of header-count scan sliding across a phase boundary. See
~tests/MEASUREMENTS.org~.

Still held back, deliberately: the progressive-typing TIMING arm. Nothing has
yet needed it — every Tier 1 step is falsifiable by count.

*** Tier 1 — the two defects and the two dead walks

Steps *34, 23, 24, 25, 26*. /Reordered 2026-09-09 by Tier 0's first arm:/ Step
34 is new and leads because it turned out to be an order of magnitude larger
than anything else here, and Step 25 drops below 24 because its own cost was
overestimated ~8x. Both defects (23, 25) stay in the tier — a defect does not
have to be the biggest item to be worth fixing early.

- *23* removes a per-row teardown-and-rebuild of ~5 elements per row per pass,
  and restores Step 3's measured -27 to -30% on ~headerCountsRestore~ for every
  release-listing pageType, where today it never applies at all.
- *34* removes ~1.05 million ~querySelectorAll~ per filter clear — 97.5% of
  that phase's total, twelve per cell, against ~27 000 for everything else in
  the phase combined.
- *25* removes ~10 500 failing per-cell ~querySelectorAll~ per clear (measured;
  the estimate here previously read ~87 700 and was ~8x too high), fixes a
  wrong button label under an active filter, and fixes a second defect the
  counts exposed — 10 500 is 500 x 21, so the scan reads a partially rendered
  tbody.
- *24* removes ~87 700 write→~getComputedStyle~ pairs per pass — the most
  expensive per-visit operation of the five walks.
- *26* removes ~8 300 source-row ~querySelector~ per pass. It is the item that
  matters most on a *narrow* filter, which is the case Steps 1/2 measured
  worst on: ~M~ shrinks, these three tallies stay at 4174.

*Before writing any of it*, flip ~sa_enable_picard_tagger~ off and re-run the
same filter on a real release-listing page. That sizes the whole Picard prize —
Steps 23 and 32 together — for free, using a setting that already exists.

Together Tier 1 removes three of the per-cell walks outright and restores the
cache on a fourth. With Step 34 measured rather than estimated the headline is
sharper than the original 40-60%: a filter clear currently issues ~1.08 million
~querySelectorAll~ and 87 654 ~getComputedStyle~, and Steps 34 and 24 alone
account for ~1.14 million of that. Confirm each by count as it lands, then take
ONE A/B timing arm at the end of the tier.

*** Tier 2 — scope and defer

Steps *27*, *28*, then *Step 6*.

- *27*: on a 47-sub-table page, ~1/47 of walks 3, 4 and 6 for a scoped sort.
- *28*: removes walk 5 from every pass but the last of a typing burst —
  4.7-8.2 s of scan per suppressed pass, host depending.
- *Step 6* is a one-function fix that makes the 📊 dropdown's warm cache survive
  a sort (~uniqDropWarm~ ~1 s against ~uniqDropCold~ ~26-46 s). It must NOT
  unify the two signatures — see that step's own text.

*** Tier 3 — structural

Steps *29, 30, 31*, then *Step 10* (sort only; ~10-20x fewer comparator text
extractions) and *Step 9*, which becomes load-bearing the moment Step 29 leans
harder on ~_rowTextCache~.

*** Tier 4 — bigger bets, judged afterwards

| Step        | Standing                                                                                              |
|-------------+-------------------------------------------------------------------------------------------------------|
| 5 (+7, +12) | Large but artwork-only, and a fresh reimplementation. *Step 33 must reuse its observer, not add one.* |
| 33          | Take after 5. Large on any page carrying the Relationships column, which is most listing pageTypes.   |
| 1/2         | Re-judge only after Tier 1 and Step 30 — see "Why Steps 1/2 are not first" above.                     |
| 15/16       | A port from a 290-commit-behind branch, not a design. Artwork pages only.                             |
| 11          | ~release-tracks~ only; ~10-15 full-table scans collapse to one.                                       |
| 13, 20      | Nit; already mitigated by Step 19.                                                                    |
| 17          | Validated manually and FAILED on ~<tr>~. Blocked — read that step's gate before spending time here.   |

*** The original Steps 1-22, consolidated

The tiers above are ordered by what to do next, which scatters the ORIGINAL
steps across four of them. This section is the same plan read the other way —
by step number — because "where did Step 9 go" is otherwise a search.

*Seven need nothing*: 3, 4, 8, 18, 19, 21 and 22 are DONE. *Three are not
orderable work*:

| Step | Why it is not in the running order                                                                         |
|------+------------------------------------------------------------------------------------------------------------|
|    7 | Fixes a gate INSIDE Step 5's ~IntersectionObserver~ code. No observer exists on ~main~ — fold it into 5.   |
|   12 | Fixes a ~ReferenceError~ in ~_artProcessIoEntriesBatched()~, which likewise only exists on the old branch. |
|   17 | Manually validated and FAILED on ~<tr>~. Blocked pending a redesign, not waiting its turn.                 |

The remaining twelve fall into three tracks that do not compete for the same
code, so they can be worked in any interleaving — but each track has its own
internal order.

**** Track A — filter and sort path

Interleaved with the new steps, in the tiers above:

| Order | Step | Sits in | Note                                                                       |
|-------+------+---------+----------------------------------------------------------------------------|
|     1 | 6    | Tier 2  | One function. Makes the 📊 warm cache survive a sort.                      |
|     2 | 9    | Tier 3  | Do it WITH Step 29, not after — see below.                                 |
|     3 | 10   | Tier 3  | Sort only, self-contained, ~10-20x fewer comparator text extractions.      |
|     4 | 1/2  | Tier 4  | Re-judge only after Tier 1 and Step 30, and see the Step 34 overlap below. |

*Step 9 pairs with Step 29 rather than following it.* Today a stale
~_rowTextCache~ entry only mis-matches a row. Step 29 feeds that same cache
into the highlight pass, at which point a stale entry also mis-HIGHLIGHTS one —
a wrong answer the user can see. The invalidation should land in the same
change, not as a follow-up.

**** Track B — CAA/EAA artwork

One track, five steps, best done in one stretch:

*5 (+7, +12) -> 15 -> 16 -> 14 -> 20.*

*Why 5 before 15, even though 15 is smaller and already written.* Step 15
raises image concurrency from 4 to 12 (~sa_caa_img_concurrency~), and the Cover
Art Archive is measured at *~1.2 requests/second* (~tests/MEASUREMENTS.org~,
"Third-party service limits"). Firing more requests concurrently before Step 5
has reduced how many are fired at all is the wrong way round on exactly the
large pages both steps target.

The counter-argument is real but weaker, and worth recording because it is not
obvious: *landing 15 first would make Step 12 disappear by construction.* Step
12's bug is code referencing ~_caaImgQueue~/~_caaMetaQueue~, which is precisely
what Step 15 creates — the crash exists only because that code was written
expecting 15 to have landed. Either order resolves it; 15-first resolves it
without a rename.

Then 16 (IDB off the icon-render hot path, deliberately separated from 15 for
its own risk profile), 14 (in-flight de-duplication — its own verification note
records that the race is narrow and could not be reproduced end-to-end), and 20
last, since Step 19 already removed its observable cost on every path it covers.

**** Track C — opportunistic

- *11* — ~release-tracks~ only, independent of everything. Do it when that
  pageType is the priority, not before.
- *13* — a cdtoc nit, one-click recoverable. Worth folding into whoever next
  touches ~runFilter()~'s single-table branch rather than scheduling on its own.

**** Step 34 overlaps Step 1, and shrinks it

Recorded here because it changes how Step 1 should be judged, and because
nothing in Step 1's own text could have predicted it.

Step 1 stops cloning rows altogether. The ~1.05 million ~querySelectorAll~
calls Step 34 targets are made by ~_stripTransientCellState~ on those clones —
so *Step 1 would remove Step 34's cost as a side effect*, and equally, landing
Step 34 first removes most of what Step 1 still has to win.

That does not reorder anything: Step 34 is cheap and safe, Step 1 measured a net
loss on narrow filters and is neither. But it is the second thing to move under
Step 1's feet, after Step 30 removes the forced-reflow term it actually lost on.
*Re-judge Step 1 against a fresh count, not against this file's original 40-60%
framing* — by the time it is reachable, both of its headline arguments will
have been claimed by cheaper steps.

** Implementation steps

*** TODO Step 1: Filter re-render (Fix C) — single-table mode only

In-place ~display:none~/~display:''~ toggling on live DOM rows instead of
~cloneNode(true)~ + tbody rebuild on every keystroke. Hoist
~initCollapsableColumns()~ (and the CAA/EAA/ERG/CDtoc/nested-~<h2>~ hook
chain it drives) to run once at initial render instead of once per filter
pass. Reference: ~archive/filter-performance-fix-v1-single-table~'s WIP.1
entry — reuse the approach, not the diff (too much of the surrounding code
has changed to apply it directly). Branch: ~filter-performance-fix~.

Scope: single-table mode only (~runFilter()~'s non-multi branch). Multi-table
mode is out of scope for this step — see the "Show single-table" button
note above for why single-table-first still covers the important case.

*Two cheaper steps have since claimed both of this one's headline arguments.*
Step 30 removes the unconditional ~window.scrollTo()~ forced reflow, which is
the term Fix C measurably LOST on (219 ms against ~main~'s 35 ms). Step 34
removes the ~1.05 million ~querySelectorAll~ that ~_stripTransientCellState~
issues on the clones this step would stop making — so most of what Fix C would
win, a far cheaper step wins first. Neither is a reason to drop this step, but
it must be re-judged on a fresh count rather than on this file's original
40-60% framing. See "Step 34 overlaps Step 1, and shrinks it" above.

*** TODO Step 2: Extend the Fix-C approach to Sort

Sort currently still calls ~renderFinalTable(allRows)~ after
~sortLargeArray()~ completes, which triggers the same full tbody rebuild +
~initCollapsableColumns()~ re-run as the old filter path. Not covered by the
old branch (its WIP.1 only touched ~runFilter()~). Apply the same
"rows are live DOM nodes, reorder in place, don't re-run the hook chain
unless something actually changed" treatment to the sort-apply path.

*** DONE Step 3: Cache ~_updateAllColHeaderCounts~

Memoize per-column unique-value/multi-row counts, invalidated only when the
visible row set actually changes (not on every trigger). Needed even after
Steps 1-2, since global expand/collapse-all clicks and multi-table renders
still call it unconditionally.

*Landed on* ~main~ *in 9.99.1049* (merge ~aef7faf~, branch
~perf-step-3-header-count-cache~, now deleted) — see "Implemented" below. Before that,
~_updateAllColHeaderCounts()~ had exactly two mitigations and neither was a
cache: a coalescing token (~_colHeaderCountsToken~, since replaced by a
per-table ~WeakMap~ — see Step 22) so back-to-back schedules cost one scan
instead of three, and one ~await _yieldToEventLoop()~ per column so the pass was
never a single unbroken task. It performed two full ~tbody.rows~ walks per
column on every filter apply and every filter clear.
~_visibleRowSetSignature(table)~ — long described here as the intended cache key
— exists and is used, but only by Step 4's ~_uniqDropDataCache~, and this step
deliberately does NOT share it (see below).

**** Partially mitigated by Step 18 — multi-table sorts only

Step 18 (scoped sub-table sort) skips the whole per-group decorate pass,
~initCollapsableColumns()~ → ~_scheduleColHeaderCounts()~ included, for every
sub-table a sort did not disturb. That is a real reduction in this step's
cost, and it is *correct* rather than merely cheaper: a sort changes a group's
row ORDER, never its row SET, so the counts it would recompute are the ones
already displayed. Filters, which DO change row sets, never take the scoped
path.

Two limits on how much of Step 3 that covers, both important:

- *Multi-table pages only, and only on a sort.* Single-table mode has no
  equivalent narrowing by design (~allRows~ is one array under one cache
  key), and no filter keystroke on any page takes the scoped path.
- *Therefore invisible in the numbers below.* They were captured on
  ~artist-events~, which is ~tableMode: 'single'~. Step 18 cannot move them,
  and did not.

A user-visible side effect worth recording: the 📊 unique-value count badges
now SURVIVE a sort of an unrelated sub-section instead of blanking back to a
placeholder and being re-scheduled. Measured on a saved payload, 19 of 22
badges populated afterwards against 0 of 22 before.

**** Re-costed after the chunked-render fix (branch ~fix-chunked-render-header-counts~)

The estimates above were made while this function was accidentally cheap on
the interactive path. ~runFilter()~ did not ~await~ its own
~renderFinalTable()~, so on a large single table the deferred scan ran against
whatever prefix of the tbody had been appended — a multiple of
~renderRowsChunked()~'s 500-row ~chunkSize~ — and produced visibly wrong
badges (~artist-events~: "Event" showing 984-2500 instead of 4158, "Location"
~.mb-col-collapse-count~ showing 1 instead of 5). That is now fixed: the scan
runs after the render completes, so it genuinely covers all 4174 rows × 21
columns (~88 000 ~getCleanColumnText()~ calls) on every filter apply and every
filter clear, not ~25% of that.

Two mitigations landed with the fix, both of which Step 3 can build on:
- ~_scheduleColHeaderCounts(table)~ — the single entry point. Waits out any
  in-flight render (so a scan can never see a partial tbody again) and
  coalesces repeat schedules via ~_colHeaderCountsToken~;
  ~initReleaseEventsColumn()~ alone fires ~initCollapsableColumns()~ three
  times in a row and now costs one scan instead of three.
- ~_updateAllColHeaderCounts()~ is ~async~ and yields one turn per column,
  aborting at the next column boundary once its token is stale — so the full
  scan is never one unbroken multi-second task.

*Measured, 2026-09-01, same machine / same session, back-to-back* via
~tests/support/capture-interaction-perf.js --pageType=artist-events~ (4174
rows, 21 columns, median of 5, fresh page per sample) — ~main~'s script (the
working tree with only ~ShowAllEntityData.user.js~ stashed) vs this branch's:

| Interaction         | ~main~   | this branch |           Δ |
|---------------------+----------+-------------+-------------|
| Global filter       | 1307 ms  | 1752 ms     |        +34% |
| Column filter       | 1335 ms  | 1891 ms     |        +42% |
| Sort                | 3103 ms  | 4241 ms     |        +37% |
| Uniq-dropdown, cold | 29450 ms | 30920 ms    | +5% (noise) |
| Uniq-dropdown, warm | 553 ms   | 914 ms      |        +65% |

*Both columns are v9.99.976 numbers, and only the right-hand one was ever
committed* — as ~interaction-perf-fix-chunked-render-header-counts-9.99.976-2026-08-31.json~,
under the name of a branch that has since been merged and deleted. The ~main~
column exists only as this table. Neither has been re-measured since, so read
them as "the cost of the chunked-render fix, at the version it landed", not as
current absolute latencies; ~main~ is now 69 versions further on. See Step
18's "Measurement coverage" note for what the harness can and cannot see
today.

Read these as a *correctness cost, not a regression in the work being done*:
the scan they are paying for used to cover a 500-row prefix and produce wrong
numbers, and now covers all 4174 rows. Note also what the harness actually
measures — it polls ~#mb-filter-status-display~ over CDP every 100 ms and
needs two stable reads, so it is sensitive to main-thread pressure. The
status text and the visible row set still update at exactly the same point in
~runFilter()~ as before (that path stayed synchronous and untouched); what
grew is the background work running after it, which delays the poll. So the
deltas above overstate the change in perceived filter latency, but they do
represent real extra main-thread work.

**** Re-measured 2026-09-07 at 9.99.1045: the cost is still being paid

The table above was captured at v9.99.976 and never re-run, so it could not
say whether the +34%/+42%/+37% was still real. It is. Same harness, same
fixture, same machine, median of 5 — and this arm IS committed, as
~interaction-perf-main-9.99.1045-2026-09-07.json~ (the plain
~interaction-perf-main.json~ name has since been overwritten by later arms
and finally retired in favour of a version+date[-host]-qualified name for
every arm — see the "Baselines on disk" table below):

| Interaction         | pre-fix (v9.99.976) | post-fix (v9.99.976) | now (v9.99.1045) | vs pre-fix |
|---------------------+---------------------+----------------------+------------------+------------|
| Global filter       | 1307 ms             | 1752 ms              | 1735 ms          |       +33% |
| Column filter       | 1335 ms             | 1891 ms              | 1730 ms          |       +30% |
| Sort                | 3103 ms             | 4241 ms              | 3968 ms          |       +28% |
| Uniq-dropdown, cold | 29450 ms            | 30920 ms             | 31149 ms         |        +6% |
| Uniq-dropdown, warm | 553 ms              | 914 ms               | 863 ms           |       +56% |

Three conclusions, all of which the older table could only guess at:

- *Step 3 is still owed the whole of it.* 69 versions of other work — Step 4,
  Step 18, the entire Step 19 artwork series — moved these numbers by a few
  percent at most. Column filter and sort came down slightly from the v9.99.976
  post-fix figures, but both remain roughly 30% above the pre-fix baseline.
  Nothing has substituted for the memoization.
- *Steps 18 and 19 are invisible here, exactly as predicted*, and the flatness
  is the evidence for the prediction rather than against the work. See Step
  18's "Measurement coverage" note for why this page cannot see either of them.
- *The warm-dropdown figure is Step 4's, and it is holding.* 863 ms against the
  31 149 ms cold open is the ~36x that step bought, still intact 74 versions
  later.

Bringing that cost back down is precisely this step. The two follow-ups that
would help most, deliberately left out of the fix branch as separate concerns:
- invert the loop to row-outer/column-inner with per-column accumulators — the
  current column-outer shape materialises ~Array.from(tbody.rows)~ up to 42
  times per pass;
- memoize on ~_visibleRowSetSignature(table)~, the primitive Step 4 already
  uses, invalidated wherever ~_invalidateUniqDropDataCacheForTable()~ already
  is (cell content can change with no row show/hide). Step 6 must land with or
  before this, or a sort will invalidate the new cache on every click.

/Both were done, and the second one differently than written — the shared
signature turned out not to be the right key and Step 6 not to be a
prerequisite. See below./

**** Implemented — branch ~perf-step-3-header-count-cache~, 2026-09-07

Shipped shape, and the three places it deviates from the plan above:

- *Its own signature, not* ~_visibleRowSetSignature()~. Every prediction in this
  file assumed the two would share a key, and Step 6 (making that helper
  order-independent) was listed as a prerequisite. It is not one.
  ~_colHeaderCountsRowSetSignature()~ sorts the visible ~data-mb-row-idx~ values
  before joining, which is sound *for these three outputs specifically* — a
  distinct-value count, a multi-row-cell count and an inline-art flag are all
  order-independent — while Step 4's bundle genuinely is not
  (~entityNameGlyphMap~ and friends are first-seen-in-row-order). Both helpers
  now carry a pointer to the other saying not to unify them. So Step 6 stays
  open, and Step 3 no longer waits on it.
- *The last four row sets, not the last one.* A single slot makes the cycle
  people actually perform — filter, clear, filter — miss on every leg. The
  signatures are the only real weight, one row-index list each.
- *A superseded pass publishes nothing.* The never-merged ~perf-steps-3-4~
  sketch (~8132bcd~) stored its key before the loop; since a scan abandons
  itself at a column boundary when its token goes stale, that would serve a
  half-filled result as complete for as long as the row set stayed put.

Invalidation is delegated from ~_invalidateUniqDropDataCache()~/~…ForTable()~
rather than duplicated across their dozen call sites, which is sound by a subset
argument: this cache reads clean cell text, list/prose structure and inline-art
placeholder presence, and ~openUniqDrop()~'s bundle reads all three and more.
That closes Step 8's remaining header-count half by construction. It also
uncovered one site that invalidated *neither* cache —
~_maybeCorrectAreaFlagRegion()~, which rewrites two cells' text up to six
seconds after render — fixed here.

***** Measured, 2026-09-08, same machine / same session, median of 5

~tests/support/capture-interaction-perf.js --pageType=artist-events~ (4174 rows,
21 columns, fresh page per sample). ~main~ is 9.99.1048 with only
~ShowAllEntityData.user.js~ swapped, so both arms share the harness. This pair is
the SHIPPED configuration — committed as ~interaction-perf-main-9.99.1048-2026-09-08.json~ and
~interaction-perf-perf-step-3-header-count-cache-9.99.1048-2026-09-08.json~.

| Interaction            |   ~main~ | this branch |        Δ |
|------------------------+----------+-------------+----------|
| Global filter          |  3033 ms |     3168 ms |    +4.5% |
| Column filter          |  3295 ms |     3337 ms |    +1.3% |
| Sort                   |  6410 ms |     6469 ms |    +0.9% |
| Uniq-dropdown, cold    | 45996 ms |    46516 ms |    +1.1% |
| Uniq-dropdown, warm    |  1676 ms |     1747 ms |    +4.2% |
| Header counts, initial |  8033 ms |     7980 ms |    -0.7% |
| Header counts, restore | 12319 ms |     8977 ms | *-27.1%* |

*These are NOT comparable to the 9.99.1045 figures earlier in this file*, which
read 1735/1730/3968/31149/863 — roughly half of what the same ~main~ produces
here. *That gap has since been bisected and is not the script*: the SAME
9.99.1045 userscript, re-run on ~petri~ on 2026-09-08, produced
3117/3285/6044/42948/1640 — within noise of 9.99.1048, exonerating the CAA/EAA
fixes in 9.99.1046-1047. The environment carries all of it, and WHICH part of
the environment was narrowed on 2026-09-08: rebooting ~petri~ restored
1.37-1.77x across every metric, while removing a resident Claude Code session
changed essentially nothing. Reboot recency is the leading candidate; the
older arm still never recorded its host, so that is a mechanism which
reproduces, not a confirmed account of that particular arm. See
~tests/MEASUREMENTS.org~. Only a within-session ratio means anything.

*Read every row but the last as noise.* ~main~ alone was captured three times
across the two sessions — 3757/3503/3033 on the global filter, 7393/6932/6410 on
sort — a spread wider than any delta above except ~headerCountsRestore~. The
2026-09-07 arm, kept as ~interaction-perf-main-9.99.1048-2026-09-07.json~, showed the same
shape with a +6.2% column filter that did not reproduce here (+1.3%).

***** The win is narrow, and where it lands is the interesting part

Only ~headerCountsRestore~ moved materially: about 3.7 s of scan removed from a
filter-clear. Everything else is flat, and that is *correct behaviour, not a
disappointing result* — it falls straight out of when a full-table scan actually
happens:

- *Initial render* — a cold miss by definition. The -5.2% there is the two
  micro-costs (the per-column ~Array.from(tbody.rows)~, materialised up to 42
  times per pass, and the per-cell inline-art probe, now behind one lazy
  table-wide ~querySelector~), not the cache.
- *Filter cleared back to the full set* — the LRU hit, and the -29.2%.
- *Everything else* — the scan runs over an already-filtered row set that is
  small by construction. There was never seconds of work there to remove.

That last point invalidated the premise this branch started from. The plan
predicted the filter metrics would improve; they cannot, because filtering is
not where the expensive scan is.

***** Two new metrics, and why the first version of them was wrong

The five original metrics observe this scan only indirectly, as main-thread
pressure delaying a CDP poll of ~#mb-filter-status-display~.
~headerCountsInitial~/~headerCountsRestore~ watch it directly.

Their first version waited only for the ~Event~ badge to reach its true 4158 and
reported *1884 ms* — while most of the header was still blank. The same
too-narrow wait made this branch's own sort regression test report "sorting
changed 15 badges", when the sort had in fact *completed* a scan the test caught
mid-flight. Both now wait for the value AND for every badge to stop changing.
That leaves a ~1 s floor from ~waitForColHeaderCountsStable()~, identical on
every arm, so it compresses a ratio and never inflates one.

***** A per-cell tier was built, measured, and removed

An additional tier memoized ~getCleanColumnText()~ per ~data-mb-row-idx~, to
make a cache MISS cheap. The arm above was captured with it
(~interaction-perf-step3-l1-l2-9.99.1048-2026-09-07.json~). It earned nothing — for the reason in
"where the win lands": the only full-table scans are a cold miss, where the memo
is empty and pure overhead, and an LRU hit, where it is never read. Against that
it cost roughly a string per rendered cell (~88 000 on this page) and a second
staleness surface. Removed.

*Both configurations have now been measured, and they agree.* The 2026-09-07
arm carrying the per-cell tier restored the header counts in 8944 ms; the
shipped tier-1-only code does it in 8977 ms — a 0.4% difference against a
comparator spread several times that. The tier contributed nothing, exactly as
predicted from where the win lands, and its removal cost nothing.

*** DONE Step 4: ~openUniqDrop()~ per-open caching

~openUniqDrop()~ does exactly 5 full ~tbody.rows~ passes from scratch on
every dropdown open with zero caching (the whole-cell value scan, the
flag-icon segment map, the relationship-cell detector, the relationship
icon counts, and the inline-CAA/EAA-thumbnail scan).

Originally written as "same caching primitive as Step 3 — build alongside
it", and that is how it first landed (branch ~filter-performance-fix~, later
~perf-steps-1-4~; also cherry-picked as the pair ~perf-steps-3-4~). It does
NOT actually need Step 3, though — see below.

*Landed*: branch ~worktree-perf-step-4~, standalone, WITHOUT Step 3. See
~ShowAllEntityData_CHANGELOG.json~ v9.99.973 (the port), v9.99.974 (the
async-column cache invalidations) and v9.99.975 (the CAA/EAA-cell one added
while rebasing onto ~main~).

- *Measured, 2026-08-31, same machine / same session / same script version
  (9.99.970)* — ~main~'s script vs this branch's, both captured back-to-back
  via ~tests/support/capture-interaction-perf.js --pageType=artist-events~
  (4174 rows, 21 columns, median of 5, fresh page per sample):

  | Interaction           | ~main~   | this branch | Δ                 |
  |-----------------------+----------+-------------+-------------------|
  | Global filter         | 1242 ms  | 1292 ms     | noise             |
  | Column filter         | 1201 ms  | 1239 ms     | noise             |
  | Sort                  | 2995 ms  | 2906 ms     | noise             |
  | Uniq-dropdown, cold   | 30649 ms | 32985 ms    | +7.6% (see below) |
  | *Uniq-dropdown, warm* | 31347 ms | *581 ms*    | *54x faster*      |

  The branch result is committed as
  ~tests/snapshots/artist-events/interaction-perf-worktree-perf-step-4-9.99.970-2026-08-31.json~.
  The ~main~ column was measured for this comparison and NOT committed — the
  2026-08-24 / 9.99.953 capture from a different machine
  (1418/1595/2994/29802/29700) was deliberately left untouched, and now lives
  at ~interaction-perf-main-9.99.953-2026-08-24.json~ (renamed when a real
  current-~main~ capture finally took the plain ~interaction-perf-main.json~
  name — see Step 18's "Measurement coverage" note; that plain name has since
  been retired too, in favour of a version+date[-host]-qualified name for
  every arm). It is not directly
  comparable, since ~main~ had since grown 17 versions of extra dropdown work
  (~roleTokenValueCounts~, the editor/changelog/data-quality scans, several
  ~SYN_SECTION_META~ splits) that a cold open has to pay.

  On the cold ~+7.6%~: treated as noise, not a regression. Cold open is
  ~30s of 5 full ~tbody.rows~ passes over 4174 rows; observed run-to-run
  spread for that same path is ~27.3-33.0s across sessions (~20%). What
  this step adds to a MISS is one O(rows) signature build plus one
  ~Map.set~ — sub-millisecond against a 30-second scan, with no mechanism
  that could cost seconds. Worth re-checking if it ever reproduces
  consistently.

- *Why Step 4 alone.* The earlier 2026-08-24 session had already shown this
  on ~perf-steps-3-4~ (= ~main~ + Steps 3 and 4 only): warm uniq-dropdown
  open 29 700 ms -> 739 ms, filter/sort unchanged within noise, and
  ~post-filter.html~/~post-sort.html~ byte-for-byte identical to ~main~'s
  baselines. That entire win is Step 4's:
  ~_updateAllColHeaderCounts()~ — the function Step 3 caches — has only two
  call sites, both ~setTimeout(…, 0)~ at the tail of the collapse-column
  init path, and neither is reachable from ~openUniqDrop()~. Steps 1-2, by
  contrast, measured as a real tradeoff (+53-68% on narrow filters, +23% on
  sort), so this branch deliberately carries neither.
- *The one thing Step 4 needed from Step 3* is the helper
  ~_visibleRowSetSignature(table)~ — a cheap O(rows) join of the visible
  rows' ~data-mb-row-idx~ values, which is the cache key. It is carried here
  standalone (9-line function). Step 3 was expected to key off the same
  primitive when it landed; it did not, and deliberately — the header counts
  are order-INDEPENDENT, so they need a signature that a sort does not change,
  which this one is not. See ~_colHeaderCountsRowSetSignature()~ and Step 3's
  own "Implemented" section.
- *Implementation shape*, deliberately minimal: a per-(table, column)
  ~WeakMap~ (~_uniqDropDataCache~) whose entries are tagged with the
  signature they were computed against. All 5 passes are gated behind a
  single ~_uniqCacheHit~ check computed once at the top of the function.
  Each Map/counter *declaration* became a
  ~_uniqCacheHit ? _uniqCacheHit.name : <original initial value>~ ternary in
  place and each scan loop got an ~if (!_uniqCacheHit)~ guard — zero changes
  to the hundreds of ~.set()~/~.get()~/~++~ call sites inside the loop
  bodies. Keying by the live ~<table>~ element means a brand-new table (new
  fetch, disk-load, multi-table sub-table) is always a cache miss with no
  manual reset needed.
- *Correctness hazard 1 — ~expandedCells~.* The "Cell structure" section's
  collapsed/expanded split is read from ~expandedCells~ (a Map of
  individually toggled cells), NOT re-derived from the visible-row
  signature — so one cell's expand/collapse click changes those counts
  WITHOUT changing which rows are visible. Fixed with
  ~_invalidateUniqDropDataCache(table, colIndex)~ at all 5 places
  ~expandedCells~ is mutated by user interaction (both branches of
  ~_applyCollapseState()~, all 3 branches of ~ensureCollapseDelegate()~'s
  inline per-cell click handling). A 6th mutation site
  (~initCollapsableColumns()~'s one-time "start expanded by default"
  pre-population) needs none — it runs before any dropdown cache for that
  table could exist.
- *Correctness hazard 2 — async-populated columns* (this is
  PERFORMANCE.org's own Step 8 for the uniq-drop cache, folded in here
  rather than left as a known-shipped bug). Several columns fill
  previously-empty cells long after the initial render with no row
  show/hide at all, which the signature cannot see. Added a table-wide
  ~_invalidateUniqDropDataCacheForTable(table)~ and called it from
  ~initReleaseEventsColumn()~ (both post-population re-scan sites),
  ~initRelationshipsColumn()~ (completion handler) and
  ~initPicardTaggerColumn()~; plus a per-cell
  ~_invalidateUniqDropDataCache()~ in ~_artSetInlineSortKey()~, which stamps
  the ~.mb-inline-art-sort-key~ sentinel the inline-art pass reads from ~8
  CAA/EAA fetch-completion callbacks.

  Added after rebasing onto ~main~'s v9.99.971/v9.99.972 (the CAA/EAA
  multi-table type/comment filter fix): a sixth site,
  ~_artBuildMultiRowArtCell()~ — both its REBUILD and FIRST-BUILD branches.
  It rebuilds a cell's image ~<li>~s, per-image type/comment badges and
  ~ul.dataset.mbArtSearch~ asynchronously with no row show/hide, and the
  dropdown reads all three (~valueCounts~ via ~getCleanColumnText()~,
  ~artTypeValueCounts~/~artCommentValueCounts~ via the badges). That commit
  independently added a ~_rowTextCache~ invalidation at the same spot —
  confirmation that this call site needs one; it just didn't know about
  ~_uniqDropDataCache~. This is the CAA/EAA-column counterpart of the
  inline-art column's ~_artSetInlineSortKey()~ case, and unlike the adjacent
  ~_artSyncSearchTextToSourceRow()~ (multi-table only by design) it covers
  single-table pages too.
- *Port-specific fix — 10 symbols the earlier branches missed.* The Step 4
  code as it stands on ~perf-steps-1-4~ gates and stores 53 names but leaves
  10 more that are ALSO populated inside the (now skipped) pass-1 loop
  ungated and unstored: ~editorDeletedValueCounts~, ~editorAnyDeletedCount~,
  ~editorRecordedNameValueCounts~, ~editorMembershipValueCounts~,
  ~editorCommentValueCounts~, ~changelogHasMessageCount~,
  ~changelogNoMessageCount~, ~releaseQualityHighCount~,
  ~releaseQualityLowCount~, ~releaseQualityNormalCount~. On a warm reopen
  they all read 0/empty, so the dropdown silently loses "🗑️ any deleted
  editor", "📜 has changelog message", "🚫 no changelog specified",
  "🟢/🟠/⚪ data quality" and every ~editordeleted~/~editorrecordedname~/
  ~editormembership~/~editorcomment~ value entry — and because those same
  counters gate the section-visibility conditions, a whole section can
  collapse to empty. Identical failure mode to the ~roleTokenValueCounts~
  miss recorded on the earlier branches. Fixed here; *still open on
  ~perf-steps-1-4~* — see "Follow-ups" below.
- *Not included*: making ~_visibleRowSetSignature~ order-independent. It
  concatenates in DOM order, so a sort produces a different string and
  forces a recompute. On ~main~ (no Step 1) a sort rebuilds the tbody
  anyway, so this is a missed extra win, not a regression versus ~main~.
  Doing it properly has to account for several cached values that are
  themselves first-seen-in-row-order (~entityNameGlyphMap~,
  ~entityNameTypeMap~, ~entityNameFlagMap~, ~valueItemSequence~), so it
  belongs in its own step — that is Step 6, harvested below. Note Step 6's
  own text (written on ~perf-steps-1-4~, where Steps 1, 3 and 4 are all
  landed) calls this "the highest-impact regression"; the sentence above is
  the accurate reading for ~main~, and Step 6 carries a note saying so.

**** Follow-ups

- The 10-symbol gap above is a LIVE bug on ~perf-steps-1-4~ (and on
  ~perf-steps-3-4~), not merely a porting hazard — it should be fixed there
  too, or those branches rebased onto this one.
- Step 3 (~_updateAllColHeaderCounts~ caching) landed in 9.99.1049 — on its
  own signature rather than this branch's ~_visibleRowSetSignature~, for the
  reason given in the bullet above.
- The full multi-step analysis — Steps 1/2/5's measurements, the traced
  forced-reflow root cause, the CSS-containment negative result, and the
  2026-08-25 code-review findings — used to live only on the
  ~perf-steps-1-4~ copy of this file, deliberately unmerged because it
  asserts things about Steps 1/2/3/5 that are not true of ~main~. It has
  since been harvested into this file instead of left there, because a
  branch is a poor place to keep a negative result: ~debug/perf.org~
  (2026-08-24) proposed re-running the very ~content-visibility~ experiment
  that ~filter-performance-fix-caa-throughput~ had already recorded as
  FAILED, with no trace of that failure on ~main~ to prevent it. Everything
  harvested is marked with the branch and version it was measured on, and
  with the fact that its code is not on ~main~ — see the Step-number
  provenance table.

*** TODO Step 5: Lazy CAA/EAA loading (IntersectionObserver)

Defer CAA/EAA icon loads + JSON-API enrichment until a row's art cell
scrolls near the viewport, instead of firing for every row at initial
render. Reference: ~archive/filter-performance-fix-v1-single-table~'s WIP.2
("Pattern 5") — ~sa_caa_io_threshold~ setting, per-ctx persistent observer.
Independent of Steps 1-4 (different subsystem); can be done in any order
relative to them.

*** TODO Step 6: Fix ~_visibleRowSetSignature~ order-dependence

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script v9.99.976). A
proposal: no implementation exists on that branch or on ~main~.

*Severity differs between* ~main~ *and that branch, and the branch text below
overstates it for* ~main~ *twice over.* On ~perf-steps-1-4~ Steps 1, 3 and 4 are
all landed, so an order-dependent signature really does throw away two shipped
caching wins on every sort — hence "highest-impact regression" below.

Neither half of that holds here:

- *It costs Step 3 nothing, at all.* The branch text assumes Step 3 keys off
  this same helper, which was this file's own plan until Step 3 actually landed
  in 9.99.1049 and deliberately did not. It carries
  ~_colHeaderCountsRowSetSignature()~, which sorts before joining precisely so a
  sort is a HIT. Step 3 is not a stakeholder in this step and was never blocked
  on it — the "Step 6 must land with or before this" note that used to sit in
  Step 3's follow-ups was wrong.
- *For Step 4 it stays a missed win, not a regression*, per ~main~'s own note
  (see Step 4, "Not included"): /"On ~main~ (no Step 1) a sort rebuilds the
  tbody anyway."/ That becomes a real regression the moment Step 1 lands.

So this is now a single-caller fix benefiting ~openUniqDrop()~ alone, and the
one thing it must NOT do is unify the two signatures: Step 4's cached values
(~entityNameGlyphMap~, ~valueItemSequence~ and friends) are
first-seen-in-row-order, which is exactly why it still has an order-dependent
key while Step 3 does not.

Root-cause fix for the highest-impact regression found in the 2026-08-25
code-review (see "Findings from performance code-review" below, regression
#1) — it silently undoes Steps 3 and 4's caching wins after any sort. Make
the signature genuinely order-independent (e.g. sort the visible
~data-mb-row-idx~ values before joining, or hash as an unordered set)
rather than concatenating in DOM/current-row-order. Highest priority of
this batch: it's a one-function fix that restores two already-shipped,
already-measured wins rather than adding a new one.

*** TODO Step 7: Fix Step 5's multi-table lazy-load gate

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976). A proposal: no implementation exists on that branch or on ~main~.

~_useIO~ (~:63104~) checks ~allRows.length~, which is always 0 in
~tableMode: 'multi'~ mode, so lazy CAA/EAA loading never activates on any
multi-table art page (e.g. ~user-ratings~) regardless of row count — see
"Findings" regression #2. Fix: compute the row count from ~groupedRows~
(summed across sub-tables) when in multi-table mode, so the threshold
check means something there too.

*Moot until Step 5 lands.* This corrects a gate inside Step 5's
~IntersectionObserver~ code, and there is no ~IntersectionObserver~ anywhere in
~main~ — so there is nothing here to fix yet. Fold it into Step 5 when that
step is reimplemented; it is not independent work.

*** DONE Step 8: Invalidate Step 3/4 caches for async-populated columns

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script v9.99.976),
where it was a proposal with no implementation. Both halves are now on ~main~
— see the closing note.

~initReleaseEventsColumn()~ invalidates Step 3's header-count cache but not
Step 4's uniq-drop cache (regression #3); ~initRelationshipsColumn()~/
~initPicardTaggerColumn()~ invalidate neither (regression #4, pre-existing
gap for Step 3, new gap for Step 4). Add
~_invalidateUniqDropDataCache(table, colIndex)~ calls alongside the
existing ~_invalidateColHeaderCountsCache~ call in
~initReleaseEventsColumn()~, and add both invalidation calls to
~initRelationshipsColumn()~/~initPicardTaggerColumn()~'s post-population
paths.

*Extended by the 2026-08-25 cloud ultra review*: a third uninvalidated site
— ~_artSetInlineSortKey()~ (~:63570~), called from ~8 CAA/EAA
fetch-completion callbacks, stamps the ~.mb-inline-art-sort-key~ sentinel
that ~openUniqDrop~'s inline-art scan reads, with no corresponding
invalidation call either. Same fix shape: call
~_invalidateUniqDropDataCache(table, colIndex)~ there too.

*All of this is on* ~main~, in two releases. The uniq-drop cache half shipped
with Step 4, with invalidations at every site named above plus three the branch text did
not know about — see Step 4's "Correctness hazard 2", which cites this step by
number and lists what it folded in. The header-count half came with Step 3 in
9.99.1049, and by construction rather than by another audit:
~_invalidateUniqDropDataCache()~/~…ForTable()~ now also drop the header-count
entry, which is sound because the header counts' inputs are a strict subset of
the uniq-drop bundle's. That fold also turned up one site invalidating NEITHER
cache — ~_maybeCorrectAreaFlagRegion()~ — fixed in the same release. *Nothing
remains of this step.*

*** TODO Step 9: Invalidate ~_rowTextCache~ for async-populated columns

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976). A proposal: no implementation exists on that branch or on ~main~.

Pre-existing gap (regression #5), not introduced by this branch, but worth
fixing alongside Step 8 since it's the same shape of bug (cache built from
pre-population placeholder content, no invalidation hook when the async
column later fills in). Lower priority than Steps 6-8 — only bites when a
user filters before an async column finishes loading, a narrower window
than the always-on caches above.

*Land this WITH Step 29, not after it.* Today a stale ~_rowTextCache~ entry
only mis-matches a row. Step 29 feeds that same cache into the highlight pass,
at which point a stale entry also mis-HIGHLIGHTS one — a visibly wrong answer
rather than a silently missing match. The severity assessment above is accurate
for ~main~ today and stops being accurate the moment Step 29 lands.

*** TODO Step 10: Precompute sort keys before ~sortLargeArray~

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976). A proposal: no implementation exists on that branch or on ~main~.

Candidate opportunity #1 from the 2026-08-25 code-review. Decorate each row
with its per-column clean sort-key text (and cache each column's
~isNumeric~) once before sorting, instead of re-deriving both inside the
comparator on every pairwise comparison — likely the largest uninvestigated
contributor to sort's remaining ~23% regression vs ~main~ noted in the
Session-summary Takeaways below. Needs before/after DevTools profiling on
~artist-events~ to confirm, same as prior steps.

*** TODO Step 11: Combine ~applyExtractTrackTitleData~'s presence-check scans

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976). A proposal: no implementation exists on that branch or on ~main~.

Candidate opportunity #2 from the 2026-08-25 code-review. Replace the
~10-15 independent full-table ~_anyTitleCellMatches()~ calls (one per
AR-column presence check) with one combined per-track characterization
pass that answers all of them in a single walk. ~release-tracks~-specific;
independent of Steps 1-9.

*** TODO Step 12: Fix undeclared ~_caaImgQueue~/~_caaMetaQueue~ crash in Step 5 (urgent)

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976), where this step is marked ~DONE~. The code is NOT on ~main~, so it
is ~TODO~ here: per this file's preamble the keyword tracks whether a step has
landed on ~main~, not whether anyone ever wrote it.

Found by the 2026-08-25 cloud ultra review — not a missed optimization, a
live bug in already-~DONE~ code. ~_artProcessIoEntriesBatched()~ (added by
the animation-frame-batching fix under Step 5, ~:61449-61470~) references
~_caaImgQueue~/~_caaMetaQueue~, neither of which is declared anywhere in
the file — the real queue is ~_caaQueue~ (~:58083~), which every other call
site (including this function's own eager-path sibling
~_artInitSmallPics~) uses correctly. Under ~'use strict'~, reading either
undeclared name throws ~ReferenceError~ the moment the IntersectionObserver
callback first fires — the ~if (_caaImgQueue)~ guard does NOT save it,
since reading an undeclared identifier throws before the condition can be
evaluated. Net effect: on exactly the large single-table CAA/EAA page Step
5 targets (~sa_caa_io_threshold~ default 500, e.g. ~artist-events~' 4174
rows), the *only* code path that loads an icon after initial render is
broken — no icon ever loads once the observer starts firing, and the
onIdle queue-completion signal never fires either. Missed by this branch's
own Step-5 measurement session because the dedicated CAA-queue-completion
timing predates the animation-frame-batching commit, and the regular
filter/sort perf snapshots seed ~sa_caa_pics_small: false~. Fix: rename
both references to ~_caaQueue~ (two-line change, confined to
~_artProcessIoEntriesBatched()~).

*Moot until Step 5 lands.* The crashing function,
~_artProcessIoEntriesBatched()~, was added by Step 5's animation-frame batching
fix and exists only on that branch — ~main~ has neither. This is recorded so
the crash is not reintroduced when Step 5 is reimplemented, not as work
outstanding against ~main~.

*Fixed*: both references in ~_artProcessIoEntriesBatched()~ renamed to the
real ~_caaQueue~, matching every other call site's guard shape.

*** TODO Step 13: Restore cdtoc sub-row expansion state across a filter hide→show cycle

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976). A proposal: no implementation exists on that branch or on ~main~.

Found by the 2026-08-25 cloud ultra review. ~runFilter()~'s single-table
branch (~:37125-37145~) explicitly hides a cdtoc tracklist sub-row when its
parent row no longer matches (correct — prevents an orphaned visible
sub-row), but the matched/show branch never restores it, even though the
adjacent comment claims "a visible row's sub-row is left exactly as the
user last toggled it." Since the sub-row's live inline ~style.display~ is
the ONLY record of the user's toggle state (unlike per-cell collapse
toggles, which persist to the ~expandedCells~ Map), the hide branch's own
overwrite permanently destroys that state — a user-expanded tracklist that
gets hidden by a filter and later re-shown comes back collapsed, with its
own toggle link still (incorrectly) reading "hide tracklist" until clicked
twice. Nit severity — narrow (cdtoc pageType only, specific action
sequence), one-click recoverable, and strictly better than pre-Step-1
behaviour (which lost the same state on every keystroke via a different
mechanism). Fix: stamp a marker (e.g.
~sub-row.dataset.mbCdtocUserOpen = '1'~) at the hide site when the sub-row
isn't already ~display: none~, and restore to ~''~ in the show branch iff
that marker is set.

*** TODO Step 14: In-flight de-duplication for the shared CAA/EAA image cache

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31, script
v9.99.976), where this step is marked ~DONE~. The code is NOT on ~main~, so it
is ~TODO~ here: per this file's preamble the keyword tracks whether a step has
landed on ~main~, not whether anyone ever wrote it.

Landed on branch ~caa-eaa-icon-fetch-dedup~ (branched off this branch's tip,
not ~main~ — see that branch's own commit history for the full change).

Prompted by a user question: on a page whose "Release"-family column shows
an inline CAA front-cover thumbnail AND has a separate "CAA" column
(~addCAA~), can the CAA column's "main" icon just reuse the inline icon's
already-fetched image instead of fetching it again? Investigation found the
two DO already fetch the byte-identical front-thumbnail URL
(~ctx.archiveHost + entityPath + '/front-' + size~) through the same shared
three-tier cache, ~_artFetchCachedImage()~ (~:58705~ before this change) —
so on any table where the inline pass (always run first, never IO-gated)
has time to resolve before the CAA-column's own icon task is dequeued from
the shared ~_caaQueue~ (true for essentially all tables above trivial
size), reuse was already happening for free via Tier 1 (in-memory) / Tier 2
(IndexedDB). The only real gap was a narrow race: ~_artFetchCachedImage~ had
no in-flight de-duplication, so on small tables (row count ≲
~sa_caa_fetch_concurrency~, default 4) a row's inline task and its
CAA-column icon task could both miss Tier 1/2 and independently issue a
real network fetch for the same URL.

Note the count/type enrichment (image count, back cover, medium, spine,
booklet, …) can NEVER be served from this reuse — it comes from a
structurally different endpoint (~ctx.apiHost + entityPath~, full JSON via
~_artEnrichIcon~) than the front-thumbnail binary URL. Out of scope by
construction, not a gap left open by this step.

Fix: added an ~_artInFlightFetches~ Map (normalised URL → in-progress
promise) inside ~_artFetchCachedImage~. A concurrent call for a URL already
in flight joins that same promise instead of starting a second IDB read /
network fetch; the Tier 2/3 body was split out into
~_artFetchUncachedImage()~ so the wrapping/bookkeeping stays in one place.
No call-site changes — ~_artLoadIcon~, the inline ~loadTask~, and
~_caaFetchStats~ are all unaffected; a joined follower call transparently
resolves with whatever ~{fromIdb, fromMemory}~ the leading call determined.
Applies uniformly to CAA and EAA, and to both the inline and column
pipelines, since all four go through this one shared function. Also applies
to a THIRD consumer of the same cache found while verifying this step: the
release-group bigbox strip (~_artInitBigPics~ → ~_loadBigImgIdb~, ~:62282~
before this change) fetches the identical front-thumbnail URL too (its
~sa_caa_big_img_size~ setting defaults to the same 250 as
~sa_caa_small_img_size~), so all three now share the same de-dup.

**Verification finding, worth recording**: reproducing the concurrent-
overlap window end-to-end through the live CAA/EAA render pipeline (all
three consumers above sharing one ~_caaQueue~) turned out NOT to be
reliably reproducible from outside. Traced live against the "Tougher Than
the Rest" pilot page (7 releases, concurrency 4): because bigbox/CAA-icon/
inline each enqueue an entire pass of per-row tasks strictly after the
previous pass's own enqueue loop finishes, and ~_caaQueue~ is a plain FIFO,
a later pipeline's task for a given release is never admitted until every
earlier-queued task ahead of it (including that SAME release's own earlier
pipeline task) has already been admitted and settled — so in practice no
genuine overlap for the same URL ever occurred on this page, and a 404
response is never cached (only a successful fetch populates Tier 1/2), so
each pipeline ended up making its own independent, fully-serialized attempt
regardless of this step's fix. This does not mean the fix is pointless —
the underlying race (two callers both past the Tier 1/2 check, before
either has cached the result) is real and this closes it — it just means
that race is narrower/timing-dependent enough that it isn't the dominant
source of redundant fetches on typical pages, and a live E2E test cannot
deterministically force it. Verified instead via a direct test-mode hook
(~window.__saTest.fetchCachedImageConcurrent(url, n)~, calls
~_artFetchCachedImage~ N times synchronously in one JS turn — guaranteed
overlap, no queue/timing dependency) in
~tests/live/caa-icon-fetch-dedup.spec.js~; confirmed to fail (5 network
calls instead of 1) with the fix temporarily disabled and pass with it
restored.

*** TODO Step 15: CAA/EAA request-throughput tuning

/Harvested from/ ~filter-performance-fix-caa-throughput~ (branch tip
2026-08-21), where this step is marked ~DONE~ and its own numbering called it
Step 6. The code is NOT on ~main~ — that branch is 290 commits behind — so it
is ~TODO~ here. See the Step-number provenance table above.

Split the single shared ~_caaQueue~ (concurrency 4, ~sa_caa_fetch_concurrency~)
into two independent queues — ~_caaImgQueue~ for image loads and
~_caaMetaQueue~ for ~_artEnrichIcon~'s JSON count-lookup ~fetch()~ calls.
Independent of Steps 1-5 (different dimension of the same CAA/EAA subsystem
Step 5 touches: this fixes *how fast* an already-fired request completes,
not *when* it fires).

Trigger: comparing against ~debug/jesus-coverart.js~ (jesus2099's
~mb_FUNKEY-ILLUSTRATED-RECORDS~, the script this feature was originally
modeled on) — its inline cover-art icons load dramatically faster and never
seem to hit the Cover Art Archive's request limit, despite firing every
request completely unthrottled.

*Landed*: branch ~filter-performance-fix-caa-throughput~ (branched from
~filter-performance-fix~, not ~main~ — Step 5's ~IntersectionObserver~/
~_caaQueue~ code only exists on ~filter-performance-fix~), see
~ShowAllEntityData_CHANGELOG.wip.json~ ~WIP.6~.

- Root cause: jesus2099's script never needs manual throttling because
  neither of its request kinds is actually CORS-bound — native
  ~<img src>~ loads are never subject to CORS (CORS only gates script code
  *reading* response bytes via ~fetch()~/XHR/canvas, not the browser
  displaying an image), and its RG-count lookups are unthrottled ~XHR~ calls
  that simply never got flagged in practice. Our own two CAA/EAA request
  kinds have asymmetric CORS exposure: ~_artLoadIcon~'s image loads (IDB
  path via ~GM_xmlhttpRequest~, which explicitly bypasses CORS; native
  fallback via plain ~<img src>~) are equally CORS-free, but
  ~_artEnrichIcon~'s JSON count lookups use a page-context ~fetch()~, which
  *is* subject to CORS. Both kinds were nonetheless sharing one 4-slot
  queue, so on any table with more than a handful of rows nearly everything
  serialized behind a cap that was only ever necessary for the JSON path.
- New settings ~sa_caa_img_concurrency~ (default 12, was 4) and
  ~sa_caa_meta_concurrency~ (default 6) replace ~sa_caa_fetch_concurrency~.
  ~_artInitQueue()~ now creates/cancels both queues every render pass; a new
  ~_caaQueuesOnIdle()~ helper fires the completion toast once both queues
  (not one shared queue) have drained.
- All 10 former ~_caaQueue.enqueue()~ call sites were reclassified by what
  they enqueue: 8 image-load sites (~ergInjectCaaInlineThumbnails~,
  ~_artLoadIcon~'s retry handler, ~_artInitLazyPics~'s icon branch,
  ~_artInitSmallPics~, ~_loadBigImgIdb~/~_loadBigImg~, the inline-pics
  refetch task, ~init{CAA,EAA}InlinePics~'s ~loadTask~) now enqueue on
  ~_caaImgQueue~; the 2 ~_artEnrichIcon~ sites (~_artInitLazyPics~'s
  enrichment branch, ~_artEnrichTable~) now enqueue on ~_caaMetaQueue~.
- No change to *when* requests fire — Step 5's ~sa_caa_io_threshold~/
  ~IntersectionObserver~ lazy-loading is untouched; only how many of each
  request kind can run concurrently changed.

*** TODO Step 16: Decouple IDB caching from the icon-render hot path

/Harvested from/ ~filter-performance-fix-caa-throughput~ (branch tip
2026-08-21), where this step is marked ~DONE~ and its own numbering called it
Step 7. The code is NOT on ~main~, so it is ~TODO~ here.

Found alongside Step 15 but deliberately not implemented there — bigger risk
profile (changes ~_artLoadIcon~'s IDB-path/native-fallback branching and the
cache-hint telemetry semantics), so it gets its own independently-verifiable
step, same pattern as every prior step in this file.

Today, when IDB caching is enabled (~sa_art_idb_enable~, the default) and an
icon is a cache miss, ~_artLoadIcon~ gates the *visible* render on a full
~GM_xmlhttpRequest → Blob → URL.createObjectURL~ round-trip before the
background-image is ever assigned — jesus2099's script, by contrast, just
assigns ~img.src~ to the plain URL immediately and lets the browser's own
(streaming, off-main-thread-decoding) image pipeline take over, which is a
meaningful part of why it *feels* faster even independent of Step 15's
concurrency fix.

Proposed approach: assign the plain remote URL immediately for display (no
~GM_xmlhttpRequest~, no blob, minimal/no queue wait) — matching jesus2099's
~<img src>~ exactly — then, in the background and without blocking the
visible render, fetch the same URL via ~GM_xmlhttpRequest~ and write the
blob into IndexedDB purely so a *future* page load (or "Load from Disk")
can skip the network. IDB becomes a background accelerator for next-session
speed rather than something the current page's first paint waits on. Needs
its own before/after telemetry check on the cache-hint reporting (a
cold-cache load would report as "browser" instead of "network" even with
IDB enabled, since the blob fetch would no longer be what puts pixels on
screen).

*Landed*: branch ~filter-performance-fix-caa-throughput~, see
~ShowAllEntityData_CHANGELOG.wip.json~ ~WIP.8~. Findings while implementing,
beyond this step's own original text above:

- **Corrected premise**: the "jesus2099 just assigns ~img.src~ immediately
  with no blocking round-trip" framing above is not accurate, confirmed by
  reading ~debug/jesus-coverart.js~'s ~loadCaaIcon~ (lines 217-235) directly.
  That function ALSO gates its visible ~background-image~ assignment on the
  ~<img>~'s ~load~ event — it just has no IDB/blob middleman in front of it.
  So the real gap was never "blocking vs. non-blocking" (both approaches
  block); it was that our own IDB-miss path added an *extra*
  ~GM_xmlhttpRequest~+Blob+~createObjectURL~ hop, routed through a
  userscript-manager-privileged XHR and queued behind ~_caaImgQueue~, before
  the browser's own native image pipeline ever got a chance to run. The fix
  actually landed is: match jesus2099's plain ~<img src>~ for the *display*
  leg of a miss (reusing the pre-existing native-fallback branch, previously
  only reached when IDB was disabled or on a ~cacheBust~ retry — now factored
  into a shared ~_artLoadNativeImg(ctx, imgurl, onSuccess, onError)~ helper),
  while adding a new background-only leg (~_artBackgroundCacheToIdb~) to
  preserve the IDB-population benefit for future page loads.
- **Deviation from the plan's literal design, found and fixed during
  implementation**: the plan's first draft proposed "splitting"
  ~_artFetchCachedImage~ into a local-cache-only checker and a
  network-fetch-only helper in place. That would have broken its five OTHER
  callers (~ergInjectCaaInlineThumbnails~'s inline loader, ~58282~'s hover
  preview, ~_loadBigImgIdb~ inside ~_artInitBigPics~, and two inline-thumbnail
  ~loadTask~ call sites) — all of them rely on ~_artFetchCachedImage~'s full
  three-tier (including network) *blocking* behaviour for their own display,
  and are explicitly out of scope for this step. Fixed by leaving
  ~_artFetchCachedImage~ completely untouched and instead adding two new,
  separate functions: ~_artCheckLocalCache(url)~ (a small, intentionally
  duplicated Tier-1/Tier-2-only copy, used solely by ~_artLoadIcon~'s new miss
  path) and ~_artBackgroundCacheToIdb(url)~ (a thin wrapper that just calls
  ~_artFetchCachedImage~ and discards/swallows its result — reusing the
  existing fetch+IDB-write logic rather than duplicating it). Zero risk to
  the five unrelated callers.
- New ~_caaBgCacheQueue~ (default concurrency 4, new
  ~sa_art_idb_bg_cache_concurrency~ setting) keeps background-caching tasks
  off ~_caaImgQueue~ entirely — bypassing ~_caaImgQueue~ for the new
  *display* leg was considered and rejected: ~_artInitSmallPics~'s own
  comment and ~sa_caa_img_concurrency~'s description both document a real,
  previously-hit ~ERR_INSUFFICIENT_RESOURCES~ failure from unthrottled image
  loads on large tables, so the display leg still goes through
  ~_caaImgQueue~ exactly as before. ~_caaQueuesOnIdle()~ (the completion-toast
  gate) deliberately still only waits on ~_caaImgQueue~+~_caaMetaQueue~ — the
  new queue draining later, silently, after the toast fires, is intentional.
- **Cache-hint telemetry**: per the user's explicit choice, no new badge
  state was added. A miss-then-native-display icon now flows through the
  same ~getResourceTimingHint~ path the native-fallback branch already used,
  landing in the ~browser~ stats bucket instead of the old synthetic
  ~network~ bucket — more accurate (reflects what the browser actually
  observed), not a regression. ~_caaFetchStats~'s doc comment was updated to
  describe this; background-cache fetches are deliberately not counted in
  ~_caaFetchStats~ at all (they never call ~_onIconLoaded~).
- **Double-fetch bandwidth tradeoff** (a cold miss now issues two requests
  for the same URL — native ~<img>~ for display, separate ~GM_xhr~ for IDB
  storage): shipped without blocking on pre-verification, as planned. Not
  yet measured against a real archive response via DevTools Network tab
  whether the second request is served from the browser's HTTP cache —
  follow-up measurement, not a blocker.
- Both existing retry mechanisms (per-icon ⚠⟳ click-retry,
  per-table "Retry" button) were confirmed unaffected — both already forced
  ~cacheBust=true~, already routing through what is now the shared
  ~_artLoadNativeImg~ helper; behaviour is unchanged, only the code path is
  now shared with the new miss-case display leg instead of duplicated.

*** TODO Step 17: ~content-visibility: auto~ for off-screen rows

/Harvested from/ ~filter-performance-fix-caa-throughput~ (branch tip
2026-08-21), where its own numbering called it Step 8.

*This is the one step whose approach was tested and rejected.* It was validated
manually against a real 6,440-row page before any code was written, and it
FAILED — see "Manual validation gate result" below. No settings, helper
functions or render-path hooks were ever written, on that branch or on ~main~.
The named fallback direction is unstarted. Anyone reaching for
~content-visibility~ on this codebase should read that gate first.

Trigger: on tables whose rows carry inline flag imagery from the "More
Flags Everywhere"/"Canadian Province Flags Everywhere" companion userscripts
(custom/subdivision-level flags, e.g. a venue's city rendered as its own
icon — see ~debug/flags-rendering.html~), a fast scrollbar-drag shows the
sticky column (entity name) painting immediately while the rest of the row
stays blank for a noticeable moment. Confirmed NOT the same mechanism as
Step 5's fix (that's the "Location"/area column's flags, an entirely
different column and a different, third-party-injected DOM subtree — CAA/EAA's
~IntersectionObserver~ never touches it) and NOT a regression from this
branch: ~debug/flags-rendering.html~'s single sampled ~<td>~ is 305KB,
carrying two inline ~data:image/svg+xml;base64,…~ images (~84KB and ~221KB
of base64 text — up to ~230KB of raw SVG vector art combined) for one row's
venue location. A single-table render has always fully materialized every
row's DOM at once (true on unmodified ~main~ too, long before Steps 1-5 and
15) — what's new here is just that this particular kind of page makes that
already-large DOM dramatically larger per row.

Mechanism (browser compositing, not our JS): ~applyStickyColumn~ sets
~position: sticky~ on the sticky column's cells, which Chromium typically
promotes to its own compositor layer — a thin, text-only layer that paints
independently and cheaply. The rest of a flag-heavy row sits in the table's
main content layer alongside hundreds of KB of embedded vector art; that
layer's raster tile takes real, measurable time to decode/paint the first
time it scrolls into view. A fast scrollbar-drag jump exposes a large batch
of never-yet-rasterized rows in one step, so the (already fully built,
already in the DOM) main-layer content stays visually blank until the raster
catches up — while the sticky layer, having nothing expensive to paint,
shows immediately.

Goal: stop the browser from doing ANY layout/paint work for rows that are
nowhere near the viewport, so a fast scroll never has a large batch of
never-yet-rasterized heavy rows to catch up on — a fix at the actual
mechanism (browser rendering), not a workaround in our JS (there is no
practical way to "lazy load" a ~data:~ URI's decode cost the way Step 5
lazy-loads a network request — the bytes are already inline in the DOM;
the only lever is whether the browser bothers to lay out/paint them yet).

**** Proposed approach

~content-visibility: auto~ + ~contain-intrinsic-size: auto <estimate>px~ on
every ~<tr>~ in ~table.tbl tbody~ (single-table mode first, matching every
prior step's scoping precedent). Semantics that matter for this codebase
specifically:

- While a ~<tr>~ is off-screen (outside an internal "near viewport" margin,
  conceptually similar to ~IntersectionObserver~), the browser skips layout,
  style recalc for layout purposes, paint, and hit-testing for its entire
  subtree — but does NOT remove or hide the DOM. ~textContent~, attributes,
  ~querySelectorAll~, ~cloneNode~, and non-geometric ~getComputedStyle~
  reads (colours, etc.) all keep working normally on a skipped row.
- What DOES break: any read of *layout geometry* on a skipped row or its
  descendants — ~getBoundingClientRect()~, ~offsetWidth~/~offsetHeight~,
  ~scrollWidth~/~scrollHeight~, ~clientWidth~/~clientHeight~ — reports
  placeholder/zero values while skipped, not the row's real laid-out size.
- ~contain-intrinsic-size~ is the row's OWN placeholder height (so total
  ~scrollHeight~/scrollbar-thumb size stay stable while rows skip in and
  out) — it does not, by itself, fix geometry reads on a skipped row's
  *descendants*, which is the actual audit concern below.

**** Audit checklist

Preliminary pass already done (grepped every ~getBoundingClientRect~/
~offsetWidth~/~offsetHeight~/~scrollWidth~/~scrollHeight~ call site — 83
total hits, most on dialogs/tooltips/dropdowns unrelated to table rows):

- *Safe, confirmed* — auto-resize column-width measurement
  (~:26247-26277~, the "Measure ALL data rows — no sampling" loop): clones
  each cell's child nodes (~cloneNode(true)~) into a separate, always-
  rendered ~measureDiv~ appended to ~document.body~, then reads
  ~measureDiv.offsetWidth~ — never reads the live row's own geometry, so
  content-visibility skipping a row doesn't affect it. (Note: this also
  means the pass's own O(rows) cost is NOT reduced by this step either —
  it clones every visible-per-filter row's cells regardless of scroll
  position, both before and after. Not a goal of this step, not a risk.)
- *Safe, confirmed* — ~applyStickyColumn~'s per-row loop
  (~:16036-16111~): reads ~getComputedStyle(cell).backgroundColor~ only
  (non-geometric, unaffected by skipping); ~_leftOffset()~
  (~:15970-15972~) is hardcoded to return ~0~, no geometry read at all.
- *Safe, confirmed* — ~_findCellListItems~ (~:16897~),
  ~_classifyCollapseCell~ (~:17419~), ~initBarcodeHighlight~ (~:64011~),
  ~_cdtocInitTracklistToggles~ (~:50256~): none read
  offsetWidth/offsetHeight/getBoundingClientRect/scrollWidth/scrollHeight
  anywhere in their bodies (grepped directly).
- *Safe by construction* — every tooltip/hover-preview positioning call
  site (~getBoundingClientRect~ on hover targets, e.g. ~:53454~, ~:59054~,
  ~:60981~, ~:62578~): these only run on ~mouseenter~ of an element the
  user is currently pointing at, which by definition cannot be in the
  skipped (off-screen) state at that moment.
- *Not yet checked* — ~_updateAllColHeaderCounts~/~openUniqDrop~'s row
  scans: expected safe (textContent/DOM-structure based per Steps 3-4's own
  analysis, no geometry reads spotted in the preliminary grep), but not
  individually re-verified line-by-line.
- *Not yet checked* — the CSV/JSON/org/HTML export functions: iterate every
  row regardless of scroll position; must confirm they read only
  ~textContent~/attributes, never geometry.
- *Not yet checked* — sizing ~contain-intrinsic-size~ itself: row height
  varies a lot by content (collapsed vs. expanded prose-clamp cells,
  single- vs. multi-line lists, CAA/EAA multi-image cells) — a single fixed
  estimate will either overshoot (wasted scroll-height padding for typical
  rows) or undershoot (visible layout shift as a row expands past its
  placeholder on first real render). Needs a measured-not-guessed default,
  e.g. sampling the first N already-rendered rows' real heights at initial
  render.
- *Not yet checked* — Steps 1/2's show/hide (~display:none~) and reorder
  (~appendChild~) passes: both already skip/move rows without reading their
  geometry (confirmed for Step 2's tint-boundary walk, which was already
  fixed to check ~tr.style.display~ only), but should be re-confirmed
  against a ~content-visibility~-skipped row specifically, since ~appendChild~
  on a skipped element is untested territory in this codebase.

**** Verification plan

DevTools Performance recording of the exact fast-scrollbar-drag gesture on
a flags-heavy ~series/events~ page, before/after — the actual acceptance
check per this file's own standard (see "Ranked areas" above), not the
operation-count estimates. Confirm: reduced/eliminated blank-paint duration
during the drag; auto-resize column widths, sticky column, filtering,
sorting, and CAA/EAA Step-5 lazy-loading all still behave correctly for a
row that was skipped and later scrolled into view; no scrollbar-thumb
jumping from a bad ~contain-intrinsic-size~ estimate.

**** Browser support

Chromium ~85+ and Firefox 125+; not yet in Safari as of this writing. Needs
a feature-detect guard (~CSS.supports('content-visibility', 'auto')~,
mirroring Step 5's ~typeof IntersectionObserver !== 'undefined'~ pattern) —
unsupported browsers simply don't get the property applied, no fallback
behaviour needed since it's purely a rendering hint with no functional
dependency elsewhere in the script.

**** Manual validation gate result (2026-08-20): FAILED — do not implement as designed

Before writing any settings/production code, this step's own risk (noted
during planning, not in the original doc: ~<tr>~ computes to
~display: table-row~, an "internal table box" display type with a history
of uneven ~content-visibility~/containment support across engines) was
checked manually against a real, live MusicBrainz page — 6,440 rows, flags
from the companion userscripts, same fixture class as ~debug/flags-rendering.html~
above. DevTools console snippet used:

#+begin_src js
document.querySelectorAll('table.tbl tbody tr').forEach(tr => {
    tr.style.contentVisibility = 'auto';
    tr.style.containIntrinsicSize = `auto ${Math.max(tr.offsetHeight, 1)}px`;
});
#+end_src

Result: after applying the snippet and scrollbar-dragging back toward a
previously-marked heavy row, **nothing rendered at all** — the table went
blank rather than cleanly skipping/unskipping individual off-screen rows.
A DevTools Performance recording taken across the same gesture got stuck at
"Stopping timeline..." for an extended period (unclear whether this was
merely slow trace processing on a large capture, or a symptom of the same
underlying breakage), which blocked completing the rest of the planned
diagnostic checklist (confirming whether the DOM row count was still
intact, whether clearing the two style properties restored rendering, and
which browser/version was in use). The test was stopped at this point
rather than pushed further — the empirical result (broken rendering on a
real page, immediately, on the very first manual try) was judged clear
enough on its own to treat as a NO-GO without needing the full root-cause
trace.

**Decision**: do not implement ~content-visibility: auto~ on ~<tr>~
elements as proposed above. This closes out this step's approach; it
remains TODO, not DONE — no settings, helper functions, or render-path
hooks from the "Proposed approach"/"Implementation" sections above were
ever written to the userscript. No code changes resulted from this step.

**Next step, if this is revisited**: per the risk noted in "Proposed
approach" above, table-row-level containment is not a safe primitive on
this codebase's real pages. Any future attempt needs a structurally
different approach — e.g. wrapping each row's heavy cell content in a
nested, non-table-row ~<div>~ and applying ~content-visibility~ to that
wrapper instead, sidestepping the internal-table-box constraint — which
was flagged as the fallback direction during planning but deliberately not
designed in detail, since the validation gate existed specifically to
avoid investing in a full design before confirming the primitive works at
all. That fallback is unstarted; this doc should be treated as blocked
here until someone picks that back up.

*** DONE Step 18: Scope a multi-table sort's re-render to the sorted sub-table

Landed on ~main~ as part of the artwork-preservation series (see the
~// @version~ header for the exact version); branch
~perf/scoped-subtable-sort-render~.

Sorting one sub-table re-ordered exactly one array (~groupedRows[i].rows~)
and then called ~_invalidateFilterCache(); runFilter();~ — which cleared
every group's cached match set and re-cloned, re-stripped, re-highlighted
and re-inserted every row of every group, plus the whole per-group decorate
pass (~initCollapsableColumns()~ and its unique-value column scan included).

~_renderDirtyGroupIdxs~ now names the groups a sort actually disturbed;
~runFilter()~'s multi branch skips the clone/strip/highlight pass for the
rest and ~renderGroupedTable()~'s reuse branch leaves their ~<tbody>~ alone.
~_invalidateFilterCacheForGroups()~ drops only their cache entries — a
re-order cannot change which rows MATCH, only the order of an
already-correct array.

*Measured, 2026-09-07*, ~/artist/5d02f264-…~ (Simon & Garfunkel,
~artist-releasegroups~: 123 rows across 17 sub-tables, 85 with cover art),
sorting the same 61-row "Album + Compilation" section by "Year" each time.
A/B on one tree, one variable — the scoping was toggled off in place rather
than switching branches — via
~tests/live/caa-icon-survives-sort-multi.spec.js~ with ~MULTI_CAA_URL~ /
~MULTI_CAA_BUTTON~, reading the sub-table's own ~.mb-sort-status~ text.

| Arm      | Rows re-inserted | Sort duration (3 runs) | Median  |
|----------+------------------+------------------------+---------|
| Unscoped |              123 | 108 / 116 / 114 ms     |  114 ms |
| Scoped   |               61 | 77 / 85 / 83 ms        |   83 ms |

~27% faster, and half the rows touched. The gain scales with how much of
the page is NOT the sorted section: on a 124-row release group whose sorted
sub-table already holds 119 of the rows, re-insertions only fall 124 → 119
and the timing difference is inside the noise. The 17-section page is the
shape this is for.

Artwork is unaffected in both arms — zero archive requests, icons and
inline thumbnails still painted at insertion — because the two source-row
mirrors land independently of this.

**** Measurement coverage: what the harness can and cannot see

Recorded here because two other steps point at it, and because the answer is
counter-intuitive: *none of the CAA/EAA work in Steps 18 and 19 can show up in
this repo's interaction-perf numbers at all.*

~tests/support/capture-interaction-perf.js~ is hardcoded to ~artist-events~,
which is ~tableMode: 'single'~ — so Step 18's scoping, a multi-table-sort
mechanism, cannot reach it — *and* it seeds ~sa_enable_caa_pics: false~, so
Step 19's artwork mirrors cannot reach it either. Those numbers being flat
across this work is a property of the harness, not evidence about the work.

What each step was measured with instead:

- *Step 18*: an in-place A/B via
  ~tests/live/caa-icon-survives-sort-multi.spec.js~ with ~MULTI_CAA_URL~ /
  ~MULTI_CAA_BUTTON~, reading the sub-table's own ~.mb-sort-status~ text —
  the table above.
- *Step 19*: insertion-time COUNTS rather than milliseconds, which is the
  right metric for work whose point is eliminating requests and DOM churn.

Baselines on disk, and their real vintage:

| File (under ~tests/snapshots/~)                                                            |   Captured |  Version | What it is                                                                                     |
|--------------------------------------------------------------------------------------------+------------+----------+------------------------------------------------------------------------------------------------|
| ~artist-events/interaction-perf-main-9.99.953-2026-08-24.json~                             | 2026-08-24 | 9.99.953 | pre-Step-4, pre-chunked-render-fix, pre-Step-18; a different machine                           |
| ~artist-events/interaction-perf-fix-chunked-render-header-counts-9.99.976-2026-08-31.json~ | 2026-08-31 | 9.99.976 | the de-facto current-~main~ row for a long time, filed under a branch since merged and deleted |
| ~artist-events/interaction-perf-worktree-perf-step-4-9.99.970-2026-08-31.json~             | 2026-08-31 | 9.99.970 | Step 4's own branch arm                                                                        |
| ~artist-releasegroups/perf-baseline.json~ (+ dated archival copies alongside it)           |   see file | see file | initial fetch+render wall time, from ~capture-snapshots.js --perf~                             |

Refreshed 2026-09-07 at 9.99.1045, since both files above had gone stale
enough to mislead (the ~main~ interaction row was 87 versions old and predated
Step 4, and the fetch baseline was 109 versions old):

- ~interaction-perf-main.json~ (since renamed to
  ~interaction-perf-main-9.99.1045-2026-09-07.json~ — see Step 3's
  re-measurement note above) was now a genuine current-~main~ capture. Its
  numbers are in Step 3's re-measurement table, which is where they answer
  something.
- ~perf-baseline.json~: median wall *33 436 ms → 26 378 ms*, fetch
  *23 059 ms → 16 692 ms*, render *502 ms → 533 ms*, itemCount 2142 → 2143.
  Verdict ~ok~.

*Do not read that 21% as a win.* This measurement brackets a live 43-page fetch
against musicbrainz.org, and the whole difference sits in the ~fetch~ stage —
upstream response time, which varies by more than this between sessions. The
one stage this codebase controls, ~render~, is flat at ~500 ms. The itemCount
drift is MusicBrainz gaining a release group. The useful content of the run is
therefore negative: *no regression* across 109 versions on the initial
fetch+render path, and nothing attributable as a gain.

*A CAA-on baseline is still unavailable, and that is a rate-limit problem, not
a missing script.* The two pages worth measuring — Springsteen's
~artist-relationships~ (24 sub-tables, 418 synthetic CAA anchors, 338 stripe
wrappers, ~750 artwork items) and his ~artist-releases~ — stall cold:
measured, the Cover Art Archive served 56 requests in the first 45 s at its
rate limit, with ZERO icons painted, and a 300 s wait never saw
~#mb-info-display-caa~ because the queue had not drained. A real browser
profile hides this behind a warm IndexedDB cache; a fresh harness profile has
none.

The enabling pieces already exist and are unused for this:
~tests/support/capture-idb-fixture.js~ captures real
~{url, base64, contentType}~ records, and ~tests/support/idbFixture.js~'s
~seedArtIdbFixture()~ rebuilds genuine ~Blob~s inside the page before the
userscript opens its DB (~storageState~ cannot round-trip Blobs — see that
file's own JSDoc). Extending that capture to a large multi-table page and
adding a CAA-on arm to ~capture-interaction-perf.js~ is the unstarted work;
until it exists, Steps 18 and 19 are measured by counts and by one bespoke
A/B, not by this repo's standing latency harness.

*** DONE Step 19: CAA/EAA artwork preserved across a re-render

Landed on ~main~ as versions *9.99.1032 → 9.99.1039* (2026-09-06/07), across
seven short-lived branches. Not originally part of this plan — it was reported
as a visual bug ("sorting looks like it re-downloads every image") and only
turned out to be a performance item once measured. Recorded here because two
of its pieces are pure request-elimination and because it is the sibling of
Step 5: Step 5 is about *when* an artwork request fires, this step is about
*not firing it again*.

The whole series in landing order, with the mechanism each added:

|   Version | What stopped happening                                     | Mechanism                                                                     |
|-----------+------------------------------------------------------------+-------------------------------------------------------------------------------|
| 9.99.1032 | icon column blanking on every keystroke/sort, single-table | opt-in ~preserveLiveArt~ on ~_stripTransientCellState()~                      |
| 9.99.1033 | "all artwork loaded" toast re-firing on every sort         | ~_showCaaCompletionToast()~ split into four jobs                              |
| 9.99.1035 | inline thumbnails vanishing, single-table                  | placeholder branch + ~_artInlinePhIsLiveBlob()~                               |
| 9.99.1036 | the big-picture stripe emptying to ⌛ hourglasses          | ~_artMemCachedObjectUrl()~ paints from the session memory cache at build time |
| 9.99.1037 | *20 doomed archive requests per sort*                      | ~_artMissCache~ / ~_ART_MISS_STATUSES~ (404 and 410 only)                     |
| 9.99.1038 | icons blanking, *multi*-table                              | ~_artMirrorIconToSourceRow()~                                                 |
| 9.99.1039 | inline thumbnails vanishing, *multi*-table                 | ~_artMirrorInlineThumbToSourceRow()~                                          |

*Measured as counts, not milliseconds*, deliberately: the point of the work is
that DOM nodes are not destroyed and requests are not issued, and a count says
that unambiguously where a timing delta would not.

|                            | small RG (7 rows) | Springsteen RG (124 rows / 3 tables) | 17-section artist page |
|----------------------------+-------------------+--------------------------------------+------------------------|
| inline thumbs at insertion | 0/7 → 7/7         | 0/119 → 119/119                      | —                      |
| icons painted at insertion | 7/7 (unchanged)   | 119/124 (unchanged)                  | 0/123 → 85/123         |
| archive requests per sort  | 20 → 0            | 0                                    | 0                      |

Three things worth keeping:

- *Why the icon fix could not be reused for the thumbnails.*
  ~_artMirrorIconToSourceRow()~ writes a VALUE (~background-image~) onto a
  ~span.caa-icon~ the source row already owns, and ~cloneNode(true)~ carries
  inline styles through. An inline thumbnail is a NODE this script creates, and
  ~_artInitInlinePics()~ only ever injects it into the live clones — so on
  ~tableMode: 'multi'~ the rows in ~groupedRows[i].rows~ never held one, and
  the placeholder branch that makes single-table work had nothing to match.
  The fix had to CONSTRUCT the placeholder on the source cell, not copy a value.
- *Both mirrors use* ~_findMasterRowByIdx()~, not a group-index lookup. Merged
  discography view folds other groups' rows into the first-occurrence table, so
  an index lookup silently misses exactly those rows.
- *Only a DEFINITIVE absence is negative-cached* (404/410). A 5xx, network
  error or timeout is left uncached on purpose, because the Cover Art Archive
  fails in bursts and caching that would turn a few seconds of upstream trouble
  into a permanently blank column.

*Still open from this series*: the re-enrichment shape — see Step 20.

*** TODO Step 20: Separate "re-apply artwork highlight" from "re-fetch artwork enrichment"

Found 2026-08-29 while verifying an unrelated fix (see ~DEBUG-NOTES.md~,
"CAA/EAA completion signal never fires after runFilter()"), scoped out at the
time as needing its own design pass, and never recorded in this file until now.

~runFilter()~ deliberately strips every cloned row's
~data-caa-enriched~/~data-eaa-enriched~ markers before re-rendering, so that
~_artHighlightImageLi~ re-applies the active filter's highlight to CAA/EAA art
cells. That part is necessary and not a bug. The side effect is that
~_artEnrichIcon~ then treats every cell as unenriched too, and re-queues a full
re-fetch of every image's enrichment data on every single filter or sort
action — including the first ~runFilter()~ after a disk restore, where the data
was already fully populated in the fixture.

*Measured* on the BoDeans ~artist-releases~ fixture (56 rows): ~_caaQueue~ held
*220 pending + 4 running* tasks immediately after a disk-load's first
~runFilter()~, taking real wall-clock time to drain. Contrast
~initRelationshipsColumn()~, which correctly skips already-~relDone~ cells and
never re-fetches them.

*Status: mitigated, not closed.* Step 19 removed the observable cost on the
paths it covers — the mirrors mean a re-render no longer arrives with blank art
to re-resolve, and ~_artMissCache~ stops the doomed retries, so a sort on a
covered page now issues zero archive requests. But the strip-and-re-enrich
shape is unchanged: the markers are still stripped on every pass, and
~_artEnrichIcon~ still cannot tell "re-apply a highlight to art I already know
about" from "go and fetch enrichment data". Any page or path the Step 19
mirrors do not cover still pays it.

The fix is to make that distinction explicit rather than to stop stripping —
the highlight genuinely does need re-applying. Two shapes worth considering:
a separate marker for "enrichment data known" that survives the strip, or
moving the highlight re-application off the enrichment path entirely.

*** DONE Step 21: Artwork-presence sort keys are stripped from the sort path

Landed on ~main~ as version *9.99.1047* (2026-09-07); branch
~fix/caa-column-filter-and-sort-semantics~ (merge ~775eff8~, fold ~d9dd871~).
Fixed alongside a second, user-reported bug in the same column — a typed
filter of ~"no"~/~"yes"~ matching artwork PRESENCE — which is what removed the
"Column filter" bypass this step's own analysis table below relied on. See
"Implemented" at the end of this step for what landed and the one deliberate
deviation from the fix shape.

A correctness bug, filed here because it was found while auditing Step 18/19's
sorting work and because its fix belongs with the sort-key handling Step 10
touches. *Pre-existing and long-standing* — not introduced by Steps 18 or 19.
The pickaxe bottoms out at ~cf5042c~ (2026-05-16, a repo-wide rename), so it
predates the visible history.

*Sorting a CAA/EAA column does not order rows by artwork presence.* It cannot:
both comparators read only ~getCleanVisibleText()~, and that function strips
the sentinel the ordering depends on.

The mechanism, verified at 9.99.1045:

- ~_CLEAN_STRIP_SEL~ (~:38143~) contains ~'.mb-caa-sort-key,.mb-eaa-sort-key,'~
  (~:38166~) and ~'.mb-inline-art-sort-key,'~ (~:38155~) — plus ~.caa-icon~,
  ~.artwork-icon~, ~.mb-caa-count-badge~ and the cache-hint wrappers, so a
  plain CAA/EAA icon cell has no surviving text at all.
- ~getCleanVisibleText()~ (~:38082~) removes all of it by clone-and-remove
  *before* its TreeWalker runs, with a ~FILTER_REJECT~ belt-and-braces at
  ~:38108~.
- ~createSortComparator()~ (~:18563~, values at ~:18565-18566~) and
  ~createMultiColumnComparator()~ (~:18598~, ~:18604-18605~) read *only*
  ~getCleanVisibleText()~. ~_sortColumnKind()~ (~:16552~) has no CAA/EAA
  branch and classifies these columns ~'text'~.

*The strip itself is correct and must stay* — its own comment explains why: a
typed column filter of ~"no"~ would otherwise match ~"not readable"~,
~"known"~, and the sentinel text itself. What is missing is that the other two
consumers were each given a replacement and sorting was not:

| Consumer               | How it survives the strip                                                                                   |
|------------------------+-------------------------------------------------------------------------------------------------------------|
| Column filter          | explicit bypass in ~testRowMatch()~ (~:41738-41749~) — exact-match test straight against the span           |
| Unique-values dropdown | migrated to structure-mode relabeling ("✓ has artwork" / "✗ no artwork" via ~_cellMatchesStructureMode()~) |
| *Sort*                 | *nothing*                                                                                                   |

*Update (9.99.1047): the "Column filter" row above no longer describes*
~main~. That bypass was REMOVED on user report: it made one typed word mean
two unrelated things at once, since
~"no"~ matched both the rows whose image types/comments genuinely contain it
(e.g. a ~"Matrix/Runout"~ type) and every row with no artwork. Artwork
presence is now *exclusively* the dropdown row above. Two consequences worth
keeping:

- The removal also fixed a plain-vs-regexp inconsistency this file did not
  know about: *both* sentinel bypasses lived in ~testRowMatch()~'s NON-regexp
  branch, so an anchored ~^no$~ never reached them and matched nothing while a
  plain ~no~ matched presence. That is the "not yet root-caused" anchoring
  anomaly ~bodeansArtistReleasesFixture.js~ had recorded against this very
  column. Plain and regexp now agree.
- ~mb-inline-art-sort-key~'s own bypass was deliberately KEPT: its
  ~'caa-inline-yes'~/~'caa-inline-no'~ values are strings nobody types by
  accident, so they carry no collision with ordinary English.

An audit of every sort-key class the script creates shows the damage is
confined to the two artwork ones — the other two never entered
~_CLEAN_STRIP_SEL~ and still sort as documented:

| Sort-key class                     | In ~_CLEAN_STRIP_SEL~? | Contract                                                                                                                                                                     |
|------------------------------------+------------------------+------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ~mb-caa-sort-key~ (serves EAA too) | *yes* (~:38166~)       | *broken* — JSDoc ~:3614~ still claims ~"no" < "yes"~ works                                                                                                                   |
| ~mb-inline-art-sort-key~           | *yes* (~:38155~)       | filter-only by design, so no sort contract broken — but ~_artSetInlineSortKey()~'s JSDoc claims the strip pass excludes its class, and that no bypass is needed. Both false. |
| ~mb-cancelled-sort-key~            | no                     | intact — JSDoc ~:3827~ correct                                                                                                                                               |
| ~mb-video-sort-key~                | no                     | intact — JSDoc ~:3525~ correct                                                                                                                                               |

Note ~.mb-eaa-sort-key~ is in the strip selector but is never created: the
~caa~ extractor always writes ~mb-caa-sort-key~ regardless of CAA vs EAA.

*Why it went unnoticed*: no spec asserts that a CAA/EAA column orders by
artwork presence. ~caa-icon-survives-sort.spec.js~ and its multi-table sibling
assert that artwork *survives* a sort, which is a different guarantee and is
met.

**** Fix shape

Mirror the filter bypass, which is the precedent this should follow:

1. In ~createSortComparator()~ and ~createMultiColumnComparator()~, before
   falling through to the text compare, read
   ~cell.querySelector('.mb-caa-sort-key,.mb-eaa-sort-key')~ directly and use
   its ~'yes'~/~'no'~ text — exactly as ~:41749~ already does for filtering.
   Both comparators must change together; they have disagreed before (see
   ~_sortColumnKind()~'s own JSDoc for that history).
2. Add a regression test. There is currently none for this guarantee, which is
   why it regressed silently.
3. Correct three JSDoc blocks that document behaviour which does not happen:
   - ~:3609-3617~ (and the inner ~appendSortKey~ block) — the ~"no" < "yes"~
     sortability claim.
   - ~_artSetInlineSortKey()~ — the "strip pass does not include this class"
     and "no bypass required" claims.
   - ~:56576~ — describes Step 3 as "not part of this branch", written when
     ~_visibleRowSetSignature~ lived on a branch. Unrelated to this bug, but
     it is the one remaining stale ~PERFORMANCE.org~ cross-reference in the
     userscript and it is cheap to fix in the same pass.

All three are userscript JSDoc, so this step carries a version bump and a
changelog entry; the consolidation revision that filed it did not.

**** Implemented — 9.99.1047, 2026-09-07

All three fix-shape points are done, with one deliberate deviation from
point 1: rather than repeating the sentinel lookup inside each comparator,
both now call a single shared ~_sortCellText()~ resolver (declared next to
~_sortColumnKind()~). Same reasoning point 1 gives for changing them
together — they have disagreed before — taken one step further, so they
cannot drift again.

~_sortCellText()~ is scoped to the artwork sentinel ALONE, and the audit
table above is why: folding in ~mb-cancelled-sort-key~ would have changed a
column that was never broken. A cancelled-event cell also carries the
visible word "cancelled", so its sort text today is ~"cancelled yes"~ vs
~"no"~ — and ~"c" < "n"~ puts cancelled events FIRST ascending, whereas
sentinel-only would silently reverse that. The rule to carry forward: only
add a class to ~_sortCellText()~ if it is also being stripped from the
visible text.

Point 2's regression test is ~§C2~ in
~tests/live/artist-releases-filter-sort.spec.js~ (BoDeans disk fixture, so
deterministic and network-free). It asserts the ordering itself, not just
that the row count survives — which is precisely the gap "Why it went
unnoticed" identifies above. Verified failing before the change, with the
presence sequence after an ascending sort coming back fully unsorted.

Point 3's third item (~:56576~) was narrower than filed: Step 3 was still
~TODO~ at the time, so only the parenthetical "not part of this branch" was
stale, not the cross-reference itself. Step 3 has since landed (9.99.1049).

*** DONE Step 22: One coalescing token per table, not one per page

Found while implementing Step 3, and shipped for as long as
~_scheduleColHeaderCounts()~ has existed.

~_colHeaderCountsToken~ is a single module-level counter. Every schedule
increments it, and a scan abandons itself the moment it sees a newer value.
That is exactly right for the case it was written for — ~initReleaseEventsColumn()~
firing ~initCollapsableColumns()~ three times in a row over the SAME table,
which should cost one scan and not three.

It is wrong for the case nobody checked. ~renderGroupedTable()~ calls
~initCollapsableColumns()~ once per sub-table, inside its own loop (both the
reuse branch and the new-table branch), and
~syncCollapseButtonsWithColumnVisibility()~ does the same across every
~table.tbl~ on the page. Each of those schedules bumps the shared counter, so
on a multi-table page *every sub-table except the last cancels itself* before
writing a single badge.

The failure is silent — an unwritten badge is an empty ~<span>~, not an error —
but it is sitting in the committed baselines:

| Baseline                                                     | ~.mb-col-uniq-count~ spans | populated |
|--------------------------------------------------------------+----------------------------+-----------|
| ~tests/snapshots/artist-releasegroups/rendered.html~         |                        423 |         1 |
| ~tests/snapshots/releasegroup-releases/rendered.html~        |                         42 |        14 |
| ~tests/snapshots/artist-events/rendered.html~ (single-table) |                         21 |         1 |

The ~artist-events~ row is a different, benign case and worth separating: that
page has one table, so nothing cancels it. Its scan is simply still running
when ~capture-snapshots.js~ takes the snapshot, and the same page's
~post-filter.html~/~post-sort.html~ — captured after
~waitForColHeaderCountsStable()~ — read 18 and 20 of 21.

Fix: key the token by table (a ~WeakMap~), so same-table schedules still
coalesce and siblings stop interfering.

*This makes multi-table pages do more work, and that is the point.* A page
rendering 47 sub-tables goes from ~1 scan to 47. It buys 423 badges that are
currently just absent, and Step 3's cache is what keeps the bill payable — each
sub-table's row set is small, and every render after the first is a hit.

*** TODO Step 23: Convert the re-wire-after-clone family to event delegation

Found 2026-09-09 while tracing the per-keystroke filter cost — see "Findings:
the per-keystroke filter cost" above. Line references read at *9.99.1049*.

~CLAUDE.md~ lists four functions that exist *only* because their features attach
listeners with ~addEventListener~ instead of delegating, so ~cloneNode(true)~
silently strips them and every filter pass has to put them back. Each is a
full-table walk, and two of them rebuild DOM rather than merely re-attaching:

| Function                        | What it does per pass                                                                          | Guard                                          |
|---------------------------------+------------------------------------------------------------------------------------------------+------------------------------------------------|
| ~initPicardTaggerColumn(true)~  | ~_picardExtractRowEntities~, then ~innerHTML=''~, then rebuild ~<ul><li><button><img>~ per row | ~sa_enable_picard_tagger~ + a ~/release/~ link |
| ~initExpandRGsFeature()~        | strips every ~[data-erg-btn]~ and re-injects a fresh button per release link                   | ~isExpandRGsPage()~ + links exist              |
| ~_rewireNestedTableH2Toggles()~ | ~document.querySelectorAll('table.tbl h2')~                                                    | cheap unless the page has nested ~<h2>~s       |
| ~_cdtocInitTracklistToggles()~  | early-returns unless ~pageType === 'cdtoc'~                                                    | already effectively free                       |

Grep the names rather than trusting these positions:
~initPicardTaggerColumn~ ~:73883~ (rebuild loop ~:73920-73940~, row scan
~:73681-73699~), ~initExpandRGsFeature~ ~:65681~ (stale-button strip
~:65710-65714~, with its own comment explaining why it exists),
~_rewireNestedTableH2Toggles~ ~:52102~, ~_cdtocInitTracklistToggles~ ~:58963~.

*On a release-listing page the first two both fire on every keystroke*, so two
columns' worth of interactive elements are destroyed and rebuilt per pass.

*Picard additionally defeats Steps 3 and 4.* Its rewire pass ends in
~_invalidateUniqDropDataCacheForTable(table)~ (~:74033~), which also drops
~_colHeaderCountsCache~ (~:52459~ → ~:52548~). That call is CORRECT for what
the function currently does — it really has just rebuilt every cell — which is
why the fix is to stop rebuilding, not to delete the invalidation. Deleting it
alone would leave a stale cache behind a real mutation.

Fix: one delegated listener per table, with the state each handler needs moved
from its closure onto ~data-*~ attributes on the element. ~ensureCollapseDelegate~
is the in-file precedent and should be followed rather than re-derived. Then
~cloneNode(true)~ carries the cells through intact, the per-pass calls at
~:42748~/~:42946~ (Picard) and ~:42920~ (ERG) go away, ~rewireOnly~ and the
stale-button strip go with them, and the cache invalidation is no longer needed
because nothing was invalidated.

*Test-framework impact.* Picard and ERG buttons gain ~data-*~ attributes in
every ~rendered.html~ that carries them, so those baselines re-capture.
*~CLAUDE.md~'s own "re-wire-after-clone" list must be corrected in the same
commit* — it is the standing guidance for anyone adding a new interactive cell
element, and leaving Picard and ERG on it afterwards would be actively
misleading.

**** Three corrections found while fixing the Picard multi-table clone bug

/Added 2026-09-10. See ~DEBUG-NOTES.md~'s "Picard column emptied on every
multi-table re-render" entry./

1. *The four-row table above is not the whole family.* Three more functions
   exist for the same reason and are on neither this list nor ~CLAUDE.md~'s:
   ~applyStickyColumn()~ (per-row ~mouseenter~/~mouseleave~ plus the custom
   props ~tr._mbStickyEnter~/~_mbStickyLeave~, removed and re-added every pass —
   the *largest* unlisted member, and separately Step 24's target),
   ~barcodeProcessTable()~ (per-cell ~removeEventListener~/~addEventListener~
   every pass; named handler, module-level state, cleanly delegatable), and
   ~initAnnotationCompareRadios()~ (NOT a listener — ~cloneNode(true)~
   re-applies MusicBrainz's pristine ~disabled~ attribute, so *delegation
   cannot fix it*; it belongs to the family but not to this step). Correct both
   rosters as part of this step.

2. *This step's payoff sentence had an unstated prerequisite, now met.* "Then
   ~cloneNode(true)~ carries the cells through intact" is true only where the
   SOURCE rows own the cells. In ~tableMode: 'multi'~ they did not — the Picard
   cell was appended to the rendered clones only, which is the bug fixed on
   2026-09-10. Delegation alone would not have fixed it: the listeners were
   never the problem, the missing ~<td>~ was. That fix is landed, so the
   sentence now holds; do not re-derive the premise.

3. *~_cdtocInitTracklistToggles()~ yields nothing and should not be budgeted
   for.* It early-returns unless ~pageType === 'cdtoc'~, and its injected
   ~.mb-cdtoc-tracklist-row~ is not part of ~allRows~, so it must be rebuilt
   regardless; only its ~<a class="toggle">~ listener is delegatable.

Also note ~_rewireNestedTableH2Toggles()~ is the risky one for a different
reason than ERG: ~h2._mbToggle~ is a *public contract* read by five other call
sites, including ~ensureCollapseDelegate~'s Ctrl+Click prose branch — the
behaviour ~CLAUDE.md~ documents as "Ctrl+Click … additionally calls
~h2._mbToggle(true)~". Delegating it means keeping ~_mbToggle~ as a shim or
touching all five.

*Part of this step's Picard win is already banked.* The 2026-09-10 fix added a
~_mbPicardWired~ JS *property* (not an attribute — ~cloneNode(true)~ copies
attributes but never JS properties, so it is an exact "was I cloned?" test) and
skips re-wiring any cell that was filled in place. A scoped sub-table sort
therefore no longer rebuilds the interactive cells of every untouched
sub-table. A global-filter keystroke re-renders every group and still pays in
full, so the delegation win remains real — just smaller than the pre-fix
numbers would suggest, and *those numbers were never captured anyway* (see the
harness note above: ~artist-events~ has no ~/release/~ links).

*** TODO Step 24: Snapshot ~applyStickyColumn~'s hover backgrounds lazily

~applyStickyColumn~ (~:19068~) runs from ~_applyPostRenderRowPasses()~
(~:42326~) on every filter pass. For each row it clears and then reads the
computed background of the sticky cell — and then does the same for *every other
cell in the row* (~:19262-19268~), storing each in ~data-mb-rest-bg~ so the
row's ~mouseenter~/~mouseleave~ handlers can swap them.

The write immediately precedes the read on the same element, so each pair
forces a style recalculation. The count is not an estimate:
~tests/snapshots/artist-events/rendered.html~ carries *87 738*
~data-mb-rest-bg~ attributes, one per call, and that file is 19 MB.

Fix: keep the sticky cell's own eager read — it must carry an opaque inline
background so it paints over scrolled content — and compute the rest of the
row's backgrounds inside the ~mouseenter~ handler on first hover, cached on the
row afterwards. No functionality is lost: the values are only ever consumed by
a hover the user has not yet performed.

*Test-framework impact: every ~tests/snapshots/*/rendered.html~ re-captures*,
and shrinks substantially (87 738 attributes on ~artist-events~, 19 287 on
~artist-releasegroups~, 11 816 on ~notes-received~, plus eight more files).
~tests/snapshots/registry.org~'s "Expected drift" section needs an entry, or
the next reader will read the shrink as a regression.

*** TODO Step 25: ~_countLiveDateFlags~ tallies the wrong rows, and always walks them

Two problems in one function (~:32881-32901~), reached from
~updateFilterButtonsVisibility()~ → ~_updateLiveDateFlagButtons()~ (~:33509~)
on every filter pass.

*Correctness.* Its JSDoc says the totals "stay a stable page-wide summary
independent of whatever filter happens to be active" (~:32874-32876~). They do
not: it walks the live ~tbody~, and ~runFilter~ REMOVES non-matching rows rather
than hiding them, so under an active filter the WARNING/ERROR buttons report
only the surviving subset. This is the same bug already fixed for
~_countLengthMismatchRows~, which counts ~_msSourceRows()~ precisely for this
reason — see ~CLAUDE.md~'s note on it. Fix the same way.

*Cost.* It runs ~cell.querySelectorAll('.mb-live-date-flag')~ for every cell of
every row of every ~table.tbl~ — ~87 700 failing subtree scans per pass on
~artist-events~. The buttons it feeds are created unconditionally in
~mb-global-summary-container~ (~:32629~, asserted by
~tests/fixtures/filter-bar-containers.spec.js~), so the early-out at ~:32919~
never fires and this runs on every pageType, including the great majority that
have no live-date flags anywhere. Gate the walk on the page actually carrying
one, the way ~_updateAllColHeaderCounts~ gates its own per-cell inline-art probe
behind a single table-wide ~querySelector~ (~:57119-57124~).

*** TODO Step 26: Memoize ~updateFilterButtonsVisibility~'s source-row tallies

~runFilter~ calls ~updateFilterButtonsVisibility()~ unconditionally at ~:43187~,
and its tail (~:33509-33511~) runs three counters. Two of them —
~_countLengthMismatchRows~ (~:32991~) and ~_pendingEditsGroups~ /
~_countPendingEditRowsSingle~ (~:33146~/~:33164~) — deliberately walk the
*source* rows via ~_msSourceRows()~, one ~querySelector~ each, so their cost is
the full row count no matter how narrow the filter is. On ~artist-events~ that
is ~8 300 ~querySelector~ calls per pass whose answers cannot have changed:
their inputs are the source rows, which a filter never mutates.

Fix: memoize both against the source-row set, invalidated where the source rows
are actually replaced (fetch, sort, disk load — the same three sites
~_invalidateFilterCache()~ already hooks, ~:44781~, ~:58524~, ~:63632~) and
wherever a flag is stamped asynchronously.

*This is the item that matters most on a narrow filter*, which is the case
Steps 1/2 measured worst on — every other per-pass cost scales with ~M~ and
shrinks as the query narrows, while these three stay flat at the full row count.

*** TODO Step 27: Scope ~_applyPostRenderRowPasses()~ to the dirty tables

~_applyPostRenderRowPasses()~ (~:42326-42341~) is page-wide: four
~document.querySelectorAll('table.tbl')~ sweeps, running ~applyZebraStriping~,
~normalizeCommentSpans~, ~applyStickyColumn~ and ~initBarcodeHighlight~ over
every table on the page, including sub-tables that are collapsed and sub-tables
a scoped sort deliberately left alone.

That *partly undoes Step 18*. Step 18 skips the clone/strip/highlight pass and
the per-group decorate pass for every sub-table a sort did not disturb, but
these four still run over all of them — so a 47-section page sorting one section
still pays the whole page's row-by-cell style-recalc storm from Step 24's walk.

Fix: honour ~_renderDirtyGroupIdxs~ (~:42185~), which already exists and already
carries exactly the right answer. Note the same caveat Step 18 records for the
colon-alignment finalizers: a pass scoped to one group must not run anything
that measures a width shared across the whole rendered row set.

*** TODO Step 28: Hold the header-count scan until typing settles

~_scheduleColHeaderCounts()~ (~:57017~) already waits out any in-flight render,
yields through ~_yieldToEventLoop()~, coalesces per table via the Step 22 token,
and memoizes through Step 3's LRU. What it does not do is wait for the user to
stop typing.

That matters because *Step 3's cache misses by construction while typing*: its
key is the visible row set (~_colHeaderCountsRowSetSignature~, ~:56913~) with a
four-entry LRU (~:52519~), and every distinct query produces a distinct row set.
Only backspacing to an already-seen set hits — and backspacing toward the full
row set is the most expensive direction of all, which is what
~headerCountsRestore~ measures at 5.7-12.3 s depending on host.

Fix: gate the schedule behind a typing-idle timer, separate from and longer than
~_adaptiveFilterDelay()~, so a burst of passes schedules exactly one scan at the
end of the burst. Nothing is lost: the badges are informational and nobody reads
them mid-keystroke.

*Test-framework impact*: ~waitForColHeaderCountsStable()~ still works but its
timing changes, and ~headerCountsInitial~/~headerCountsRestore~ then measure a
slightly different thing. Say so in ~tests/MEASUREMENTS.org~ when it lands.

*** TODO Step 29: Stop re-deriving cell text in the highlight pass

~testRowMatch~ (~:41688~) runs twice per surviving row. The first call
(~matchOnly=true~, on the source row) reads every cell through ~_rowTextCache~
(~:35148~). The second (~matchOnly=false~, on the clone — ~:42854~ single,
~:42662~ multi) re-derives all of it with fresh ~getCleanColumnText~ /
~getCleanVisibleText~ calls, because the clone is a different element and the
cache is keyed by element identity.

Two fixes, independent of each other:

- Feed the source row's cached per-cell text into the clone pass. The clone is a
  ~cloneNode(true)~ of that exact row and the text is identical by construction.
- Scope the global-filter highlight. ~highlightText(row, q, …, -1, …)~
  (~:38622~, dispatch at ~:38666-38669~) walks *every* ~<td>~ of the row, doing
  ~td.normalize()~ plus a full ~highlightCrossTag~ TreeWalk (~:38785~) on each.
  The match pass already knows which cells hit; on a query that matches one
  column this is ~C~ times more work than needed.

Both need care around ~_CLEAN_STRIP_SEL~: the cached text is stripped text, and
the highlighter operates on live nodes, so the mapping between them is where a
bug would hide. Step 9 becomes load-bearing here — a stale ~_rowTextCache~ entry
currently only mis-matches a row, but after this it would also mis-highlight it.

*** TODO Step 30: Three micro-costs on the per-pass path

Small, independent, and cheap to verify. Grouped because none justifies its own
step.

- *The unconditional ~window.scrollTo(__scrollX, __scrollY)~* at the tail of
  every pass (~:43011~) forces a reflow whether or not the scroll position
  actually moved. Guard it. This is the exact term that made Steps 1/2 lose on
  narrow filters (219 ms against ~main~'s 35 ms), so landing it changes the
  arithmetic those steps get re-judged on.
- *~getCleanColumnText~ calls ~root.normalize()~ unconditionally* (~:38414~).
  On the no-clone fast path ~root~ IS the live cell, so this mutates the live
  DOM on every one of the ~88 000 calls a full header-count pass makes.
- *~getCleanColumnText~ ends with an unconditional
  ~element.querySelector('ul.mb-caa-art-ul')~* (~:38473~) — a full failing
  subtree scan per call on every page with no artwork columns.

*** TODO Step 31: Multi-table incremental narrowing

Single-table mode narrows a lengthening plain-text global query to the previous
match set instead of rescanning ~allRows~ — ~_incrMatchSet~ and friends
(~:35122-35124~), gate at ~:42794-42812~, deliberately disabled for regexp and
exclude because neither narrows monotonically.

Multi-table mode has no equivalent: it re-runs
~_sourceRows.filter(r => testRowMatch(r, matchCtx, true))~ per group per pass
(~:42596~), with only ~_filterResultCache~ (50 entries, FIFO) between it and a
full rescan. Port the same gate per group.

Cheap and mechanical, but genuinely lower value than it looks: the match scan is
walk 1 of five, and the profiling trace in the Session-summary section below put
it at 39.6 ms of a ~230 ms pass. Take it after the walks that dominate.

*** TODO Step 32: On-demand Picard column — considered, deferred

Raised as "should the Picard column only render when actually needed, e.g.
behind a button?". The instinct is right — it is the most expensive column per
filter pass — but recorded here as DEFERRED with the reasoning, so it is not
re-proposed blind.

- *Hiding it does not help.* ~toggleColumnInTable~ (~:19567~) sets
  ~style.display = 'none'~; the cells stay in the DOM, and every one of the five
  walks iterates ~row.cells~ or the header list. Only not BUILDING it helps.
- *Inserting or removing a column at runtime shifts every column index*, and
  that is the expensive part: ~data-col-idx~ on every column-filter input, sort
  state, the sticky-column index, ~_uniqDropDataCache~ and
  ~_colHeaderCountsCache~ (both keyed per ~colIndex~), the colon-alignment
  descriptors, and saved column widths. ~injectColumnFilterRow~'s own JSDoc
  documents the "ghost filter cell" self-healing path that exists BECAUSE
  ~initPicardTaggerColumn~ adds a column after the initial build; a toggle would
  make that path run repeatedly instead of once. No removal path exists today,
  only injection.
- *"Build it or don't" is already a setting.* ~sa_enable_picard_tagger~
  (default true) decides this at render time, which is the only point where the
  decision is cheap. A button amounts to "change the setting and re-render".

After Step 23 the residue is one extra column in five O(rows x columns) walks —
~5% on a 21-column page, proportionally more on a narrow table — plus its own
cloned DOM. Real, but well behind Steps 24-26. *Measure it first with the
setting that already exists*, and only design a toggle if the number justifies
the index churn.

*** TODO Step 33: Lazy Relationships-column fetch

~_initRelationshipsColumnImpl()~ (~:62582~) collects every unpopulated
~td.mb-rel-cell~ on the page (~:62619-62620~), dedupes to ~uniqueMbids~
(~:62659~) and fetches one WS2 request per unique MBID at initial render,
regardless of where the row sits relative to the viewport.

It is well-behaved in the ways artwork was not — it skips ~relDone~ cells so a
filter keystroke never re-fetches, and it has a two-tier cache (memory plus the
~rel-ws2~ IndexedDB store with a TTL). The gap is purely *when* the first fetch
fires: a cold large page queues one request per distinct entity against
MusicBrainz's rate limit before the user has scrolled anywhere.

Same treatment as Step 5, different subsystem — and it *must reuse Step 5's
~IntersectionObserver~ rather than adding a second one*, so take it after that
step lands. Note Step 7's lesson while doing it: Step 5's own gate read
~allRows.length~, which is always 0 in ~tableMode: 'multi'~, so the threshold
silently never fired there.

Separately worth probing: the millisecond-Length feature batches 100 MBIDs into
one ~rid:~ SEARCH request, measured in ~tests/MEASUREMENTS.org~. Whether that
endpoint can serve ~inc=…-rels~ too is unknown and untested —
~scripts/probe-ms-rid-batch-size.py~ and ~scripts/probe-ms-browse-endpoints.py~
are the model for finding out before assuming it.

*** TODO Step 34: Collapse ~_stripTransientCellState~'s twelve per-cell scans into one

/Found 2026-09-09 by Tier 0's first arm, not by reading the code./ It is the
largest single cost on the filter path by an order of magnitude, and this file
had it filed as one of five comparable walks until the counts arrived.

*Measured*, ~artist-events~ at 9.99.1049, one filter clear: stack sampling
attributes *97.5%* of the phase's 1 078 372 ~querySelectorAll~ calls to this
one function — roughly *1.05 million*, twelve per cell across 87 654 cells,
against ~27 000 for everything else in the phase combined. Full numbers in
~tests/MEASUREMENTS.org~.

The shape: ~runFilter~ calls it once per cell of every cloned row (~:42848~
single, ~:42656~ multi), and its body (~:60597~) runs about a dozen
UNCONDITIONAL ~querySelectorAll~ passes over that cell — the CAA/EAA
enrichment markers, art ~<li>~ items, expand buttons, collapse toggles, icon
spans, inline placeholders, three cache-hint families, the ERG buttons, and a
jesus2099 anchor sweep. Each is a full subtree walk, and each returns nothing
on any page that lacks that feature.

*The measured page had artwork switched off entirely* (~sa_enable_caa_pics:
false~, seeded by the fixture) and still paid every CAA/EAA query. That is
where most of the million calls go: not doing work, but asking twelve times per
cell whether there is any.

Two fixes, independent and both worth having:

- *One combined pass instead of twelve.* Query the union selector once per
  cell and dispatch each hit with ~.matches()~, turning twelve subtree walks
  into one. Unconditional, so it helps equally on a page that DOES have
  artwork — which is the case a settings gate cannot help at all.
- *Hoist the feature gates.* Nine of the twelve are CAA/EAA-only. A page with
  ~sa_enable_caa_pics~ off, or with no art column, can skip them outright. The
  precedent is ~_updateAllColHeaderCounts~'s own lazy table-wide probe
  (~:57119-57124~), added for exactly this reason and with exactly this
  argument: "a page with no addCAA/addEAA inline thumbnails anywhere — which is
  most of them — otherwise pays one querySelector per cell to keep answering
  no".

*Verify by count, not by timing*: ~node tests/support/capture-pass-cost.js~,
comparing ~filterClear~'s ~querySelectorAll~ against
~pass-cost-main-9.99.1049-2026-09-09-petri.json~. That counter is one of the 28
that are bit-identical across runs, so any movement in it is real.

*It also shrinks Step 1.* Fix C stops cloning rows, and these calls are made
on the clones — so Step 1 would remove this cost as a side effect, and landing
this step first removes most of what Step 1 still has to win. Cheap and safe
beats large and risky, so the order stands; see "Step 34 overlaps Step 1, and
shrinks it" above for what that does to Step 1's case.

*Do not fold this into Step 23.* Both touch per-pass cell work and it is
tempting, but they are different mechanisms with different risk: Step 23 changes
how listeners are bound, this changes how a cell is queried, and the artwork
preservation rules in CLAUDE.md's "CAA/EAA artwork — what must not break" bear
on this one alone. In particular ~preserveLiveArt~ and the ~_hadInlineArtPh~
branch both live inside this function and must survive any restructuring of it.

** Session summary: measured comparison, ~main~ vs ~perf-steps-1-4~ (2026-08-24)

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31). *Everything below
was measured with Steps 1, 2, 3 and 5 applied, none of which are on* ~main~ —
so the "this branch" column describes a configuration that does not exist here.
It is kept because the two findings it establishes are branch-independent and
expensive to rediscover: that Fix C's win depends on filter selectivity (it
LOSES on a narrow filter, which is the common case), and the traced reason why.

Consolidated results from a full measurement session on ~artist-events~
(Bruce Springsteen's events tab, 4174 rows, 21 columns — the pageType
~tests/snapshots/registry.org~ earmarks as the dedicated performance-
comparison target). All numbers are external/user-perceived wall-clock via
Playwright unless marked "internal" (self-reported ~performance.now()~
duration from inside the userscript itself, which excludes debounce/
Playwright-polling overhead but — see the sort row below — can also
understate real perceived latency when a forced-reflow call is what's
being removed). Full methodology, test files, and raw JSON:
~tests/live/artist-events-interactions.spec.js~,
~tests/support/capture-interaction-perf.js~,
~tests/snapshots/artist-events/interaction-perf-<branch>-<version>-<capturedAt>[-<hostname>].json~.

*** Headline: narrow filter (real usage: a specific, mostly-complete search term)

"Country" = "United Kingdom" — 158 of 4174 rows match (~4%).

| Interaction         | ~main~   | ~perf-steps-1-4~ | Δ                      |
|---------------------+----------+------------------+------------------------|
| Global filter       | 1418 ms  | 2383 ms          | +68% slower            |
| Column filter       | 1595 ms  | 2437 ms          | +53% slower            |
| Sort (unfiltered)   | 2994 ms  | 3669 ms          | +23% slower (was +47%) |
| Uniq-dropdown, cold | 29802 ms | 27838 ms         | ~parity                |
| Uniq-dropdown, warm | 29700 ms | 791 ms           | *37x faster*           |

Sort's "was +47%" parenthetical is the *before* number for the ~e6aa959~
sort-skip fix (see its own subsection below) — landed mid-session, already
reflected in the "now" column above.

*** Filter selectivity flips the winner: wide filter

Same mechanism, different query — "Country" = "United States", 3380 of 4174
rows match (~81%). *Internal* (self-reported) duration, since this was a
targeted root-cause probe, not a full external-wall-clock run:

| Interaction (internal)     | ~main~ | ~perf-steps-1-4~ | Δ                            |
|----------------------------+--------+------------------+------------------------------|
| Column filter, narrow (4%) | 230 ms | 321-348 ms       | perf-steps-1-4 slower        |
| Column filter, wide (81%)  | 977 ms | 397 ms           | *perf-steps-1-4 2.5x faster* |

~main~'s cost scales *linearly* with match count (cloneNode+insert+rescan
is proportional to the rebuilt table's size). ~perf-steps-1-4~'s cost is
roughly *constant* regardless of match rate (dominated by a fixed
forced-reflow cost across the full 4174-row table, whether 158 or 3380 rows
end up visible). Real interactive typing is progressive — early keystrokes
are wide, later ones narrow — so Steps 1-2 likely net out closer to a wash
in practice than the single-query headline table above suggests on its own.

*** Root cause of the narrow-filter regression (traced via temporary instrumentation)

~performance.now()~ marks added directly inside each branch's
~runFilter()~ single-table branch (profiled, then reverted — not committed;
see ~DEBUG-NOTES.md~'s 2026-08-24 entry for the full trace and how to
reproduce), for the narrow ("United Kingdom", 158/4174) case:

| Sub-step                              | ~main~   | ~perf-steps-1-4~ |
|---------------------------------------+----------+------------------|
| Filter-test scan (all 4174 rows)      | 39.6 ms  | 37.3 ms          |
| Row mutation (clone vs. style-toggle) | 18.0 ms  | 23.5 ms          |
| DOM insert/replace                    | 121.4 ms | —                |
| *Forced reflow*                       | 34.8 ms  | 219.3 ms         |
| ~initCollapsableColumns()~ rescan     | 7.4 ms   | 0 ms             |
| ~_syncCollapseHasMatchInTable()~      | —        | 38.8 ms          |
| *Total (self-reported)*               | ~230 ms  | ~321-348 ms      |

~main~ pays ~139ms more for cloning/inserting rows; ~perf-steps-1-4~ pays
~184ms more in forced-reflow cost alone, which dominates. Root cause:
~main~'s rebuild only ever lays out a table containing the MATCHES (158
rows); Step 1 keeps all 4174 rows permanently in the live DOM, so the next
forced reflow (here, ~window.scrollTo()~, called unconditionally at the end
of every ~runFilter()~ pass to restore scroll position) must lay out the
FULL row set, including the ~4016 now-hidden ones. Interestingly,
~initCollapsableColumns()~'s documented "30-120s for 8k+ rows" cost — the
ORIGINAL dominant-cost target behind Steps 1-4 — turns out to be cheap here
specifically BECAUSE ~main~'s rebuild already shrinks the table to just the
matches before that rescan runs; it may still dominate for a filter that
narrows only slightly (not verified).

**** Attempted mitigation: CSS containment — did not work

Tried both ~content-visibility: auto~ on every ~<tr>~ (see this file's own
"Known limitations" section below for why that's a considered, deferred
technique for a related cost) and ~table-layout: fixed~ with locked
~<col>~ widths (the exact technique this codebase's own manual-resize
handler already uses, with the comment "so subsequent width writes are
cheap" — see ~ShowAllEntityData.user.js~ around ~:25860~). Measured against
the same forced-reflow probe, isolated to the table (~table.offsetHeight~,
not ~document.body.offsetHeight~, to rule out unrelated page content):

| Technique                                | forced reflow (2 runs)                                |
|------------------------------------------+-------------------------------------------------------|
| Baseline (table-layout: auto)            | 200.0 ms / 199.8 ms                                   |
| ~content-visibility: auto~ on all ~<tr>~ | 250.8 ms / 198.7 ms (no improvement; sometimes worse) |
| ~table-layout: fixed~ + locked ~<col>~   | 211.5 ms / 177.7 ms (within noise, no real win)       |

Neither technique meaningfully reduces the cost — it isn't the
auto-table-layout column-width algorithm that dominates, it's the sheer
per-row layout bookkeeping for the table's 4174 ~<tr>~ elements, which neither
technique eliminates. A real fix would need actual DOM virtualization
(rendering only the rows near the viewport), a materially bigger change
than either of these. Not implemented.

*** Sort-skip fix (landed, commit ~e6aa959~): internal vs. external gap

Sort always called ~runFilter()~ internally after reordering, even with no
filter active — pure no-op work once the DOM is already correctly
reordered. Gated behind a new ~_hasAnyActiveSingleTableFilter()~ check.

| Measurement                            | before fix | after fix |    Δ |
|----------------------------------------+------------+-----------+------|
| Internal (self-reported) sort duration | 2842 ms    | 995 ms    | -65% |
| External (wall-clock) sort duration    | ~4388 ms   | 3669 ms   | -16% |

*Methodology lesson*: internal ~performance.now()~ deltas alone are NOT a
reliable proxy for perceived latency when a forced-reflow call is what's
being removed — the layout cost doesn't disappear, it's deferred to
whenever the browser (or a later DOM read, e.g. Playwright's own status-
text polling) next needs it. Always cross-check against external wall-clock
for this class of change. Even after this fix, sort remains ~23% slower
than ~main~ (see headline table above) — the fix mitigates the Step 1-2
regression, it does not close it.

*** Step 5 (lazy CAA/EAA): dramatic, unambiguous win

~artist-events~ normally seeds ~sa_enable_caa_pics: false~ for every other
measurement in this session (the EAA queue doesn't complete in reasonable
time at this row count) — but the pageType's own ~addEAA: 'Event'~ feature
makes CAA/EAA a real, relevant cost to measure once, with CAA force-enabled
and ~GM_xmlhttpRequest~ still stubbed (always a fast 404, so this isolates
queue/DOM-update overhead, not real network latency):

| Branch                            | Time to CAA/EAA queue completion |
|-----------------------------------+----------------------------------|
| ~main~ (no Step 5, eager loading) | *did not complete within 180s*   |
| ~perf-steps-1-4~ (Step 5, lazy)   | 42.2 s                           |

Matches PERFORMANCE.org's own ~99% estimate for this step. Unambiguous win,
no caveats found.

*** Cross-check: Steps 3-4 alone (branch ~perf-steps-3-4~, main + only these 2 commits)

Cherry-picked Steps 3-4 directly onto ~main~ (skipping 1-2-5) to isolate
their effect, confirming they have no dependency on Steps 1-2's DOM-mutation
approach — both only wrap ~_updateAllColHeaderCounts()~/~openUniqDrop()~,
neither of which Steps 1-2 touch:

| Interaction         | ~main~   | ~perf-steps-3-4~ | Δ                     |
|---------------------+----------+------------------+-----------------------|
| Global filter       | 1418 ms  | 1340 ms          | no regression (noise) |
| Column filter       | 1595 ms  | 1602 ms          | no regression (noise) |
| Sort                | 2994 ms  | 2907 ms          | no regression (noise) |
| Uniq-dropdown, warm | 29700 ms | 739 ms           | *40x faster*          |

~post-filter.html~/~post-sort.html~ diffed against ~main~'s committed
baseline as byte-for-byte *"unchanged"* — Steps 3-4 add caching only, never
alter rendered output. This is the clean, no-downside subset: all of the
uniq-dropdown win, none of the filter/sort regression. Pushed to
~origin/perf-steps-3-4~, PR not yet opened.

*** Takeaways

- *Steps 3-4 (header-count + uniq-dropdown caching): ship these.* Clean,
  large win (~37-40x on uniq-dropdown warm-open), zero measured downside,
  verified independent of Steps 1-2. Live on ~perf-steps-3-4~.
- *Step 5 (lazy CAA/EAA): ship this too*, independently — dramatic,
  unambiguous win for any CAA/EAA-relevant page over the lazy-load
  threshold, no regression surface found.
- *Steps 1-2 (filter/sort in-place re-render): real tradeoff, not a clean
  win.* Loses on narrow/completed-query filters and (even after the
  sort-skip mitigation) on sort; wins substantially on wide filters. The
  original premise — that ~initCollapsableColumns()~'s "30-120s for 8k+
  rows" rescan is the dominant unaddressed cost — doesn't hold for a narrow
  filter on this page: that rescan turns out to already be cheap on ~main~,
  because its own rebuild shrinks the table to just the matches *before*
  the rescan runs. Worth keeping and iterating on (real, if partial, wins
  exist — sort-skip is proof), but should not be presented as a strict
  improvement over ~main~ without the caveats in this section.

** Findings from performance code-review (2026-08-25)

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31). These findings were
raised against *that branch's* Steps 1-5, so the "regressions" listed are
regressions in unmerged code, not in ~main~. They became this file's Steps
6-14; read them as the rationale behind those steps. Two exceptions apply to
~main~ directly and are noted inline: the ~_visibleRowSetSignature~
order-dependence (Step 6, which affects ~main~'s shipped Step 4) and the
async-column invalidation gaps (Step 8, whose uniq-drop half landed on ~main~
with Step 4).

Local ~/code-review~ pass (forked agent, ~12 min / 55 tool calls — not the
cloud ultra review, no ultra credit spent), scoped to two questions:
correctness regressions in already-landed Steps 1-5, and further
performance opportunities not yet covered by them. Ranked most severe/
highest-impact first within each group; each item below becomes its own
Step 6+ heading in "Implementation steps".

*** Correctness regressions in Steps 1-5

1. ~_visibleRowSetSignature()~ (~:49412~) is documented as order-independent
   ("regardless of row order") but is actually an order-DEPENDENT string
   concat over ~tbody.rows~ in DOM iteration order. Sort (Step 2) reorders
   rows in place without hiding/showing any of them, so the row SET is
   unchanged but the signature STRING changes — every cache keyed on this
   signature (both Step 3's header-counts cache and Step 4's uniq-drop
   cache) spuriously misses after any sort, forcing the full O(rows x
   columns) recompute both steps were built to skip. This silently negates
   the "37-40x faster warm uniq-dropdown open" win documented above the
   moment a sort has happened first — far and away the highest-impact
   regression found, since it undoes two already-shipped, already-measured
   steps at once.
2. Step 5's lazy-load gate (~_useIO~, ~:63104~) computes
   ~allRows.length >= sa_caa_io_threshold~, but ~allRows~ is only populated
   in single-table mode — it stays empty for every ~tableMode: 'multi'~
   page (e.g. ~user-ratings~' "Release groups"/"Events" sub-tables, which
   declare ~addCAA~/~addEAA~). ~_useIO~ is therefore always ~false~ on
   multi-table CAA/EAA pages regardless of row count, so lazy loading never
   activates there — reproducing the exact "did not complete within 180s"
   eager-load queue-saturation failure Step 5 was built to fix, for every
   multi-table art page.
3. ~initReleaseEventsColumn()~'s post-population re-init (~:53971~) calls
   Step 3's ~_invalidateColHeaderCountsCache(t)~ but never Step 4's
   ~_invalidateUniqDropDataCache~ — a uniq-drop dropdown opened for
   Release events/Country/Date while that column is still populating can
   cache a near-empty bundle that then never refreshes (nothing invalidates
   it afterward) until an unrelated filter/sort happens to change the
   row-set signature.
4. ~initRelationshipsColumn()~/~initPicardTaggerColumn()~ (~:54689~) never
   re-trigger ~initCollapsableColumns()~/the recount pass at all — Step 3's
   own implementation notes already flagged this as a pre-existing gap for
   header counts; Step 4 adds a second, equally uninvalidated cache
   (~relIconCounts~ and friends) on top of it with no new invalidation path
   either.
5. ~_rowTextCache~ (~:30460~, filter-match text memoization) is never
   invalidated anywhere in the file. A filter run before an async column
   (Release events/Relationships/Picard) finishes populating permanently
   caches that row's pre-population (often empty) text; a later filter
   query that should match the now-arrived data silently won't, for the
   rest of that render's lifetime. Pre-existing, not introduced by this
   branch — but Step 1 (rows now persist live across far more filter passes
   than before) makes it bite more often.

*** New performance opportunities (candidate Step 6+, not yet in the plan above)

1. ~sortLargeArray~'s comparator (~createSortComparator~/
   ~createMultiColumnComparator~, ~:15508~) calls ~getCleanVisibleText()~
   (clone + ~TreeWalker~ text extraction) fresh on every pairwise
   comparison instead of once per row before sorting — for a 5,000-row
   merge sort (~n log n~ ~ 60,000+ comparisons), that's 100,000+ redundant
   DOM-text extractions instead of the 5,000 actually needed.
   ~createMultiColumnComparator~ additionally re-derives each column's
   ~isNumeric~ from the header text on every single comparison, even though
   it's invariant per column for the whole sort. Likely the single largest
   un-investigated contributor to the "Sort (unfiltered): 2994ms main /
   3669ms perf-steps-1-4" numbers above — the root-cause trace in this file
   only examined reflow/rebuild cost, never the comparator itself. Fix:
   precompute each row's sort-key text (and each column's ~isNumeric~) once
   before calling ~sortLargeArray~ (decorate-sort-undecorate).
2. ~applyExtractTrackTitleData()~'s ~_anyTitleCellMatches()~ helper
   (~:7748~) runs one independent full-table DOM scan per "does any track
   need column X" presence check (video/recArtist/disambig/streaming/
   recOf/recordedAtEvent/recordedAtPlace/recordedInArea/mixedAtPlace/
   recOfDate, ...) — ~10-15 sequential full re-walks of every row for what
   is logically one combined page-wide characterization pass, on every
   ~release-tracks~ fetch/re-render.

*** Cross-check: cloud ultra review (2026-08-25)

Ran the first of 3 free ~/code-review ultra~ passes against this branch's
diff vs ~main~ (multi-agent, cloud, ~5-10 min). Independently re-derived
three of the five regressions above with no visibility into this file's
own analysis (confirms they're real, not local-review artifacts) and
surfaced two things the local pass missed:

- Confirmed, matching Steps 6/7/9 above one-for-one: ~_visibleRowSetSignature~
  order-dependence (severity ~normal~ — the review's own words: "silently
  negating this branch's own measured 37-40x faster warm uniq-dropdown open
  headline win"), the multi-table Step-5 IO gate (severity ~nit~ — correctly
  notes it's a no-regression-vs-~main~ miss, not a new break), and
  ~_rowTextCache~ (severity ~pre_existing~, matching this file's own framing
  exactly).
- Extended Step 8: found a third uninvalidated ~_uniqDropDataCache~ site
  (~_artSetInlineSortKey()~ — folded into Step 8 above).
- **New, most severe finding overall — a live crash, not a missed
  optimization**: Step 5's animation-frame-batching fix references two
  undeclared variables (~_caaImgQueue~/~_caaMetaQueue~ instead of
  ~_caaQueue~), throwing ~ReferenceError~ the first time the
  IntersectionObserver callback fires — breaking lazy CAA/EAA loading
  entirely on exactly the large-table case Step 5 was built for. Filed as
  new Step 12 above, ahead of Steps 6-11 in urgency since it's already-
  shipped, already-broken code rather than a future optimization.
- New nit: cdtoc sub-row expansion state lost across a filter hide→show
  cycle (Step 1's else-branch overwrites the only state record without a
  restore path). Filed as new Step 13 above.

** Known limitations (out of scope)

/Harvested from/ ~perf-steps-1-4~ (branch tip 2026-08-31). Applies to ~main~
unchanged — it is a property of the companion flag userscripts and the
browser's rasterizer, not of any step in this plan.

*** Flag-image rasterization cost on flag-heavy tables

Reported symptom, distinct from the Step 5 scroll-jank fix above: on tables
whose rows carry inline flag imagery from the "More Flags Everywhere"/
"Canadian Province Flags Everywhere" companion userscripts (custom/
subdivision-level flags, e.g. a venue's city rendered as its own icon — see
~debug/flags-rendering.html~), a fast scrollbar-drag shows the sticky column
(entity name) painting immediately while the rest of the row stays blank for
a noticeable moment. Confirmed NOT the same mechanism as the CAA/EAA fix
above (that's the "Location"/area column's flags, an entirely different
column and a different, third-party-injected DOM subtree — CAA/EAA's
~IntersectionObserver~ never touches it) and NOT a regression from this
branch: ~debug/flags-rendering.html~'s single sampled ~<td>~ is 305KB,
carrying two inline ~data:image/svg+xml;base64,…~ images (~84KB and ~221KB
of base64 text — up to ~230KB of raw SVG vector art combined) for one row's
venue location. A single-table render has always fully materialized every
row's DOM at once (true on unmodified ~main~ too, long before this work) —
what's new here is just that this particular kind of page makes that
already-large DOM dramatically larger per row.

Mechanism (browser compositing, not our JS): ~applyStickyColumn~ sets
~position: sticky~ on the sticky column's cells, which Chromium typically
promotes to its own compositor layer — a thin, text-only layer that paints
independently and cheaply. The rest of a flag-heavy row sits in the table's
main content layer alongside hundreds of KB of embedded vector art; that
layer's raster tile takes real, measurable time to decode/paint the first
time it scrolls into view. A fast scrollbar-drag jump exposes a large batch
of never-yet-rasterized rows in one step, so the (already fully built,
already in the DOM) main-layer content stays visually blank until the raster
catches up — while the sticky layer, having nothing expensive to paint,
shows immediately.

Not implemented — deliberately deferred rather than folded into Step 5,
since a real fix (the standards mechanism for "skip layout/paint of
off-screen content until it's about to be needed" is CSS
~content-visibility: auto~ + ~contain-intrinsic-size~ on ~<tr>~) is a
structural change with real risk: every place that reads a row/cell's
dimensions while it *could* be off-screen — auto-resize column-width
measurement, the sticky-column left-offset calculation, anything using
~getBoundingClientRect~/~offsetWidth~/~offsetHeight~ on a row not currently
in the viewport — would need auditing, since ~content-visibility: auto~
makes an element report placeholder (contain-intrinsic-size) dimensions
while off-screen rather than its real laid-out size. Candidate for its own
dedicated, independently-verifiable step if picked up later. Not pursued on
this branch — CAA/EAA request-throughput tuning (Steps 6-8 on the source
branches) is out of scope for this port; see this file's git history on
~filter-performance-fix-caa-throughput~ if that work is wanted later.

** Resuming after /clear

Point a fresh session at this file plus the step number, e.g.:
"Implement Step 1 from ShowAllEntityData/PERFORMANCE.org". Each step is
self-contained (branch, scope, and approach are documented in its own
heading) so no prior conversation context is needed.
