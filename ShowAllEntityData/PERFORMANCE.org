* Performance improvement plan

Tracking doc for filter/render/dropdown performance work on ShowAllEntityData.

Each implementation step below is its own top-level heading with a TODO/DONE
keyword. Flip TODO -> DONE (and add a short note: what changed, which commit
range / branch) as each step lands. Do not delete completed steps — the
history of what was tried and why is the point of this file.

** Context

~runFilter()~ currently clones every matching row with ~cloneNode(true)~ on
every keystroke, replaces the tbody, and re-runs the full post-render hook
chain (CAA/EAA artwork, ~initExpandRGsFeature~, ~initCollapsableColumns~,
CDtoc, Picard tagger, nested-~<h2>~ rewiring). The abandoned
~filter-performance-fix~ branch ("Fix C") replaced this with in-place
~display:none~/~display:''~ toggling on live DOM rows, so the hook chain
runs once at initial load instead of once per keystroke, plus
IntersectionObserver-based lazy CAA/EAA loading for large tables
("Pattern 5").

That branch diverged from ~main~ at commit ~0683c23~ and was never merged;
~main~ has since moved ~347 commits ahead (~23k lines changed in the
userscript alone). It cannot be merged cleanly (4 real conflicts, one
~740 lines) and the surrounding systems it touches (CAA/EAA pipeline,
collapsable-columns, disk-load restore) have all changed since. It has been
archived (not deleted) as ~archive/filter-performance-fix-v1-single-table~
for reference — the ideas need a fresh reimplementation against current
~main~, not a merge.

** Findings: is the underlying problem still real?

Yes, confirmed directly against current ~main~ (~ShowAllEntityData.user.js~):

- ~runFilter()~'s single-table branch (~:36407-36434~) still clones every
  matching row and fully rebuilds the tbody on every keystroke.
- The multi-table branch (~:36240-36280~) does the same per group.
- No ~IntersectionObserver~ exists anywhere in the file — lazy CAA/EAA
  loading was never independently reimplemented.
- The "Show single-table" button (~:1070-1626~) now lets any non-overflowing
  multi-table sub-table be viewed as its own single-table snapshot tab, which
  makes a single-table-only fix cover *more* ground than when the old branch
  was written. Performance work should keep targeting single-table mode
  first, same as the old branch already did (its own WIP changelog scoped
  Fix C to single-table mode only).

*** The dominant, previously-underweighted cost: ~_updateAllColHeaderCounts~

The file's own comments (~:48541-48543~, ~:49212-49216~) document that this
O(rows x columns) per-column unique-value + multi-row-count scan can take
"30-120s synchronously" for 8k+ rows — enough to trigger Chrome's "Page
Unresponsive" dialog, which is why it's already deferred via
~setTimeout(0)~ rather than fixed at the root. It runs at the tail of
~initCollapsableColumns()~, which today fires on *every* single-table filter
keystroke (~:36480~), every sort (~:40029~, ~:54785~), every global
expand/collapse-all click (~:16555~, ~:52626~, ~:52637~), and every
multi-table render (~:42703~, ~:43249~). Scaled to 5,000 rows that's roughly
19-75s of background work re-triggered on every one of those actions —
larger than the cloneNode/DOM-rebuild cost the old branch was framed around.

** Ranked areas + estimated impact (5,000-row, ~20-column table)

Estimates are engineering estimates from operation-count analysis (Big-O
comparison, anchored to the one concrete empirical number in the codebase:
the documented 30-120s/8k-rows cost above), not measured browser profiles.
Real DevTools Performance-tab profiling before/after is the actual
acceptance check for each step.

| # | Area                                             | Est. improvement                                |
|---+---------------------------------------------------+--------------------------------------------------|
| 1 | Filter re-render (Fix C)                         | ~95-99% per-keystroke latency (wide tables)      |
| 2 | Same fix extended to Sort                        | ~90-99% per-sort-click latency                   |
| 3 | Cache ~_updateAllColHeaderCounts~                | ~90%+ on toggle / multi-table triggers           |
| 4 | ~openUniqDrop()~ per-open caching                | ~90-98% on repeat dropdown opens                 |
| 5 | Lazy CAA/EAA loading (IntersectionObserver)      | ~99% fewer art requests queued at initial load   |

** Implementation steps

*** DONE Step 1: Filter re-render (Fix C) — single-table mode only

In-place ~display:none~/~display:''~ toggling on live DOM rows instead of
~cloneNode(true)~ + tbody rebuild on every keystroke. Hoist
~initCollapsableColumns()~ (and the CAA/EAA/ERG/CDtoc/nested-~<h2>~ hook
chain it drives) to run once at initial render instead of once per filter
pass. Reference: ~archive/filter-performance-fix-v1-single-table~'s WIP.1
entry — reuse the approach, not the diff (too much of the surrounding code
has changed to apply it directly). Branch: ~filter-performance-fix~.

Scope: single-table mode only (~runFilter()~'s non-multi branch). Multi-table
mode is out of scope for this step — see the "Show single-table" button
note above for why single-table-first still covers the important case.

*Landed*: branch ~filter-performance-fix~, see
~ShowAllEntityData_CHANGELOG.wip.json~ WIP.1. Findings while implementing,
beyond the archived branch's own approach:

- The live fetch pipeline (~startFetchingProcess~) already called the whole
  post-render hook chain (~initCollapsableColumns~, ERG, Picard, CAA/EAA)
  exactly once, independent of this fix — it had evolved that way across
  main's 347 commits since the archived branch diverged. Only the disk-load/
  hydration path still relied on ~runFilter()~'s old per-keystroke behaviour
  for the *first* post-hydration hook-chain run; that path now calls
  ~initCollapsableColumns()~ and the CAA/EAA init functions directly, moved
  to after ~updateH2Count()~ so ~initCollapsableColumns~'s global toggle
  button can find ~filterContainer~ already attached to the h2.
- ~applyZebraStriping~, the CSV/JSON/org/HTML export functions, and the
  sub-table-filter (STF) CAA/EAA bigbox sync were already
  ~display:none~-aware (skip hidden rows/cells) before this change — this
  significantly de-risked the show/hide approach; no changes needed there.
- New regression this approach would otherwise introduce, now fixed: cdtoc
  pages' tracklist sub-rows are a synthetic sibling ~<tr>~ inserted by
  ~_cdtocInitTracklistToggles()~, not part of ~allRows~. The filter show/hide
  pass now hides a row's tracklist sub-row alongside it, and the sort
  reorder pass now moves it along with its parent (both keyed on
  ~.mb-cdtoc-tracklist-row~) — otherwise a hidden/reordered row would leave
  its sub-row orphaned.
- ~_updateAllColHeaderCounts~ (the dominant cost per the Findings above) and
  ~openUniqDrop()~'s unique-values scan are NOT visibility-aware — after this
  step they reflect counts as of the last full hook-chain run (initial
  render or disk-load), not the currently-filtered row set, since they're no
  longer re-triggered per keystroke. This is the accepted, intended effect
  of hoisting described above; making them reflect the live filtered set
  again is Steps 3/4's job (caching with proper invalidation), not this
  step's.

*** DONE Step 2: Extend the Fix-C approach to Sort

Sort currently still calls ~renderFinalTable(allRows)~ after
~sortLargeArray()~ completes, which triggers the same full tbody rebuild +
~initCollapsableColumns()~ re-run as the old filter path. Not covered by the
old branch (its WIP.1 only touched ~runFilter()~). Apply the same
"rows are live DOM nodes, reorder in place, don't re-run the hook chain
unless something actually changed" treatment to the sort-apply path.

*Landed*: branch ~filter-performance-fix~, see
~ShowAllEntityData_CHANGELOG.wip.json~ WIP.3. Scoped to single-table mode
only, matching Step 1 (confirmed with the user before implementing).

- The "reorder live DOM rows in place, don't re-run the hook chain" part of
  this step was already substantially done as a side effect of Step 1's own
  implementation: single-table sort has always delegated to ~runFilter()~ for
  its actual DOM update (never called ~renderFinalTable()~ directly, despite
  this heading's original wording — that described the OLD indirect effect of
  runFilter()'s single-table branch), and Step 1 already made that branch
  reorder-and-show/hide instead of clone-and-rebuild. What Step 1 had not yet
  added was the DOM row reorder itself (rows must be physically moved to the
  new sort order BEFORE the show/hide pass runs, since nothing rebuilds the
  tbody afterward) — that landed as part of Step 1 already.
- What Step 2 actually found and fixed, auditing the sort path for remaining
  full-rebuild/hook-rerun spots per the above:
  1. ~applyMultiSortColumnTints()~ walked every ~tbody tr~ including
     filter-hidden ones (tbody now always holds the full row set — see Step
     1) when computing shade-toggle run boundaries, shifting tint bands away
     from what the visible rows alone would show. Fixed by filtering to
     ~tr.style.display !== 'none'~ before the walk.
  2. The CAA/EAA big-picture strip's image order silently froze at its
     original fetch-time order on every subsequent sort: before Step 1,
     sorting triggered ~initCaaPics()~/~initEaaPics()~ on every action, and
     ~_artInitBigPics()~ fully rebuilt the strip from the (by-then sorted)
     tbody, so its order tracked sort order for free as a side effect. Step 1
     removed that per-action rebuild. Fixed with a new, cheap
     ~_artReorderBigBoxForTable()~ (+ ~caaReorderBigBoxForTable()~/
     ~eaaReorderBigBoxForTable()~ wrappers, mirroring the existing
     ~_artUpdateBigBoxForTable()~/~caaUpdateBigBoxForTable()~/
     ~eaaUpdateBigBoxForTable()~ pattern) that only *moves* existing wrapper
     elements to match the new row order via ~box.appendChild~ — no rebuild,
     so none of ~_artInitBigPics~'s badge/generation-counter double-count
     risk. Called once per sort (not per filter keystroke, since filtering
     alone never changes row order) from the sort handler, right after it
     reorders the live tbody rows.

*Follow-up (2026-08-24)*: sort was still calling ~runFilter()~ unconditionally
after every reorder — pure no-op work when no filter is active (nothing to
hide, no highlights to reapply), since the DOM is already correctly reordered
by this point. Gated behind a new ~_hasAnyActiveSingleTableFilter()~ check
(single-table mode only); see ~ShowAllEntityData_CHANGELOG.wip.json~ WIP.9
for the full note. *Important measurement caveat found while validating
this*: the self-reported internal JS duration dropped ~65% (~2842ms ->
~995ms on ~artist-events~), but external/user-perceived wall-clock time only
improved ~15-20% — removing the forced synchronous reflow defers that layout
cost rather than eliminating it, and it still has to happen before the next
paint. Internal ~performance.now()~ deltas alone are NOT a reliable proxy for
perceived latency when a forced-reflow call is what's being removed — always
cross-check against external wall-clock (Playwright ~waitForXSettled~ timing
or real DevTools profiling) for this class of change.

*** DONE Step 3: Cache ~_updateAllColHeaderCounts~

Memoize per-column unique-value/multi-row counts, invalidated only when the
visible row set actually changes (not on every trigger). Needed even after
Steps 1-2, since global expand/collapse-all clicks and multi-table renders
still call it unconditionally.

*Landed*: branch ~filter-performance-fix~, see
~ShowAllEntityData_CHANGELOG.wip.json~ WIP.4.

- Added ~_colHeaderCountsSigCache~, a ~WeakMap<table, signature>~. The
  signature is a cheap O(rows) join of the visible rows' ~data-mb-row-idx~
  values (an existing per-row identity, already stamped on every ~<tr>~ at
  extraction time and preserved through ~cloneNode(true)~ — see the
  ~_mbRowIdxCounter~ comment near ~:29997~). ~_updateAllColHeaderCounts~ now
  computes this signature first and returns early when it matches the last
  one computed for that table, skipping the O(rows x columns) scan.
  Keying by the live ~<table>~ element means a brand-new table (new fetch,
  disk-load, or multi-table sub-table) is always a cache miss — no manual
  reset needed.
- This correctly turns into a no-op skip for the call sites the "Findings"
  section above flagged as redundant:
  ~syncCollapseButtonsWithColumnVisibility~'s column show/hide re-init, and
  multi-table's per-sub-table re-render/reuse branches (row content there is
  always freshly cloned from the same source rows, so the signature is
  stable across a rebuild that reproduces the same filtered set). The global
  expand/collapse-all button was checked and does NOT call
  ~initCollapsableColumns~/this function at all (it drives
  ~_applyCollapseState~ directly), and ~getCleanColumnText~/
  ~_classifyCollapseCell~ were confirmed to read full DOM content
  independent of collapse/expand toggle state — so cached counts stay
  correct across expand/collapse-all clicks even though nothing re-triggers
  them.
- One real correctness hazard found and fixed while implementing: the
  signature only tracks WHICH rows are visible, not what their cells
  contain. ~initReleaseEventsColumn()~ mutates cell content in place (fills
  previously-empty Release events/country/date cells) with no row
  show/hide, then deliberately re-runs ~initCollapsableColumns()~ twice to
  pick up that new content (its own pre-existing comments: "it ran before
  cells were populated so found 0 ul>li items"). Naively caching purely on
  row visibility would have made those re-runs silently skip and leave the
  header counts frozen at their pre-population (mostly empty) values. Fixed
  by adding ~_invalidateColHeaderCountsCache(table)~ and calling it at both
  of that function's re-scan sites, right before ~initCollapsableColumns~.
  Any FUTURE call site that mutates cell content post-render without a row
  visibility change needs the same explicit invalidation — checked
  ~initRelationshipsColumn~ and ~initPicardTaggerColumn~ (both populate
  cells asynchronously) and confirmed neither one re-triggers
  ~initCollapsableColumns~/this function at all today, so their header
  counts were already not kept fresh before this change either — no new
  regression, but a pre-existing gap outside this step's scope if that ever
  changes.

*** DONE Step 4: ~openUniqDrop()~ per-open caching

~openUniqDrop()~ (~ShowAllEntityData.user.js:45002-47611~) does >=5 full
~tbody.rows~ passes from scratch on every dropdown open with zero caching.
Same caching primitive as Step 3 — build alongside it.

*Landed*: branch ~filter-performance-fix~, see
~ShowAllEntityData_CHANGELOG.wip.json~ WIP.5.

- Reused Step 3's exact primitive: a `WeakMap`-based per-(table, column)
  cache keyed by a signature of the currently visible rows'
  ~data-mb-row-idx~ values, via the shared ~_visibleRowSetSignature(table)~
  added in Step 3.
- All 5 of the function's ~tbody.rows~ passes (the big value-scan loop, the
  flag-icon segment map, the relationship-cell detector, the relationship
  icon counts, and the inline-CAA/EAA-thumbnail scan) are gated behind a
  single cache-hit check computed once at the top of the function. Deliberately
  did NOT restructure the ~40+ Maps/counters into a bundled accumulator
  object or move any code — each declaration's default value became a
  `_uniqCacheHit ? _uniqCacheHit.name : <original initial value>` ternary
  in place, and each scan loop got an `if (!_uniqCacheHit)` guard around its
  existing body, unchanged. This kept the diff to declaration lines and loop
  guards only, with zero changes to the hundreds of `.set()`/`.get()`/`++`
  call sites inside the loop bodies.
- Correctness hazard found and fixed while implementing, same shape as
  Step 3's release-events case but with a different root cause here: the
  "Cell structure" section's collapsed/expanded split
  (~multiRowCollapsedCount~/~multiRowExpandedCount~) is read from
  ~expandedCells~ (a Map of individually toggled cells), NOT re-derived from
  the visible-row signature — so a single cell's expand/collapse click
  changes these counts WITHOUT changing which rows are visible, and would
  otherwise leave a re-opened dropdown showing a stale split forever (until
  some unrelated filter/sort happened to invalidate it). Fixed by adding
  ~_invalidateUniqDropDataCache(table, colIndex)~ and calling it at all 5
  places ~expandedCells~ is mutated by user interaction: both branches of
  ~_applyCollapseState()~ (header/global-button-driven toggles) and all 3
  branches of ~ensureCollapseDelegate()~'s own inline per-cell click
  handling (CAA/EAA inline expand, prose clamp toggle, list-cell toggle). A
  6th mutation site (~initCollapsableColumns~'s one-time "start expanded by
  default" pre-population for Edit details/notes columns) was checked and
  needs no invalidation call — it only ever runs on the first pass over a
  column for a given fetch, before any dropdown cache for that render could
  exist.
- Port-specific fix found while reconciling this step against ~main~'s own
  drift since the original branch work: ~main~ had independently grown a
  ~roleTokenValueCounts~ Map (the "Entity info - Role" section's per-atomic-
  role-word breakdown) at the exact declaration this step gates — the
  original branch commit predates that feature, so it never accounted for
  it. Gated ~roleTokenValueCounts~ behind the same `_uniqCacheHit` ternary
  as its sibling ~eventRoleValueCounts~, and added it to the
  ~_setUniqDropDataCache~ bundle — without this it would have silently come
  back empty on every cache hit (reopening a column's dropdown with no
  filter/sort change in between would show zero role-token entries).

*** DONE Step 5: Lazy CAA/EAA loading (IntersectionObserver)

Defer CAA/EAA icon loads + JSON-API enrichment until a row's art cell
scrolls near the viewport, instead of firing for every row at initial
render. Reference: ~archive/filter-performance-fix-v1-single-table~'s WIP.2
("Pattern 5") — ~sa_caa_io_threshold~ setting, per-ctx persistent observer.
Independent of Steps 1-4 (different subsystem); can be done in any order
relative to them.

*Landed*: branch ~filter-performance-fix~, see
~ShowAllEntityData_CHANGELOG.wip.json~ WIP.6. Reimplemented against current
~main~ rather than merged — the archived branch's shape (~ArtCtx~-generic
~_artInitPics~/~_artInitSmallPics~/~_artEnrichTable~/~_caaQueue~) turned out
to still match current ~main~ closely, so the port was mostly a straight
carry-over:

- New ~sa_caa_io_threshold~ setting (default 500 rows, 0 disables), new
  module-level ~_caaArtIO~/~_eaaArtIO~ next to the existing ~_caaQueue~, and
  new ~_artInitLazyPics(ctx, table)~ (creates/reuses a per-ctx
  ~IntersectionObserver~, 200px root margin, unobserve-on-intersect, enqueues
  ~_artLoadIcon~/~_artEnrichIcon~ through the existing ~_caaQueue~).
- ~_artInitPics(ctx)~ computes ~_useIO~ once (`allRows.length >=
  sa_caa_io_threshold`, guarded on ~IntersectionObserver~ existing) and
  branches both the small-icon pass (~_artInitSmallPics~ vs
  ~_artInitLazyPics~) and the enrichment pass (~_artEnrichTable~, now
  skipped entirely when ~_useIO~ since the observer enqueues enrichment
  itself per icon) on it. The big-picture strip
  (~_artCountLinks~/~_artInitBigPics~) is untouched — same scope boundary as
  the archived branch, only the per-row icon column + its enrichment defer.
- Disconnect/reset of both observers was placed in ~_artInitQueue()~ — the
  function current ~main~ already runs exactly once per render pass, before
  ~initCaaPics()~/~initEaaPics()~, to recreate ~_caaQueue~ — rather than
  inside ~initCaaPics()~ itself as the archived branch did (queue creation
  and IO reset live in two different functions on current ~main~ now,
  ~_artInitQueue()~ vs ~initCaaPics()~/~initEaaPics()~, whereas the archived
  branch still had them combined in one). This keeps both observers reset
  exactly once per render pass, matching the queue's own lifecycle, and
  means the observer persists correctly across Step 1/2's now-single-pass
  filter/sort actions (they never call ~_artInitQueue()~).
- ~_artRetryTable()~'s forced-reload path (cache-busted, per-table "Retry"
  button) was left calling ~_artInitSmallPics~ eagerly, unchanged — the
  archived branch did the same; a manual retry should not silently re-defer
  behind the viewport.

**** Fix: batch the IntersectionObserver callback across animation frames

Reported symptom (large ~series/events~ page, 800 rows, ~sa_caa_io_threshold~
default 500 so lazy-loading is active): dragging the scrollbar quickly to a
distant part of the table showed only the header row — the already-rendered
~<tr>~ content stayed blank/unpainted for a noticeable moment before
appearing all at once. Not a row-data problem (all rows are fully built at
initial render, same as before Step 5) — a scroll-jank/checkerboard symptom:
the browser had already-painted-content-eligible DOM but couldn't composite
it because the main thread was busy.

Root cause: a fast scrollbar-drag jump brings dozens/hundreds of
previously-off-screen icons within the observer's 200px root margin at once,
and the callback's ~entries.forEach~ ran the whole batch's ~unobserve~ +
~enqueue~ calls synchronously in a single task, right as the browser needed
the main thread free to paint the just-scrolled-to rows. Before Step 5, all
of this work happened once at initial render, before the user had started
interacting — Step 5 moved it to run exactly when the user scrolls, which is
what turned it into a visible stall.

Fix: new ~_artProcessIoEntriesBatched()~ splits the callback's intersecting
entries into ~_ART_IO_BATCH_SIZE~ (20) -sized chunks and processes one chunk
per ~requestAnimationFrame~ instead of the whole batch in one tick.
~_artInitLazyPics~'s observer callback now just delegates to it. Doesn't
change any request-concurrency queue at all — only spreads out the
DOM-mutation cost of *starting* observer-triggered loads/enrichment across
frames, so no single frame does more than 20 icons' worth of
unobserve+enqueue work.

Landed: see ~ShowAllEntityData_CHANGELOG.wip.json~ ~WIP.7~.

*** TODO Step 6: Fix ~_visibleRowSetSignature~ order-dependence

Root-cause fix for the highest-impact regression found in the 2026-08-25
code-review (see "Findings from performance code-review" below, regression
#1) — it silently undoes Steps 3 and 4's caching wins after any sort. Make
the signature genuinely order-independent (e.g. sort the visible
~data-mb-row-idx~ values before joining, or hash as an unordered set)
rather than concatenating in DOM/current-row-order. Highest priority of
this batch: it's a one-function fix that restores two already-shipped,
already-measured wins rather than adding a new one.

*** TODO Step 7: Fix Step 5's multi-table lazy-load gate

~_useIO~ (~:63104~) checks ~allRows.length~, which is always 0 in
~tableMode: 'multi'~ mode, so lazy CAA/EAA loading never activates on any
multi-table art page (e.g. ~user-ratings~) regardless of row count — see
"Findings" regression #2. Fix: compute the row count from ~groupedRows~
(summed across sub-tables) when in multi-table mode, so the threshold
check means something there too.

*** TODO Step 8: Invalidate Step 3/4 caches for async-populated columns

~initReleaseEventsColumn()~ invalidates Step 3's header-count cache but not
Step 4's uniq-drop cache (regression #3); ~initRelationshipsColumn()~/
~initPicardTaggerColumn()~ invalidate neither (regression #4, pre-existing
gap for Step 3, new gap for Step 4). Add
~_invalidateUniqDropDataCache(table, colIndex)~ calls alongside the
existing ~_invalidateColHeaderCountsCache~ call in
~initReleaseEventsColumn()~, and add both invalidation calls to
~initRelationshipsColumn()~/~initPicardTaggerColumn()~'s post-population
paths.

*** TODO Step 9: Invalidate ~_rowTextCache~ for async-populated columns

Pre-existing gap (regression #5), not introduced by this branch, but worth
fixing alongside Step 8 since it's the same shape of bug (cache built from
pre-population placeholder content, no invalidation hook when the async
column later fills in). Lower priority than Steps 6-8 — only bites when a
user filters before an async column finishes loading, a narrower window
than the always-on caches above.

*** TODO Step 10: Precompute sort keys before ~sortLargeArray~

Candidate opportunity #1 from the 2026-08-25 code-review. Decorate each row
with its per-column clean sort-key text (and cache each column's
~isNumeric~) once before sorting, instead of re-deriving both inside the
comparator on every pairwise comparison — likely the largest uninvestigated
contributor to sort's remaining ~23% regression vs ~main~ noted in the
Session-summary Takeaways below. Needs before/after DevTools profiling on
~artist-events~ to confirm, same as prior steps.

*** TODO Step 11: Combine ~applyExtractTrackTitleData~'s presence-check scans

Candidate opportunity #2 from the 2026-08-25 code-review. Replace the
~10-15 independent full-table ~_anyTitleCellMatches()~ calls (one per
AR-column presence check) with one combined per-track characterization
pass that answers all of them in a single walk. ~release-tracks~-specific;
independent of Steps 1-9.

** Session summary: measured comparison, ~main~ vs ~perf-steps-1-4~ (2026-08-24)

Consolidated results from a full measurement session on ~artist-events~
(Bruce Springsteen's events tab, 4174 rows, 21 columns — the pageType
~tests/snapshots/registry.org~ earmarks as the dedicated performance-
comparison target). All numbers are external/user-perceived wall-clock via
Playwright unless marked "internal" (self-reported ~performance.now()~
duration from inside the userscript itself, which excludes debounce/
Playwright-polling overhead but — see the sort row below — can also
understate real perceived latency when a forced-reflow call is what's
being removed). Full methodology, test files, and raw JSON:
~tests/live/artist-events-interactions.spec.js~,
~tests/support/capture-interaction-perf.js~,
~tests/snapshots/artist-events/interaction-perf-<branch>.json~.

*** Headline: narrow filter (real usage: a specific, mostly-complete search term)

"Country" = "United Kingdom" — 158 of 4174 rows match (~4%).

| Interaction           |     ~main~ | ~perf-steps-1-4~ | Δ                     |
|------------------------+-----------+------------------+------------------------|
| Global filter          |   1418 ms |          2383 ms | +68% slower            |
| Column filter          |   1595 ms |          2437 ms | +53% slower            |
| Sort (unfiltered)      |   2994 ms |          3669 ms | +23% slower (was +47%) |
| Uniq-dropdown, cold    |  29802 ms |         27838 ms | ~parity                |
| Uniq-dropdown, warm    |  29700 ms |           791 ms | *37x faster*           |

Sort's "was +47%" parenthetical is the *before* number for the ~e6aa959~
sort-skip fix (see its own subsection below) — landed mid-session, already
reflected in the "now" column above.

*** Filter selectivity flips the winner: wide filter

Same mechanism, different query — "Country" = "United States", 3380 of 4174
rows match (~81%). *Internal* (self-reported) duration, since this was a
targeted root-cause probe, not a full external-wall-clock run:

| Interaction (internal) | ~main~ | ~perf-steps-1-4~ | Δ                    |
|-------------------------+-------+------------------+-----------------------|
| Column filter, narrow (4%)  |  230 ms |   321-348 ms | perf-steps-1-4 slower |
| Column filter, wide (81%)   |  977 ms |         397 ms | *perf-steps-1-4 2.5x faster* |

~main~'s cost scales *linearly* with match count (cloneNode+insert+rescan
is proportional to the rebuilt table's size). ~perf-steps-1-4~'s cost is
roughly *constant* regardless of match rate (dominated by a fixed
forced-reflow cost across the full 4174-row table, whether 158 or 3380 rows
end up visible). Real interactive typing is progressive — early keystrokes
are wide, later ones narrow — so Steps 1-2 likely net out closer to a wash
in practice than the single-query headline table above suggests on its own.

*** Root cause of the narrow-filter regression (traced via temporary instrumentation)

~performance.now()~ marks added directly inside each branch's
~runFilter()~ single-table branch (profiled, then reverted — not committed;
see ~debug/NOTES.md~'s 2026-08-24 entry for the full trace and how to
reproduce), for the narrow ("United Kingdom", 158/4174) case:

| Sub-step                          |    ~main~ | ~perf-steps-1-4~ |
|------------------------------------+----------+------------------|
| Filter-test scan (all 4174 rows)   |   39.6 ms |          37.3 ms |
| Row mutation (clone vs. style-toggle) | 18.0 ms |       23.5 ms |
| DOM insert/replace                 |  121.4 ms |               — |
| *Forced reflow*                    |   34.8 ms |         219.3 ms |
| ~initCollapsableColumns()~ rescan  |    7.4 ms |             0 ms |
| ~_syncCollapseHasMatchInTable()~   |         — |          38.8 ms |
| *Total (self-reported)*            |  ~230 ms |          ~321-348 ms |

~main~ pays ~139ms more for cloning/inserting rows; ~perf-steps-1-4~ pays
~184ms more in forced-reflow cost alone, which dominates. Root cause:
~main~'s rebuild only ever lays out a table containing the MATCHES (158
rows); Step 1 keeps all 4174 rows permanently in the live DOM, so the next
forced reflow (here, ~window.scrollTo()~, called unconditionally at the end
of every ~runFilter()~ pass to restore scroll position) must lay out the
FULL row set, including the ~4016 now-hidden ones. Interestingly,
~initCollapsableColumns()~'s documented "30-120s for 8k+ rows" cost — the
ORIGINAL dominant-cost target behind Steps 1-4 — turns out to be cheap here
specifically BECAUSE ~main~'s rebuild already shrinks the table to just the
matches before that rescan runs; it may still dominate for a filter that
narrows only slightly (not verified).

**** Attempted mitigation: CSS containment — did not work

Tried both ~content-visibility: auto~ on every ~<tr>~ (see this file's own
"Known limitations" section below for why that's a considered, deferred
technique for a related cost) and ~table-layout: fixed~ with locked
~<col>~ widths (the exact technique this codebase's own manual-resize
handler already uses, with the comment "so subsequent width writes are
cheap" — see ~ShowAllEntityData.user.js~ around ~:25860~). Measured against
the same forced-reflow probe, isolated to the table (~table.offsetHeight~,
not ~document.body.offsetHeight~, to rule out unrelated page content):

| Technique                          | forced reflow (2 runs) |
|--------------------------------------+------------------------|
| Baseline (table-layout: auto)        | 200.0 ms / 199.8 ms   |
| ~content-visibility: auto~ on all ~<tr>~ | 250.8 ms / 198.7 ms (no improvement; sometimes worse) |
| ~table-layout: fixed~ + locked ~<col>~   | 211.5 ms / 177.7 ms (within noise, no real win) |

Neither technique meaningfully reduces the cost — it isn't the
auto-table-layout column-width algorithm that dominates, it's the sheer
per-row layout bookkeeping for the table's 4174 ~<tr>~ elements, which neither
technique eliminates. A real fix would need actual DOM virtualization
(rendering only the rows near the viewport), a materially bigger change
than either of these. Not implemented.

*** Sort-skip fix (landed, commit ~e6aa959~): internal vs. external gap

Sort always called ~runFilter()~ internally after reordering, even with no
filter active — pure no-op work once the DOM is already correctly
reordered. Gated behind a new ~_hasAnyActiveSingleTableFilter()~ check.

| Measurement                          | before fix | after fix | Δ            |
|----------------------------------------+-----------+-----------+---------------|
| Internal (self-reported) sort duration | 2842 ms   |    995 ms | -65%          |
| External (wall-clock) sort duration    | ~4388 ms  |   3669 ms | -16%          |

*Methodology lesson*: internal ~performance.now()~ deltas alone are NOT a
reliable proxy for perceived latency when a forced-reflow call is what's
being removed — the layout cost doesn't disappear, it's deferred to
whenever the browser (or a later DOM read, e.g. Playwright's own status-
text polling) next needs it. Always cross-check against external wall-clock
for this class of change. Even after this fix, sort remains ~23% slower
than ~main~ (see headline table above) — the fix mitigates the Step 1-2
regression, it does not close it.

*** Step 5 (lazy CAA/EAA): dramatic, unambiguous win

~artist-events~ normally seeds ~sa_enable_caa_pics: false~ for every other
measurement in this session (the EAA queue doesn't complete in reasonable
time at this row count) — but the pageType's own ~addEAA: 'Event'~ feature
makes CAA/EAA a real, relevant cost to measure once, with CAA force-enabled
and ~GM_xmlhttpRequest~ still stubbed (always a fast 404, so this isolates
queue/DOM-update overhead, not real network latency):

| Branch                         | Time to CAA/EAA queue completion |
|----------------------------------+-----------------------------------|
| ~main~ (no Step 5, eager loading) | *did not complete within 180s*   |
| ~perf-steps-1-4~ (Step 5, lazy)   | 42.2 s                           |

Matches PERFORMANCE.org's own ~99% estimate for this step. Unambiguous win,
no caveats found.

*** Cross-check: Steps 3-4 alone (branch ~perf-steps-3-4~, main + only these 2 commits)

Cherry-picked Steps 3-4 directly onto ~main~ (skipping 1-2-5) to isolate
their effect, confirming they have no dependency on Steps 1-2's DOM-mutation
approach — both only wrap ~_updateAllColHeaderCounts()~/~openUniqDrop()~,
neither of which Steps 1-2 touch:

| Interaction         |     ~main~ | ~perf-steps-3-4~ | Δ                          |
|-----------------------+-----------+------------------+-----------------------------|
| Global filter         |   1418 ms |          1340 ms | no regression (noise)      |
| Column filter         |   1595 ms |          1602 ms | no regression (noise)      |
| Sort                  |   2994 ms |          2907 ms | no regression (noise)      |
| Uniq-dropdown, warm   |  29700 ms |           739 ms | *40x faster*               |

~post-filter.html~/~post-sort.html~ diffed against ~main~'s committed
baseline as byte-for-byte *"unchanged"* — Steps 3-4 add caching only, never
alter rendered output. This is the clean, no-downside subset: all of the
uniq-dropdown win, none of the filter/sort regression. Pushed to
~origin/perf-steps-3-4~, PR not yet opened.

*** Takeaways

- *Steps 3-4 (header-count + uniq-dropdown caching): ship these.* Clean,
  large win (~37-40x on uniq-dropdown warm-open), zero measured downside,
  verified independent of Steps 1-2. Live on ~perf-steps-3-4~.
- *Step 5 (lazy CAA/EAA): ship this too*, independently — dramatic,
  unambiguous win for any CAA/EAA-relevant page over the lazy-load
  threshold, no regression surface found.
- *Steps 1-2 (filter/sort in-place re-render): real tradeoff, not a clean
  win.* Loses on narrow/completed-query filters and (even after the
  sort-skip mitigation) on sort; wins substantially on wide filters. The
  original premise — that ~initCollapsableColumns()~'s "30-120s for 8k+
  rows" rescan is the dominant unaddressed cost — doesn't hold for a narrow
  filter on this page: that rescan turns out to already be cheap on ~main~,
  because its own rebuild shrinks the table to just the matches *before*
  the rescan runs. Worth keeping and iterating on (real, if partial, wins
  exist — sort-skip is proof), but should not be presented as a strict
  improvement over ~main~ without the caveats in this section.

** Findings from performance code-review (2026-08-25)

Local ~/code-review~ pass (forked agent, ~12 min / 55 tool calls — not the
cloud ultra review, no ultra credit spent), scoped to two questions:
correctness regressions in already-landed Steps 1-5, and further
performance opportunities not yet covered by them. Ranked most severe/
highest-impact first within each group; each item below becomes its own
Step 6+ heading in "Implementation steps".

*** Correctness regressions in Steps 1-5

1. ~_visibleRowSetSignature()~ (~:49412~) is documented as order-independent
   ("regardless of row order") but is actually an order-DEPENDENT string
   concat over ~tbody.rows~ in DOM iteration order. Sort (Step 2) reorders
   rows in place without hiding/showing any of them, so the row SET is
   unchanged but the signature STRING changes — every cache keyed on this
   signature (both Step 3's header-counts cache and Step 4's uniq-drop
   cache) spuriously misses after any sort, forcing the full O(rows x
   columns) recompute both steps were built to skip. This silently negates
   the "37-40x faster warm uniq-dropdown open" win documented above the
   moment a sort has happened first — far and away the highest-impact
   regression found, since it undoes two already-shipped, already-measured
   steps at once.
2. Step 5's lazy-load gate (~_useIO~, ~:63104~) computes
   ~allRows.length >= sa_caa_io_threshold~, but ~allRows~ is only populated
   in single-table mode — it stays empty for every ~tableMode: 'multi'~
   page (e.g. ~user-ratings~' "Release groups"/"Events" sub-tables, which
   declare ~addCAA~/~addEAA~). ~_useIO~ is therefore always ~false~ on
   multi-table CAA/EAA pages regardless of row count, so lazy loading never
   activates there — reproducing the exact "did not complete within 180s"
   eager-load queue-saturation failure Step 5 was built to fix, for every
   multi-table art page.
3. ~initReleaseEventsColumn()~'s post-population re-init (~:53971~) calls
   Step 3's ~_invalidateColHeaderCountsCache(t)~ but never Step 4's
   ~_invalidateUniqDropDataCache~ — a uniq-drop dropdown opened for
   Release events/Country/Date while that column is still populating can
   cache a near-empty bundle that then never refreshes (nothing invalidates
   it afterward) until an unrelated filter/sort happens to change the
   row-set signature.
4. ~initRelationshipsColumn()~/~initPicardTaggerColumn()~ (~:54689~) never
   re-trigger ~initCollapsableColumns()~/the recount pass at all — Step 3's
   own implementation notes already flagged this as a pre-existing gap for
   header counts; Step 4 adds a second, equally uninvalidated cache
   (~relIconCounts~ and friends) on top of it with no new invalidation path
   either.
5. ~_rowTextCache~ (~:30460~, filter-match text memoization) is never
   invalidated anywhere in the file. A filter run before an async column
   (Release events/Relationships/Picard) finishes populating permanently
   caches that row's pre-population (often empty) text; a later filter
   query that should match the now-arrived data silently won't, for the
   rest of that render's lifetime. Pre-existing, not introduced by this
   branch — but Step 1 (rows now persist live across far more filter passes
   than before) makes it bite more often.

*** New performance opportunities (candidate Step 6+, not yet in the plan above)

1. ~sortLargeArray~'s comparator (~createSortComparator~/
   ~createMultiColumnComparator~, ~:15508~) calls ~getCleanVisibleText()~
   (clone + ~TreeWalker~ text extraction) fresh on every pairwise
   comparison instead of once per row before sorting — for a 5,000-row
   merge sort (~n log n~ ~ 60,000+ comparisons), that's 100,000+ redundant
   DOM-text extractions instead of the 5,000 actually needed.
   ~createMultiColumnComparator~ additionally re-derives each column's
   ~isNumeric~ from the header text on every single comparison, even though
   it's invariant per column for the whole sort. Likely the single largest
   un-investigated contributor to the "Sort (unfiltered): 2994ms main /
   3669ms perf-steps-1-4" numbers above — the root-cause trace in this file
   only examined reflow/rebuild cost, never the comparator itself. Fix:
   precompute each row's sort-key text (and each column's ~isNumeric~) once
   before calling ~sortLargeArray~ (decorate-sort-undecorate).
2. ~applyExtractTrackTitleData()~'s ~_anyTitleCellMatches()~ helper
   (~:7748~) runs one independent full-table DOM scan per "does any track
   need column X" presence check (video/recArtist/disambig/streaming/
   recOf/recordedAtEvent/recordedAtPlace/recordedInArea/mixedAtPlace/
   recOfDate, ...) — ~10-15 sequential full re-walks of every row for what
   is logically one combined page-wide characterization pass, on every
   ~release-tracks~ fetch/re-render.

** Known limitations (out of scope)

*** Flag-image rasterization cost on flag-heavy tables

Reported symptom, distinct from the Step 5 scroll-jank fix above: on tables
whose rows carry inline flag imagery from the "More Flags Everywhere"/
"Canadian Province Flags Everywhere" companion userscripts (custom/
subdivision-level flags, e.g. a venue's city rendered as its own icon — see
~debug/flags-rendering.html~), a fast scrollbar-drag shows the sticky column
(entity name) painting immediately while the rest of the row stays blank for
a noticeable moment. Confirmed NOT the same mechanism as the CAA/EAA fix
above (that's the "Location"/area column's flags, an entirely different
column and a different, third-party-injected DOM subtree — CAA/EAA's
~IntersectionObserver~ never touches it) and NOT a regression from this
branch: ~debug/flags-rendering.html~'s single sampled ~<td>~ is 305KB,
carrying two inline ~data:image/svg+xml;base64,…~ images (~84KB and ~221KB
of base64 text — up to ~230KB of raw SVG vector art combined) for one row's
venue location. A single-table render has always fully materialized every
row's DOM at once (true on unmodified ~main~ too, long before this work) —
what's new here is just that this particular kind of page makes that
already-large DOM dramatically larger per row.

Mechanism (browser compositing, not our JS): ~applyStickyColumn~ sets
~position: sticky~ on the sticky column's cells, which Chromium typically
promotes to its own compositor layer — a thin, text-only layer that paints
independently and cheaply. The rest of a flag-heavy row sits in the table's
main content layer alongside hundreds of KB of embedded vector art; that
layer's raster tile takes real, measurable time to decode/paint the first
time it scrolls into view. A fast scrollbar-drag jump exposes a large batch
of never-yet-rasterized rows in one step, so the (already fully built,
already in the DOM) main-layer content stays visually blank until the raster
catches up — while the sticky layer, having nothing expensive to paint,
shows immediately.

Not implemented — deliberately deferred rather than folded into Step 5,
since a real fix (the standards mechanism for "skip layout/paint of
off-screen content until it's about to be needed" is CSS
~content-visibility: auto~ + ~contain-intrinsic-size~ on ~<tr>~) is a
structural change with real risk: every place that reads a row/cell's
dimensions while it *could* be off-screen — auto-resize column-width
measurement, the sticky-column left-offset calculation, anything using
~getBoundingClientRect~/~offsetWidth~/~offsetHeight~ on a row not currently
in the viewport — would need auditing, since ~content-visibility: auto~
makes an element report placeholder (contain-intrinsic-size) dimensions
while off-screen rather than its real laid-out size. Candidate for its own
dedicated, independently-verifiable step if picked up later. Not pursued on
this branch — CAA/EAA request-throughput tuning (Steps 6-8 on the source
branches) is out of scope for this port; see this file's git history on
~filter-performance-fix-caa-throughput~ if that work is wanted later.

** Resuming after /clear

Point a fresh session at this file plus the step number, e.g.:
"Implement Step 1 from ShowAllEntityData/PERFORMANCE.org". Each step is
self-contained (branch, scope, and approach are documented in its own
heading) so no prior conversation context is needed.
