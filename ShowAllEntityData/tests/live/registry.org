Index of every spec in tests/live/ — what page it hits, what it verifies, and
why that page was picked. Unlike tests/snapshots/registry.org (the
HTML-snapshot regression harness's own dashboard, scoped strictly to pages
with a committed raw.html/rendered.html baseline pair), this table covers
every *functional/behavioral* live spec — no baseline diffing involved, so a
row here has no "script version" / "last captured" columns of its own; run
`npm run test:live` to see current pass/fail. Manual dashboard data only, not
machine-parsed — keep it in sync by hand when adding/removing a live spec.

Every spec carries a Playwright tag (`@core`/`@extended`/`@perf`) picked by
"does this exercise a shared mechanism every change could regress" (`@core`)
vs. "a bespoke/pageType-specific edge case" (`@extended`) vs. "the deliberate
perf-comparison instrumentation" (`@perf` — see PERFORMANCE.org, run
manually, no CI gate). `npm run test:live` now runs only `@core` (fast
sanity net); `npm run test:live:extended` runs `@core`+`@extended` (today's
full live suite); `npm run test:live:perf` runs just the perf-comparison
spec; `npm run test:all` still runs literally everything (fixtures + live).
See `pageTypes-testing-reference.org`'s "Identifier selection criteria" for
how a new spec's URL/identifier should be picked, and the
`add-live-behavior-test` skill for the full authoring workflow.

| spec file                                    | pageType              | URL                                                     | page title                       | verifies                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | notes                                                                                                                                                                                                                                                                                                                                                                                                                             |
|----------------------------------------------+-----------------------+---------------------------------------------------------+----------------------------------+-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| smoke.spec.js                                | artist-recordings     | /artist/70248960-cb53-4ea4-943a-edb18f7d336f/recordings | Bruce Springsteen                | Action-button toolbar injects on a real, current musicbrainz.org page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Init-only — never clicks "Show all" (this listing is ~476 native pages). Canary against MB markup drift, not a fetch/render test.                                                                                                                                                                                                                                                                                                 |
| release-group-fetch.spec.js                  | releasegroup-releases | /release-group/6a118838-5cef-447f-aee4-84dda2eba8ad     | 1978‐07‐07: The Roxy Theatre…    | "Show all" click + fetch consolidates a single-native-page release list; assertGroupedRenderCompleted self-consistency                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Asserts invariants, not fixed row counts — this release-group's real data can change. Single MB page (no pagination); see the paginated row below for that case.                                                                                                                                                                                                                                                                  |
| release-group-paginated-fetch.spec.js        | releasegroup-releases | /release-group/c497fc44-ddaf-3cce-a9b4-bfec958a0f3c     | Greetings From Asbury Park, N.J. | startFetchingProcess()'s pagination loop consolidates a 2-native-page release list into one render                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Same self-consistency-invariant approach as release-group-fetch.spec.js, for the multi-page case.                                                                                                                                                                                                                                                                                                                                 |
| stop-button-pagination.spec.js               | artist-releasegroups  | /artist/70248960-cb53-4ea4-943a-edb18f7d336f?all=1&va=0 | Bruce Springsteen                | Stop button (stopAfterPages, n=2 default) truncates a paginated fetch after ~2 pages and still renders the partial data                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 22 native pages / 2142 rows total — comfortably more than n=2, unlike the paginated-fetch row above (only 2 pages, nothing to truncate).                                                                                                                                                                                                                                                                                          |
| uniqdrop-structure-section.spec.js           | artist-works          | /artist/70248960-cb53-4ea4-943a-edb18f7d336f/works      | Bruce Springsteen                | window.__saTest.getUniqDropSections() "Structure" section reflects all 4 cell-structure states (empty/single/collapsed/expanded) on the "Lyrics languages" column                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Counts asserted as >0, not exact — real MB data drifts.                                                                                                                                                                                                                                                                                                                                                                           |
| filter-global.spec.js                        | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Global filter (#mb-global-filter-input) narrows the page-wide row count; every sub-table's count sums to it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | "Tunnel" narrows to exactly 1 of 7 releases — title text, not row count, so immune to MB data drift.                                                                                                                                                                                                                                                                                                                              |
| filter-column.spec.js                        | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Per-column filter (Format) narrows the page-wide row count; sub-table counts still sum correctly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 3 of 7 releases are 7"/12" Vinyl.                                                                                                                                                                                                                                                                                                                                                                                                 |
| filter-subtable.spec.js                      | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Sub-Table Filter (STF) narrows only its own group (2 of 6 in "Official release"), sibling group ("Promotion release", 1) untouched                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | STF hides rows via style.display rather than re-rendering — a different mechanism from global/column filters (see filterSortAssertions.js).                                                                                                                                                                                                                                                                                       |
| sort-column.spec.js                          | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Sorting a column (Format) reorders rows without changing any page-level or sub-table row count                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Confirms sortLargeArray()/runFilter() only reorders — never drops/adds rows.                                                                                                                                                                                                                                                                                                                                                      |
| disk-fixture-load.spec.js                    | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | loadFromDiskFixture() (Load-from-disk dialog + file input) renders the same data as a live fetch, with no live network round-trip for the core rows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Fixture: tests/fixtures/saved-data/releasegroup-releases.json.gz, captured once via tests/support/capture-fixture.js.                                                                                                                                                                                                                                                                                                             |
| idb-cache-hit-bigbox.spec.js (test 1)        | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Bigbox strip (_artInitBigPics) AND inline Release-column thumbnail (_artInitInlinePics) both render real blob: images from a directly-seeded IndexedDB, with GM_xmlhttpRequest stubbed to always-404 (network can't be the source)                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Fixture: tests/fixtures/idb-state/releasegroup-releases-caa-bigbox-images.json, captured once via tests/support/capture-idb-fixture.js.                                                                                                                                                                                                                                                                                           |
| idb-cache-hit-bigbox.spec.js (test 2)        | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Control: with no IDB seed, both the bigbox strip and the inline thumbnail fail closed under the same 404 stub                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Proves test 1 is genuinely exercising the IDB tier, not trivially passing regardless.                                                                                                                                                                                                                                                                                                                                             |
| subtable-filter-sort-caa-interaction.spec.js | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Reproduces debug/multi-table-sort-filter-bug.org: STF (sub-table filter) narrowing survives a column sort (row count/highlighting AND CAA bigbox/badge), STF clears fully even after a sort, and the h3 "Clear all filters" button appears for an STF-only filter                                                                                                                                                                                                                                                                                                                                                                                                                         | Only live spec needing REAL CAA network access — uses realNetworkGmXhr.js's passthrough GM_xmlhttpRequest instead of gmStubs.js's always-404 stub, since an always-404 CAA fetch would make the bigbox/badge assertions meaningless.                                                                                                                                                                                              |
| releasegroup-releases-caa-type-comment-filter.spec.js (test 1) | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Regression test for a real, confirmed-live bug: a column filter typed against the CAA column's per-image TYPE/COMMENT text (not the "yes"/"no" presence sentinel) always matched zero rows on this pageType. Filters the "Official release" sub-table's (uncollapsed) CAA column for "ont cass" (a substring of the cassette release's real "Front cassette case" search text) and asserts exactly the one matching row survives.                                                                                                                                                                                                                                                    | Root-caused to renderGroupedTable() always cloneNode(true)-ing rows into the DOM, so the "source" rows runFilter() matches against never receive the async CAA/EAA enrichment applied to the live clone — fixed via _artSyncSearchTextToSourceRow() in ShowAllEntityData.user.js. Real CAA network required (same realNetworkGmXhr.js path as the row above) for the bug to manifest at all.                                    |
| releasegroup-releases-caa-type-comment-filter.spec.js (test 2) | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Regression test for a second real, confirmed-live bug: with every CAA cell explicitly uncollapsed, typing into the CAA column filter silently re-collapsed even the surviving match — discarding the user's explicit choice (single-table pages never did this). Asserts the surviving row's cell stays expanded and its highlight is visible with no extra click.                                                                                                                                                                                                                                                                                                                    | Root-caused to _artBuildMultiRowArtCell()'s FIRST-BUILD branch (forced on every keystroke by test 1's own fix) hardcoding new cells to collapsed, ignoring the expandedCells map — fixed by making that branch consult it too.                                                                                                     |
| releasegroup-releases-caa-type-comment-filter.spec.js (test 3) | releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c     | Tougher Than the Rest            | Companion coverage for the DEFAULT (never-expanded) case: a CAA cell that starts and stays collapsed through a matching type/comment filter still gets the .mb-collapse-toggle-has-match yellow cue, and expanding it reveals the highlighted text — matching the single-table behavior.                                                                                                                                                                                                                                                                                                                                                                                               | Traced live (classList-hook instrumentation) to already-existing code (_artHighlightArtCell()'s own _syncCollapseHasMatchInTable() call) — no separate production fix was needed once test 1's row-matching fix landed; this test is permanent coverage for that now-reachable, already-correct behavior.                        |
| releasegroup-releases-filter-sort.spec.js    | releasegroup-releases | /release-group/c497fc44-ddaf-3cce-a9b4-bfec958a0f3c     | Greetings From Asbury Park, N.J. | The multi-table sibling of artist-releases-filter-sort.spec.js's comprehensive coverage — every other releasegroup-releases spec above shares the thin "Tougher Than the Rest" pilot page (7 rows/2 groups) and only checks row-count narrowing + sibling-group isolation; this fills the remaining gap on this pageType: per-column typed filter cases with .mb-column-filter-highlight assertions, 2-column/3-way combos, three distinct filter-application ORDERS (global->sub-table->column, the reverse, and a third arbitrary interleaving — same final row count regardless of order, by set-intersection commutativity), sort-then-restore checkpoints (with a pre-applied column filter plus the CAA presence filter), and uniq-value-dropdown contents (filters cleared and one filter active) — all exercised independently on BOTH the "Official release" (119 rows) and "Promotion release" (4 rows) sub-tables of a single page, confirming column/STF filters and Cc/Rx/Ex modifiers are genuinely per-sub-table (verified directly against runFilter()'s multi-table branch, not assumed). | Real CAA network throughout (same realNetworkGmXhr.js path as the row above) — a full setup (2-page pagination + real CAA fetch for ~119 images) takes ~2 minutes by itself, so scenarios are grouped into a handful of test()s with many test.step()s each. Targets a DIFFERENT release-group URL than every other row in this pageType cluster — already used by release-group-paginated-fetch.spec.js for the 2-page-pagination case above; a richer, real-CAA page was needed since "Tougher Than the Rest" is too thin for meaningful highlight/uniq-dropdown coverage. Fixture: tests/support/greetingsReleaseGroupReleasesFixture.js — every expected count derived from the raw pre-render native HTML (debug/Greetings-releasegroup-releases-initial-page-{1,2}.html) then confirmed/corrected against real live runs (see that file's own header for the two genuine live surprises this required: Label combining name+comment text, and the Sub-Table Filter being a broad cross-column text search rather than column-scoped). Report: npm run test:live:report:releasegroup-releases (generate-releasegroup-releases-filtersort-report.js, built on the shared tests/support/filtersortReportGenerator.js engine — also now used by artist-releases-filter-sort.spec.js's own report script). |
| third-party-title-mismatch.spec.js           | release-tracks        | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | A simulated jesus2099-style title/recording-name mismatch marker is picked up by the uniqdrop "Flags - Title mismatch" section                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Simulator fixture: tests/fixtures/thirdPartyScripts/jesus2099-title-mismatch.js (hand-built, not a vendored copy of the real script).                                                                                                                                                                                                                                                                                             |
| third-party-rogue-filter-write.spec.js       | release-tracks        | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | _isGenuineFilterInputEvent()'s guard rejects an untrusted column-filter write (simulated rogue userscript autofill), resets the poisoned input, and never applies a filter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Simulator self-reports via window.__thirdPartySimResult since the guard resets the value synchronously (no async gap to observe it).                                                                                                                                                                                                                                                                                              |
| release-tracks-streaming-ars-leak.spec.js    | release-tracks        | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | Regression test for a real, confirmed-live bug: the "MB: Inline per-recording streaming & download links" userscript's `<dl class="ar">` container isn't always a sibling of native `<div class="ars">` (every debug/*.html capture happens to show it that way) — when nested inside it instead, the emptied shell (its own "▼ Streaming/Downloads:" label) was left stranded inside the "ARs" column's cell after the real link data was correctly extracted into "Streaming/Downloads". Asserts the "ARs" cell never contains `<dl class="ar">`/"Streaming/Downloads:" while "Streaming/Downloads" still gets the real link, both before and after filtering "Compilation of". | Simulator fixture: tests/fixtures/thirdPartyScripts/streaming-downloads-nested-in-ars.js (hand-built, reproduces the nested shape applyExtractTrackTitleData()'s own JSDoc already flags as possible). Root-caused to the wholesale `_bareArsDiv` child-move sweeping the still-intact dl.ar in before _buildStreamingDownloadsTd() extracts its <strong>/<a> grandchildren out — fixed by detaching dl.ar from the DOM the moment it's found, regardless of nesting. |
| column-visibility-survives-refilter.spec.js  | release-tracks        | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | Regression test for a real, confirmed-live bug, shared-mechanism (not release-tracks-specific): hiding a column via the 👁️ "Visible" Column Visibility button, then filtering (or sorting) ANY column made the hidden column's cell data reappear, fully populated — even though its header stayed correctly hidden. Hides "ARs" via toggleColumn(), filters "Title" for "to", and asserts every visible row's ARs `<td>` still has `display: none` (both after filtering and after clearing it), and the header itself stays hidden throughout. | Root-caused to toggleColumn()/toggleColumnInTable() mutating only the `<tr>` elements live in the DOM at toggle time — renderFinalTable()/renderGroupedTable() both replace `<tbody>` content wholesale with fresh clones of the captured source rows on every re-render, which never saw that mutation. Fixed via a new `_reapplyColumnVisibility(table)` called from both render tails, using each table's own (never-rebuilt) header row as the source of truth. Tagged @core since this is a shared render-pipeline mechanism, not a release-tracks quirk — any pageType with the Column Visibility feature enabled can reproduce it. |
| release-tracks-part-of-series-multirow.spec.js | release-tracks      | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | Regression test for a real, confirmed-live bug: a track's "part of:" AR crediting more than one series (comma/"and"-joined `<span class="serieslink">` markers in one native `<dd>`, e.g. "Thunder Road" crediting 3 series) rendered "Part of series" as ONE unsplit `<li>` blob instead of one row per series. Asserts a multi-series cell splits into one `<li>` per series (each containing exactly one `/series/` link, no leftover ","/" and " separator text), and that the resulting 2+-item cell gets the standard list-cell `.mb-cell-collapse-toggle` count badge. | Root-caused to `PEER_SPLIT_KINDS` excluding `series` (grouped with the genuinely chain-shaped place/event/work/area kinds), so `_buildKindSplitListTd()`'s `kinds.size === 0` branch cloned the whole `<dd>` verbatim into one row. Fixed by adding `series` to `PEER_SPLIT_KINDS` (confirmed peer-shaped, not chain-shaped, against every "part of:" `<dd>` in debug/r-final.html, debug/tracklist-overflow.html, debug/full.html — a series never nests another series as its own decoration the way an area nests its own ancestry). |
| release-tracks-part-of-series-uniqdrop.spec.js | release-tracks      | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | `add-column-extractor`/`uniq-dropdown-section` follow-up to the multirow split above: once "Part of series" splits into one `<li>` per series, its 📊 unique-values dropdown gets three new per-facet sections — "Part of series - Name"/"- Date"/"- Number" (`_findCellPartOfSeriesParts()`, `SYN_SECTION_META`). Asserts the sections render with the expected `» series name: `/`» date: `/`» number: ` entries, every entry carries `dataset.mbUniqSynLabel` (quickfilter wiring), checking the first "Number" entry narrows the page-wide row count and highlights the matched number inside the surviving row's cell, and reopening the panel reproduces the same checked state (dropdown data-cache warm-path). | Also fixed a pre-existing latent bug in the `window.__saTest.getUniqDropSections()` test-mode debug hook while writing this: its column-name lookup matched on a re-derived, decoration-stripped `textContent` only, which silently returned `null` for any column whose header carries a dynamically-injected glyph icon (e.g. this column's own `serieslink` glyph via `_dynamicArColumnGlyphs`) — the glyph injection embeds an invisible zero-width-space character the strip regex didn't account for. Fixed by preferring `th.dataset.colName` first, mirroring `openUniqDrop()`'s own `_colHeaderName` resolution — test-mode-only, no production behavior change. |
| release-tracks-recorded-at-place-multidate.spec.js | release-tracks   | /release/1d404e1d-fcb6-3a52-b478-e706e893c897           | Born to Run                      | Regression test for a real, confirmed-live bug: "Thunder Road" (and several other tracks on this release) credits TWO places under one shared "recorded at:" `<dt>`, each individually bounded by its own "(from … until …)" date range. `_buildRecordedAtPlaceTd()` only ever extracted the LAST place's date (from the tail of the whole `<dd>`) and re-appended that SAME shared date onto every place's `<li>`, so an earlier place's `<li>` showed its own date PLUS a spurious duplicate of the last place's date. Asserts a multi-place cell's places each carry exactly one `.mb-credit-date` span, holding only that place's own date text, never a sibling place's. | Root-caused to date extraction happening once per `<dt>` (popped off the whole `<dd>`'s last node BEFORE segmenting by place) rather than once per PLACE segment. Fixed by moving the "(on …)"/"(from … until …)" extraction to run per segment, AFTER the existing comma/"and"-separator trim, skipping the decorative empty `<!-- -->` comment nodes MusicBrainz wraps each date in on both sides; `_liveResult`/`_appendLiveDateFlag()` moved alongside it so the live-recording-date comparison also runs per place instead of once per `<dt>`. Shared builder with "Mixed at place", so both columns benefit. |
| artist-events-interactions.spec.js           | artist-events         | /artist/70248960-cb53-4ea4-943a-edb18f7d336f/events     | Bruce Springsteen                | Global/column filter, sort (both directions), header unique-value/multi-row-cell badge sanity, and uniq-dropdown "Structure" cache reflects a cell expand/collapse (does not go stale) on the 4174-row performance-comparison target                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Loads via tests/fixtures/saved-data/artist-events.json.gz (loadFromDiskFixture) — no live 42-page fetch. Constants shared with capture-interaction-perf.js/capture-snapshots.js via tests/support/artistEventsFixture.js. Part of this repo's artist-events performance-comparison work (PERFORMANCE.org Steps 1-5). Now that Step 4 is on main, the uniq-dropdown cache test guards main itself, not just a branch diff.         |
| discography-view.spec.js                     | artist-releasegroups  | /artist/5d02f264-e225-41ff-83f7-d9b1f0b1874a?all=1&va=0 | Simon & Garfunkel                | Discography-view (Complete/Official/Non-Official/Merged) switching: sort visuals (header tint, glyph highlight, zebra striping, sort-status text) persist across a view switch exactly like row order already does, including across/after the merged view specifically (a separate row-insertion code path from the normal render pipeline); the master-toggle button stays synced with actual collapse state and never leaks another view's hidden sub-tables when clicked; a genuinely view-specific state (an active STF filter) still correctly resets                                                                                                                               | 123 rows total (85 with inline CAA thumbnails) — picked specifically because this artist's Non-Official view has no "Album" category, the condition that exposed the master-toggle desync/leak bug.                                                                                                                                                                                                                               |
| event-parts-extraction.spec.js               | work-recordings       | /work/8727a75a-8d33-3a2c-912a-f57952773201              | A Rainy Night in Soho            | Two `eventParts` synthetic-column extractor bugs on the same work page: (1) reproduces debug/event-extraction-bug.org — Event-Venue/City/Country no longer corrupt when Event-Additional-Info itself contains a comma; (2) reproduces debug/multiple-dates.html — an uncertain-day date ("YYYY-MM-DD/DD", e.g. "2001-12-22/23") is recognized as Event-Date, not Event-Detail                                                                                                                                                                                                                                                                                                             | Test 1 exercises the non-USA/Canada/UK ("Right-to-Left fallback") branch, unlike the two USA-based debug/*.html snapshots for that bug. Three recordings of the same 2024-05-12 Nowlan Park, Kilkenny, Ireland performance: one buggy row (comma in Additional-Info) plus two same-shaped controls (no comma) as regression guards. Test 2 uses a separate recording on the same work, from 2001.                                 |
| tag-value-sort-overflow-row.spec.js          | tag-value             | /tag/rock                                               | Tag "rock"                       | Reproduces debug/tag-rock-initial.org: each sub-section's native trailing "See all N entities" overflow row (already replaced by a "Show all N rows" button on first render) does not resurrect as a real, permanently visible row — in ANY sub-table, not just the one acted on — after a column sort on any uncollapsed sub-table; "Show all N rows" button counts match the native page's own "See all N" counts                                                                                                                                                                                                                                                                       | Needs REAL CAA/EAA network access (uses realNetworkGmXhr.js), since the .org repro explicitly toggles both global CAA/EAA buttons before sorting. Root-cause fix: applyListToTable marks the overflow `<tr>` with a `dataset` attribute at creation time; renderGroupedTable strips it from group.rows/originalRows by that marker instead of by DOM reference (a clone, so indexOf never matched).                               |
| artist-releases-filter-sort.spec.js          | artist-releases       | /artist/84c38d3a-3400-4c28-b988-90558bb6fae0/releases   | BoDeans                          | Comprehensive representative-coverage matrix, per debug/filterSort-tests.org: typed column filters (one per filterable column, incl. Cc/Rx/Ex demos, comment-boundary cross-tag matches, deliberately non-word-aligned substring-overlap cases, CAA type/comment combos, and a Catalog# substring case that also asserts the collapsed multi-row-cell `.mb-collapse-toggle-has-match` yellow indicator), a real chaban day-of-week third-party-interop case (§A2, via a fixture-HTML-patched load — see chabanDayOfWeekFixture.js), multi-column combos + global/column typing-order pairs, sort-then-restore row-count invariants, uniq-dropdown content checks (filters cleared and with one filter left active), and uniq-dropdown-DRIVEN filtering (checking dropdown items rather than typing, incl. single/combination and a cross-column combo) — every §A/§B/§C case also asserts `.mb-column-filter-highlight`/`.mb-global-filter-highlight` presence/absence, `#mb-filter-status-display` exact text, the `.mb-row-count-stat` tooltip content, and `#mb-toggle-filter-highlight-btn`/`#mb-clear-column-filters-btn` visibility are all correct. Opt-in §J (TEST_PREFILTER_LOAD=1) now drives the Load-from-Disk dialog's REAL Phase-2/Phase-3 filtered path (case-sensitive "BoDeans" query) and asserts `#mb-toggle-prefilter-btn`'s exact text + post-load highlighting; opt-in §K (TEST_COLVIS=1) hides/re-shows 9 columns via the real `getByLabel()`-addressable checkboxes (an earlier `.mb-colvis-menu-item` selector never matched real DOM — §K had never actually been exercised live before). `npm run test:live:artist-releases:full` runs both opt-ins together. | Fixture: tests/fixtures/saved-data/artist-releases-bodeans.json.gz, captured once via `capture-fixture.js --only=artist-releases-bodeans` with REAL CAA/Relationships network access (realNetworkGmXhr.js) so exact counts are safe to hard-code. Full case-by-case rationale and every expected count's provenance: debug/artist-releases-filterSort-test-report.org (generated via `npm run test:live:report:artist-releases`). CAA per-image type/comment highlighting is a confirmed-live UNSTABLE quirk (0 spans on a fresh load, a growing count the more CAA-column filter cycles precede it — likely an async thumbnail-retry pipeline re-highlighting without clearing prior spans) — those 4 cases use `skipHighlightCheck: true` rather than assert an unreliable number; flagged as a real, deferred CAA-highlighting defect, not fixed here. |
| recording-fingerprints-acoustid-link-status.spec.js | recording-fingerprints | /recording/7ada2178-c1db-4a24-9760-810681e95308/fingerprints | (recording, "Associated AcoustIDs") | `uniq-dropdown-section` addition: the "AcoustID" column's 📊 unique-values dropdown gets a new "AcoustID info - Link status" section — "🔗 linked"/"🚫 unlinked" fixed flags, from `_findCellAcoustIdLinkStatus()` reading MusicBrainz's own native `disabled-acoustid` class on the AcoustID's own `<a class="external">` link. Asserts both entries render with correct counts summing to the row total, carry `dataset.mbUniqSynLabel` (quickfilter wiring), the "unlinked" entry's label reuses the REAL `disabled-acoustid` class (not an invented icon — per the user's explicit "same glyphs/classes as MusicBrainz" request), and checking it narrows the table to only rows whose AcoustID anchor carries that class. | This pageType has no `features`/`collapsableColumns` in its `pageDefinitions` entry at all — confirms the 📊 dropdown's synthetic-section machinery works even on a bare page definition, gated purely by column name (`isAcoustIdCol`). 83 rows (56 linked / 27 unlinked at capture time, per debug/rec-fingerprint.html) — a genuinely different recording than the existing tests/snapshots/registry.org `recording-fingerprints` baseline (only 3 rows, all linked), picked specifically for this state diversity. |
| artist-aliases-locale-info-uniqdrop.spec.js  | artist-aliases        | /artist/70248960-cb53-4ea4-943a-edb18f7d336f/aliases    | Bruce Springsteen                | `uniq-dropdown-section` addition: the "Locale" column's 📊 unique-values dropdown gets a new "Locale info" family — "- Language" (one entry per distinct locale language, from `_findCellLocaleInfo()`) and "- Primary" ("🥇 primary"/"◦ not primary" fixed flags, MusicBrainz's own `<span class="comment">primary</span>` marker). Asserts both sections render with real (>0) counts, every entry carries `dataset.mbUniqSynLabel` (quickfilter wiring), and checking "🥇 primary" narrows the table to only rows whose own "Locale" cell text ends with "(primary)". | Column-gated purely by name (`isLocaleCol`), same convention as `isAcoustIdCol`/`isReleaseCol`. Alias `Type`s with no Locale at all (e.g. "Search hint") are correctly excluded from both facets — see `_findCellLocaleInfo()`'s own JSDoc. |
| user-ratings-event-cancelled-uniqdrop.spec.js | user-ratings-type     | /user/vzell/ratings/event/                              | Editor "vzell" - Event ratings   | `uniq-dropdown-section` addition: `_eventCancelledKind`'s page-type gate now includes `'user-ratings-type'` (alongside `tag-value`/`user-tag-value`), so the "Event" column's 📊 unique-values dropdown gets the already-existing "Event info - Event cancelled" section — reuses `_findCellEventCancelled()`/`_highlightEventCancelledMatch()` unchanged, no new extractor. Asserts the section renders with a real (>0) count, the entry carries `dataset.mbUniqSynLabel`, and checking it narrows the table to only rows whose own "Event" cell carries the native `span.cancelled` marker AND highlights that "cancelled" text. | `/user/vzell/ratings/event/` is publicly viewable logged-out (confirmed live — no `playwright/.auth/vzell.json` needed), so this spec needs no auth setup unlike its URL might suggest. Uses `#page` as its render container, not `#content` (same quirk as `user-tags` — see project CLAUDE.md). |
| instrument-list-comment-description-uniqdrop.spec.js | instrument-list | /instruments | Instrument list | `uniq-dropdown-section` addition: each of `instrument-list`'s 8 per-family first columns (e.g. "Family", "Wind instrument") gets two new independent binary flags — "Instrument info - Comment" ("💬 has comment", a direct-child `<span class="comment">`) and "Instrument info - Description" ("📝 has description", free text after the "—" separator) — via new `_findCellInstrumentFacets()`/`_highlightInstrumentCommentMatch()`/`_highlightInstrumentDescriptionMatch()`. Asserts both sections render with real (>0) counts, every entry carries `dataset.mbUniqSynLabel`, checking each narrows the table and highlights the matched content — including across a nested `<a>` link inside a description (the "bin" -> "rudra veena" case) — and that the "—" separator itself is never swallowed into the highlight. | Multi-table pageType (8 sub-tables, one per family) — every row/column query in this spec is scoped to the "Family" table specifically (`th.closest('table')`), never the page-wide `table.tbl tbody tr`, since that would silently mix in unrelated rows from the other 7 sub-tables. `/instruments` is fully public, no auth needed. |
