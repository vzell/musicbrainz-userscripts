{
    "scripts": [
        {
            "id": "jesus2099-inline-stuff",
            "file": "mb_INLINE-STUFF.user.js",
            "author": "jesus2099",
            "version": "2026.4.9",
            "when": "now",
            "touches": [
                "Release page: inserts ISRC/AcoustID fragments before the ARs of each track row (div.ars).",
                "Release page: pink/lightcyan background-colors duplicate ISRC/AcoustID cells.",
                "Marks a track title whose display name differs from its underlying recording name by writing '≠rec. name: ...' into that title <a>'s own title tooltip AND adding a 'name-variation' class to it — this is the exact '≠' marker _titleHasRecNameMismatch() detects and tests/fixtures/thirdPartyScripts/jesus2099-title-mismatch.js simulates.",
                "Edit/notes pages: pink-highlights duplicate edit rows.",
                "Recording page: adds a millisecond length display.",
                "Non-visual: reads localStorage['jesus2099debug'].",
                "Non-visual: makes its own AcoustID API calls (api.acoustid.org)."
            ]
        },
        {
            "id": "jesus2099-supermind",
            "file": "mb_SUPER-MIND-CONTROL-II-X-TURBO.user.js",
            "author": "jesus2099",
            "version": "2026.5.21",
            "when": "init",
            "touches": [
                "Global CSS (ROW_HIGHLIGHTER, default ON): pink row-hover background via 'div#content table.tbl > tbody > tr:hover > td' — matches ANY table.tbl on the page, including ShowAllEntityData's own rendered tables, regardless of load order.",
                "Global CSS (SPOT_CAA, default ON): box-shadow on CAA/EAA <img> thumbnails.",
                "Global CSS (HIDE_RATINGS, default OFF/opt-in): hides 'td.rating'/'th.rating' cells entirely if the user enabled it.",
                "Work/artist-relationships/place-performances pages (RECORDING_LENGTH_COLUMN, default ON): adds its own extra column to the NATIVE MusicBrainz relationships table by reusing the 'treleases' class (never native on these page types) plus a title tooltip and yellow header text-shadow — this is exactly what _isJesus2099Treleases()/purgeJesus2099Artifacts() in ShowAllEntityData.user.js detect and strip.",
                "Label/artist-relationships pages (RELEASE_EVENT_COLUMN, default ON): same treleases-reuse mechanism, for release dates instead of recording lengths.",
                "SPOT_AC (default ON): dashed maroon underline on '.name-variation'/'.artist-credit-variation' spots — a second, CSS-only mechanism for the same 'name differs' signal INLINE-STUFF marks via the title attribute.",
                "TAG_TOOLS/MARK_PENDING_EDIT_MEDIUMS/USER_STATS/RELEASE_CLONER/TRACKLIST_TOOLS/etc. touch pages or areas ShowAllEntityData doesn't render (release editor, tag pages, user stats) — not expected to interact visibly."
            ]
        },
        {
            "id": "more-flags-everywhere",
            "file": "MusicBrainz_More_Flags_Everywhere.user.js",
            "author": "Lotheric",
            "version": "2026-09-05.2048",
            "when": "now",
            "touches": [
                "Installs a MutationObserver on document.body ({childList:true, subtree:true}, ~50ms debounce) that scans every newly-added DOM node for any 'a[href*=\"/area/\"]' link.",
                "Inserts a small <span class=\"flag-custom-region\"><img></span> flag icon immediately before each matched area link it hasn't already marked (dataset.flagProcessed guard) — applies inside area/place columns and credited-area chains in ShowAllEntityData's own rendered tables too.",
                "Also processes <h1> entity headers the same way, adding a flag icon there.",
                "Because it re-scans on every DOM mutation, expect it to re-decorate ShowAllEntityData's tables again after every filter/sort re-render (cloneNode(true) rebuild), not just the initial one.",
                "Non-visual: fetches flag images from commons.wikimedia.org/upload.wikimedia.org via GM_xmlhttpRequest.",
                "Non-visual: caches fetched flag images in IndexedDB, and clears any old localStorage flag cache on startup."
            ]
        },
        {
            "id": "sanojjonas-visualise",
            "file": "Sanojjonas_Visualise_Stuff.user.js",
            "author": "sanojjonas",
            "version": "0.0.69.125",
            "when": "now",
            "touches": [
                "Builds and appends its OWN new colored schedule/timetable <table> into div#content on artist/event/place/instrument/series pages (its doStuff() has its own `case \"series\":` branch) — festival lineup visualizations for a fixed set of known festivals (aff/clubAff/floatline/pukkelpop/graspop), fetched as JSON from a gitlab.io-hosted userscript asset host.",
                "Adds a manual 'Visualise' toggle button rather than always auto-running (unless config.autoVisualise is enabled).",
                "Does NOT modify ShowAllEntityData's own rendered table.tbl rows directly — lower expected DOM-collision risk than the jesus2099/More-Flags scripts — but it does append into the same div#content container ShowAllEntityData targets, so a render-order/placement conflict is still possible.",
                "CAVEAT: its body opens with top-level 'await' (const aff = await getJsonFile(...)), which only runs inside an ES module or an async function — tests/support/liveUserscripts.js wraps every injected body in an async IIFE specifically so this script doesn't throw a SyntaxError on injection.",
                "Non-visual: reads/writes localStorage (nextMap-<mbid> cache).",
                "Non-visual: loads an external add-to-calendar-button@2 script from a CDN (cdn.jsdelivr.net).",
                "Non-visual: @grant none — its fetch() calls go straight to the network, not through GM_xmlhttpRequest/gmStubs.js."
            ]
        }
    ],
    "combinations": {
        "jesus2099-only": ["jesus2099-inline-stuff", "jesus2099-supermind"],
        "more-flags-only": ["more-flags-everywhere"],
        "sanojjonas-only": ["sanojjonas-visualise"],
        "kitchen-sink": ["jesus2099-supermind", "jesus2099-inline-stuff", "more-flags-everywhere", "sanojjonas-visualise"]
    }
}
