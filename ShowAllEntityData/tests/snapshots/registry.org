| pageType             | URL                                                       | page title           | tableMode | script version | last captured | feature/change verified | notes                                                                                                                                                                    |
|----------------------+------------------------------------------------------------+-----------------------+-----------+----------------+----------------+--------------------------+---------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| artist-releasegroups | /artist/70248960-cb53-4ea4-943a-edb18f7d336f?all=1&va=0    | Bruce Springsteen    | multi     | 9.99.936       | 2026-08-23     | pilot harness setup     | sa_enable_caa_pics/sa_enable_relationships_column forced OFF for this capture only — real CAA queue for this artist's 2142 rows didn't finish even after a 10-minute wait (linear-scaling from a small-page ~0.56s/row baseline puts it at ~20min); not a bug, just impractical for a capture cycle. See task doc's Part 1 implementation notes. |
| release-tracks       | /release/1d404e1d-fcb6-3a52-b478-e706e893c897              | Born to Run          | multi     | 9.99.936       | 2026-08-23     | pilot harness setup     | requires sa_enable_release_tracks=true (seeded via seedGmValues in tests/pagetypes.json)                                                                                |
| releasegroup-releases | /release-group/f83d2211-dd81-4b1e-9a02-e89733891e1c        | Tougher Than the Rest | multi     | 9.99.936       | 2026-08-23     | IDB bigbox/inline test coverage | sa_enable_caa_pics/sa_enable_relationships_column forced OFF for this capture only — the Relationships column's real, unstubbed WS/2 fetch and the CAA completion toast both introduced run-to-run non-determinism (purchase-link order/content drift, a still-visible toast at capture time); confirmed via a byte-level diff before disabling. Both features are covered separately by tests/live/idb-cache-hit-bigbox.spec.js, which seeds IndexedDB directly instead of hitting live network. |
| artist-events        | /artist/70248960-cb53-4ea4-943a-edb18f7d336f/events        | Bruce Springsteen    | single    | 9.99.953       | 2026-08-24     | perf-comparison instrumentation added | Deliberately large/paginated (42 native MB pages, 4174 rows) single-table pageType, picked specifically as a future performance-comparison target. Now has: post-filter.html/post-sort.html (in addition to raw.html/rendered.html) capturing the FULL rendered DOM after a canonical column-filter and after a sort, to catch regressions PERFORMANCE.org Steps 1/2's in-place-mutation rewrite could introduce that no single correctness assertion would catch; interaction-perf-<branch>.json (tests/support/capture-interaction-perf.js) timing global filter/column filter/sort/uniq-dropdown cold-vs-warm-open, kept per-branch (not overwritten) to compare main vs perf-steps-1-4; and tests/live/artist-events-interactions.spec.js for correctness assertions. All three load via the committed tests/fixtures/saved-data/artist-events.json.gz disk fixture, not a live fetch — deterministic and immune to MB data drift/branch-to-branch timing. Canonical filter/sort/uniq-drop targets shared across all three via tests/support/artistEventsFixture.js. sa_enable_caa_pics forced OFF throughout (EAA queue doesn't complete in reasonable time at this row count). Two pre-existing, perf-steps-1-4-unrelated quirks found and worked around while building this (both on `main`, both present unchanged on perf-steps-1-4 since neither is touched by Steps 1-4): (1) `_updateAllColHeaderCounts`'s `.mb-col-collapse-count` badge for "Location" reports 1 despite 5 real multi-row cells existing (`_classifyCollapseCell` mis-classification for this column's multi-venue cell shape — not asserted against in the correctness spec, flagged for separate investigation); (2) clearing a filter or sorting this large table leaves the `#mb-render-heading` chunked-render overlay and the real `tbody tr` count lagging behind their own "done" signals (`waitForFilterSettled`/`waitForSortSettled`'s status-text-based resolution) — worked around via the new `waitForActualRowCount()` helper (`filterSortAssertions.js`) plus an explicit `#mb-render-heading`-absence wait, needed after BOTH the filter-clear and the sort step independently. |
| user-tags            | /user/vzell/tags?show_downvoted=0                          | Editor "vzell"       | multi     | 9.99.954       | 2026-08-25     | bespoke coverage-plan capture (container re-root, no div#content) | 112 rows total across 2 groups (5 + 107). Uses the real vzell account's own upvoted-tags data (not fixed/deterministic across time the way a release/recording's data is) — a future re-capture may show a genuine diff just from vzell voting on more tags, not a script regression. |
| series-releases      | /series/aa3694d3-a3d0-48ed-8f07-5b576de87908               | Bruce Springsteen Studio Collection | single | 9.99.954 | 2026-08-25 | bespoke coverage-plan capture (entityFeatures per H2) | 12 rows. Live H2 read back as "Releases" for this series, confirming `labelFromH2`'s substitution and the `entityFeatures.Releases` branch — a series grouping a different entity kind would need its own separate capture to cover that branch. |
| recording-fingerprints | /recording/875a6a0d-1fcc-416e-959f-433f96b0da17/fingerprints | Video "Born to Run" by Bruce Springsteen | single | 9.99.954 | 2026-08-25 | bespoke coverage-plan capture (trivial passthrough, no extra column pipeline) | 3 rows. |
| user-open-edits      | /user/vzell/edits/open                                     | Open edits by vzell  | single    | 9.99.954       | 2026-08-25     | bespoke coverage-plan capture (renameH2ToH1, login-gated) | 2 rows. Requires an authenticated session (`playwright/.auth/vzell.json` via `npm run auth:login`) — anonymous fetch redirects to a sign-in page instead. Uses vzell's own live open-edits count, which will drift over time same as user-tags above. |
| notes-received       | /edit/notes-received                                       | Recent notes left on your edits | single | 9.99.954   | 2026-08-25     | bespoke coverage-plan capture (unboundedPagination + custom confirm dialog) | 1688 rows across 29 native pages. First attempt hung indefinitely: `unboundedPagination` pageTypes pop a custom DOM confirm dialog (`Lib.showCustomConfirm`) before fetching once MusicBrainz's own pagination widget goes "ambiguous" (ellipsis, no true last page) — not a native browser dialog, so Playwright's automatic dialog handling never saw it, and nothing clicked it. Fixed by adding `tests/support/customDialog.js`'s `dismissCustomConfirmDialog()`, now called in `capture-snapshots.js` right after every "Show all" click. Also login-gated, same as user-open-edits. `renderTimeout` raised to 300000 in `tests/pagetypes.json` for the 29-page fetch. |
| release-discids      | /release/9b94e384-60e3-44e3-b0ba-dd09a68485df/discids      | Darkness on the Edge of Town | multi | 9.99.954  | 2026-08-25     | bespoke coverage-plan capture (trivial passthrough) | 1 row (single CD medium's disc ID). First attempt used REL_BTR (Born to Run, 12" Vinyl) — that release has NO disc IDs at all ("This release has no mediums that can have disc IDs"), so the button never produced a table and the capture timed out. Swapped to this CD release (Volker-supplied MBID) which actually has a submitted disc ID/CD TOC. |
