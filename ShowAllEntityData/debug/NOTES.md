RecordingRatingsRaw.html - (2026-05-15) raw DOM of https://musicbrainz.org/user/vzell/ratings/recording BEFORE the script processes it; key finding: page uses the SAME <h2>+<ul> structure as the overview page (no native table.tbl) — Structure H listToTable conversion IS required for user-ratings-type, not a native table; 2 items only (no pagination for this user); each <li> has <span class="inline-rating"> with star widget; 3-col page (has "</a> by <bdi>" artist attribution)
RecordingRatings.html - (2026-05-15) updated to show CORRECT output after v9.99.591 fix: 6 cols (Ratings | Recording | Artist | Video | Name | Comment), 6 cells per row. NOTE: generated from the user-ratings OVERVIEW page sub-table (via listToTable Structure H), not from the entity-specific page directly.
ratings.html - saved HTML from https://musicbrainz.org/user/vzell/ratings when on the "Ratings" tab (2026-05-15); shows the DOM structure used by pageType 'user-ratings': h2 section headings (Artist ratings, Recording ratings, Release group ratings, Work ratings) each followed by a ul; the last li in each ul is a "View all ratings" link; Recording/Release group sections have "</a> by <bdi>" artist attribution (3-col), Artist/Work sections do not (2-col); "... <omitted content> ..." is used if actual content is not needed for claude tokenization
