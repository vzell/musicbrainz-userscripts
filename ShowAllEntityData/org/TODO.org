* DONE data quality is missing
** i

1.)

The data quality (see "function _findCellReleaseDataQuality(cell)") is missing in the unique value drop down menu on the
"Title" column on e.g. the following pages

#+begin_example
https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/relationships
https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/relationships?direction=1&link_type_id=44&page=1
#+end_example

#+begin_example
@debug/quality-row.html row with quality from https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/relationships (sub-table "Vocals release")
@debug/qualitz/page.html - https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/relationships?direction=1&link_type_id=44&page=1
#+end_example

2.)

On pageType 'release-tracks' the column "Recording date" is missing the date specific sections in the unique value drop
down menu what other date columns have like e.g. "Date info - Month" etc

3.)

On pageType 'release-tracks' the column "Event-Date" (if present) is missing the date specific sections in the unique
value drop down menu what other date columns have like e.g. "Date info - Month" etc, see @debug/release-with-live-disambiguation.html

Fold the test into what we did in commit "2a8a963"

4.)

the column "Event-Date" which is created by the "extractor: 'eventParts'" is also missing the date specific sections see @debug/release-with-live-disambiguation.html

* DONE when milliseconds length is clicked on the "Length" column, and a sort is been done midflight, the glpyh doesn't react anymore on clicks

when milliseconds lenght is clicked on the "Length" column, and a sort is been done midflight, the glpyh doesn't react anymore on clicks

* DONE bug

On e.g. https://musicbrainz.org/release/9d451257-ebce-44ec-aad8-b48609bfaf7a
when repeatedly sorting on a column, all multi-line columns, eg "Recorded at place" get wider for every click on sort glpyh

* DONE bug release-tracks

On pageType 'release-tracks' there is a bug parsing information when the underlying entity has pending edits on
MusicBrainz, e,g the following work

#+begin_example
<span class="mp"><a class="wrap-anywhere" href="/work/d662d712-f9f8-4118-8bb2-a821395dbf96"><bdi>Light of Day</bdi></a></span>
#+end_example

It's NOT rendered in the "Recording of work" column on the final rendered page

#+begin_example
release-tracks-initial.html - initial raw HTML
release-tracks-final.html - final rendered page
#+end_example 

* DONE adjust forum.org

1.)

Rename forum.org to FORUM.org

2.)

currently the file holds the following changes: ## Changes since last version (9.99.746 -> 9.99.964)

a.)

Add all the missing stuff starting from 9.99.964

b.)

Reread the current content and adjust accordingly if same newer changes contradict

* TODO add acoustid support

#+begin_example
// ==UserScript==
// @name        MusicBrainz: Compare AcoustIDs easier!
// @namespace   https://musicbrainz.org/user/chaban
// @version     1.1.5
// @description Displays AcoustID fingerprints in more places at MusicBrainz.
// @tag         ai-created
// @author      otringal, chaban
// @license     MIT
// @match       *://*.musicbrainz.org/artist/*/recordings*
// @match       *://*.musicbrainz.org/artist/*/*edits*
// @match       *://*.musicbrainz.org/collection/*/*
// @match       *://*.musicbrainz.org/edit/*
// @match       *://*.musicbrainz.org/recording/*
// @match       *://*.musicbrainz.org/release-group/*
// @match       *://*.musicbrainz.org/release/*
// @match       *://*.musicbrainz.org/search/*
// @match       *://*.musicbrainz.org/user/*/edits*
// @match       *://*.musicbrainz.org/user/*/votes*
// @exclude     *musicbrainz.org/release/*/edit*
// @exclude     *musicbrainz.org/release/*/edit-relationships*
// @connect     api.acoustid.org
// @icon        https://acoustid.org/static/acoustid-wave-12.png
// @grant       none
// @run-at      document-end
// @downloadURL https://update.greasyfork.org/scripts/548262/MusicBrainz%3A%20Compare%20AcoustIDs%20easier%21.user.js
// @updateURL https://update.greasyfork.org/scripts/548262/MusicBrainz%3A%20Compare%20AcoustIDs%20easier%21.meta.js
// ==/UserScript==
#+end_example

* DONE "Area" is missing flag handling on https://musicbrainz.org/tag/rock

* DONE release pending edits bug

It looks like if a URL for the pageType 'release-tracks'
e.g. https://musicbrainz.org/release/4fc7d1d0-af50-422d-b98a-c60b9f6e8c56 (see also
@debug/release-tracks-pendings-edits.html for the raw HTML of the initial page)
get's not picked up by our userscript, see also the following console log

#+begin_example
10:50:55.949 [VZ: MusicBrainz - Show All Entity Data In A Consolidated View With Filtering And Multi-Sorting Capabilities] 🚀 Userscript (9.99.746+2026-07-31) loaded with external library (4.0.0+2026-07-31) active on MusicBrainz page: https://musicbrainz.org/release/4fc7d1d0-af50-422d-b98a-c60b9f6e8c56 
10:50:55.950 [VZ: MusicBrainz - Show All Entity Data In A Consolidated View With Filtering And Multi-Sorting Capabilities] ❌ Required elements not found. Terminating. {pageType: '', hasHeader: true}
10:50:55.952 [VZ: MusicBrainz - Show All Entity Data In A Consolidated View With Filtering And Multi-Sorting Capabilities] 📝 Changelog source: remote — replaced 0 local entries with 921 remote entries (fromCache=true)
#+end_example

* DONE column content shifted on https://musicbrainz.org/tag/rock for the sub-tables "Areas" and "Artists" (fixed, branch fix/tag-value-entity-column-leak, see DEBUG-NOTES.md's 2026-09-14 entry)

on the pageType "tag-value" e.g. https://musicbrainz.org/tag/rock the sub-tables "Areas", "Artists", "Series", Instruments", "Labels", "Places" and "Works" have no 
extractors defined on the main/first column.

1.)

The following columns happens to be extracted right into the "MB-Name", "Comment" and "Primary-Alias" columns

#+begin_example
Instruments
Labels
Places
#+end_example

2.)

The following columns

#+begin_example
Series
Works
#+end_example

have the "Event-Additional-Info" column filled with the same content as the "Comment" column, which is wrong

3.)

The following columns have data extracted wrongly

#+begin_example
Areas - into the "Event-Type" and "Event-Detail" instead onf "MB-Name" and "Comment"
Artists - into the "Event-Type" and "Event-Date" instead onf "MB-Name" and "Comment"
#+end_example

4.)

Also some of them are missing the "Show single-table"-button which renders the specific sub-table in single-table mode on a new tab

5.)

See @debug/tag-rock.html for the raw HTML on the final rendered table

* DONE "Relationships" column is missing on e.g. URL https://musicbrainz.org/user/vzell/tag/back%20scan%20missing (fixed, branch fix/relationships-column-missing-entities, see DEBUG-NOTES.md's 2026-09-14 entry)

1.)

On at LEAST the following pageTypes the "Relationships" column is missing on sub-tables/tables where the main column is
either of type release-group, release, label or work

Multi-table:
#+begin_example
https://musicbrainz.org/tag/rock - see the newly captured @debug/tag-rock.html for the raw HTML on the final rendered table - sub-table "Labels", "Release groups", "Releases" and "Works"
https://musicbrainz.org/user/vzell/tag/back%20scan%20missing - sub-table "Releases"
#+end_example

Single-table:
#+begin_example
https://musicbrainz.org/report/ASINsWithMultipleReleases - column "Release"
https://musicbrainz.org/report/DiscogsLinksWithMultipleReleaseGroups - column "Release group"
#+end_example

2.)

Also check potential other pageTypes which are missing the "Relationships" column



