#+TITLE: HTTP 503 / transient-failure handling — analysis
#+DATE: 2026-09-19

* Status

Originally analysis only, on branch =analysis/503-handling= — read off the code
at 9.99.1116, with no 503 reproduced live and no spec for any finding. Findings
still marked "read in the code" remain hypotheses; pin them with a spec (see
[[Verification plan]]) before fixing.

*Shipped since:*

| Finding | Branch                        | Spec                                             |
|---------+-------------------------------+--------------------------------------------------|
| F5      | =fix/caa-metadata-transient=  | =tests/fixtures/caa-metadata-transient-503.spec.js= |

*Work shape* (decided 2026-09-19): no umbrella =503= branch. =main= is the
trunk and each item below gets its own short-lived branch off it, live-tested
and merged back. The items are independently shippable, and a two-level branch
hierarchy breaks the WIP-changelog rule — =scripts/fold-wip-changelog.py= is
one-level (branch → =main=) and deletes the WIP file when it folds, so the whole
batch would have had to ship as one version block. This file stays on =main= as
the tracker.

Remaining order: item 1 (shared transient classifier, =fix/ws2-transient-classifier=)
first, since items 3-4 and 6 depend on it; then F1-F3, then F4, then F7.

F7 now carries a UI design — see [[Retry UI: one segmented control per table]].
It has a *prerequisite the rest of F7 does not*: the Relationships half can be
built today, the CAA half cannot, because nothing on the art path records that
a request failed transiently. Sequence it accordingly, or split the branch.

* The question

For every place the script talks to the network: what happens on a 503 (or
another transient failure), what does the end user SEE, can they retry, at
which level (page, table/sub-table, row, cell), and what could be automated
without being intrusive.

* Inventory

"Auto" = retries without the user doing anything. "Signal" = what the user can
see. "Level" = the finest granularity at which the user can retry.

|  # | Request                                                                            | Auto retry                               | Failure classified?                           | Signal to the user                                                               | Manual retry level                                                                    |
|----+------------------------------------------------------------------------------------+------------------------------------------+-----------------------------------------------+----------------------------------------------------------------------------------+---------------------------------------------------------------------------------------|
|  1 | HTML page fetch, main pagination loop (=fetchHtml()=, =startFetchingProcess=)      | none                                     | any non-OK throws                             | *none* — console =Lib.error= only, then the loop =break=s (see F1)               | whole fetch only (press "Show all" again)                                             |
|  2 | =fetchMaxPageGeneric()= (page count)                                               | none                                     | any error                                     | *none* — returns =1=, i.e. "one page" (F2)                                       | whole fetch only                                                                      |
|  3 | artist-releasegroups pre-fetch pass (=fetchHtml()=)                                | none                                     | any error                                     | console only, =break= (F3)                                                       | whole fetch only                                                                      |
|  4 | WS/2 via =_ws2GetJson()= (⏱ ms Length batch, Relationships lookups and browse)     | 3 tries, *503 and thrown errors only*    | 503 vs other non-OK vs thrown                 | see rows 5 and 6                                                                 | see rows 5 and 6                                                                      |
|  5 | Relationships column                                                               | via row 4                                | =outcome: 'ok'/'error'=, errors never cached  | *best in the script*: per-cell ⚠︎, hover ⟳, =done/total= badge, 📊 "Load state"   | *cell* (click), *table* (=mb-rel-retry-{i}=), *page* (=_relRetryAll=)                 |
|  6 | ⏱ ms Length                                                                        | via row 4                                | =ok/empty/error/partial=                      | button state: ⏳ loading, yellow *retry*, yellow *partial*, dimmed *unavailable* | *table/column* (press the ⏱ button again; partial retries only the outstanding MBIDs) |
|  7 | Release-events column (=initReleaseEventsColumn()=, one =inc=release-rels= lookup) | none, *and does not use =_ws2GetJson()=* | any non-OK                                    | *none* — =_dbg= and =return= (F4)                                                | none (reload the page)                                                                |
|  8 | CAA/EAA metadata (=_artEnrichIcon()= Tier 3, =fetch()=)                            | none                                     | 404/410 definitive vs transient (F5, fixed)   | =Lib.warn= for a transient failure; nothing on screen                            | *table* (=mb-caa-toggle-btn-retry-{i}=), now really evicting                          |
|  9 | CAA/EAA image bytes (=_artGmFetchBlob()=)                                          | none                                     | 404/410 *definitive* vs 5xx/timeout transient | transient failures are not cached as a miss (correct)                            | *table* (same retry button, cache-busted)                                             |
| 10 | Expand release group / release (=ergCreateButton()=, =XMLHttpRequest=)             | none                                     | =status === 200= or not                       | inline "Error loading release group (HTTP 503)" in the expanded row              | *row*: click the ▶ again (behaviour on the second click unverified)                   |
| 11 | Picard / barcode / other non-network features                                      | n/a                                      | n/a                                           | n/a                                                                              | n/a                                                                                   |

Nothing reads =Retry-After=, and there is no user-facing setting for retry
count or delay (grep for =sa_*retr*= is empty). The script's only pacing is
the Relationships gate (=_relAwaitRateSlot()=, one request per 1100 ms) and the
⏱ batch delay; the HTML pagination loop is sequential with *no* delay.

* Findings, most serious first

** F1 — a failed HTML page silently truncates the result, and the status line says it completed  (read in the code)

The pagination loop does =pagesProcessed++= /before/ fetching page =p=, and its
catch block is =Lib.error(...); break;=. After the =break= the loop falls
through to render whatever was accumulated, and the final status reads
=Loaded ${pagesProcessed} pages (${totalRows} rows)=. So:

- =pagesProcessed= counts the page that FAILED;
- nothing on screen distinguishes "5 of 5 pages" from "2 of 5 pages, then a 503";
- the Save-to-Disk path takes the same route, so a truncated set may be
  written to disk as if complete (to confirm — not traced to the write).

This is the only finding that can make a user believe wrong DATA is complete,
which is why it outranks the rest. =fetchHtml()= itself has no retry: a single
503 on page 4 of 40 ends the whole run at page 3.

** F2 — =fetchMaxPageGeneric()= turns a fetch failure into "1 page"  (read in the code)

Its catch returns =1=. A 503 on the page-count request makes the script fetch
page 1 only and report success. Same silent-truncation class as F1, one step
earlier, and it also has no retry.

** F3 — the artist-releasegroups pre-fetch pass =break=s on error  (read in the code; effect unverified)

The pass builds the "official headers" set that the main pass uses. A break
leaves the set incomplete. What that does to grouping downstream has not been
traced; it needs a fixture with a 503 on pre-fetch page 2.

** F4 — the release-events column gives up silently and uses a weaker path than its siblings  (read in the code)

=initReleaseEventsColumn()= calls =fetch()= directly rather than
=_ws2GetJson()=, so it gets neither the 3-attempt retry nor error
classification, and a failure ends in =_dbg(...)= then =return=. The column
stays empty with nothing to tell the user why, and there is no retry short of a
reload. It is one request per page, so a single 503 costs the whole column.

** DONE F5 — a 503/429/5xx on CAA metadata is recorded as "no artwork", persisted for 7 days, and the retry button may not clear it  (confirmed by a spec; fixed)

In =_artEnrichIcon()= Tier 3, every non-OK status takes one branch:
=countCache.set(path, 0)=, =imagesCache.set(path, [])=,
=_artIdbPutMetadata(path, 0, [])=. The comment says "404 — expected"; the code
does not act on that distinction. Two consequences:

1. A transient failure is stored as a fact about the release, in IDB, for
   =sa_art_idb_metadata_ttl_days= (default 7). Every later page load hits Tier 2
   and shows no artwork with no request and no warning.
2. =_artRetryTable()= purges =countCache=/=imagesCache= (session Maps) and
   =_artMissCache=, but a search of its body finds no IDB metadata delete. If
   that holds, the retry button re-enters =_artEnrichIcon()=, hits the stored
   zero at Tier 2, and does nothing.

This is the same mistake the ⏱ and Relationships features already fixed
("a transport failure is not 'no data'", in CLAUDE.md), left standing in the
oldest network path. The image-bytes path (row 9) already does it correctly;
only the metadata path is wrong. Highest-value, smallest fix, and the only
finding whose damage /outlives the session/.

*Fixed on =fix/caa-metadata-transient=.* Both halves confirmed by
=tests/fixtures/caa-metadata-transient-503.spec.js= — consequence 2 was exactly
as suspected: the retry re-entered Tier 2 and issued *zero* requests for the
whole table, which the "no IDB metadata eviction" mutation reproduces.

What shipped:
- =_artEnrichIcon()= Tier 3 gates the =_artIdbPutMetadata()= call on
  =_ART_MISS_STATUSES= (404/410), an existing constant declared next to
  =_artMissCache= whose sibling =_artGmFetchBlob()= already drew this exact
  distinction for image bytes. The same local also replaces the old bare
  =resp.status === 404= log-severity test, which logged a 410 as a warning
  while treating it as definitive.
- =_artRetryTable()= is now =async= and evicts each entity's =metadata= record
  before step 3, gated on =sa_art_idb_enable= so the IDB-off path stays fully
  synchronous (step 5's =performance.clearResourceTimings()= is order-sensitive).
  Keys come from the *art anchors* via =ref || href.replace(artSuffix)= — the
  writer's own expression — not from =ctx.rowLinkSel=, which drifts on Path-C
  synthetic anchors and would also delete keys never written.
- =sa_art_idb_metadata_ttl_days= 7 → 30, matching every sibling TTL. Safe only
  *because* of the first bullet.

*Deliberately NARROWER than this file's own item 2 below.* Only the IDB write is
gated; =ctx.countCache=/=ctx.imagesCache= still take the zero for every non-OK
status. Gating those too would re-fire one request per failed entity on every
filter keystroke and every sort — hammering the archive precisely while it is
already struggling. The zero that /outlives the session/ was the whole of the
damage. Pinned by the "the in-memory zero is gated on the status too" entry in
=scripts/mutations/caa-metadata-transient.json=, so the decision is a tested one
rather than an unexplained divergence.

** F6 — retry policy is narrower than the failures it meets  (read in the code)

=_ws2GetJson()= retries only =status === 503= plus thrown errors. 502 and 504
(what a reverse proxy returns during a MusicBrainz deploy) and 429 break out at
once and are reported as final. =Retry-After= is ignored everywhere, so the
script cannot back off by the amount the server asks for. Note that
=_relAwaitRateSlot()='s fixed 1100 ms spacing also means all three of a row's
attempts land inside a burst that typically lasts longer than 3.3 s.

** F7 — Relationships retry-table reloads everything, not just what failed  (read in the code; scope of cost unmeasured)

=_relRetryTable()= collects every =td.mb-rel-cell[data-mbid]= in the table and
evicts and re-requests all of them, including the ones that succeeded. On a
2000-row table with three failures that is 2000 requests at 1 per 1.1 s to
recover three cells. The per-cell ⟳ is the right tool there; the table button
should default to failed cells only. Not a correctness issue.

** F8 — what already works

Relationships is the model to copy: errors are a distinct outcome, never
cached, visible per cell and as a count on the header, filterable in 📊, and
retryable at cell, table and page level. ⏱ has honest button states, and its
partial retry re-requests only the outstanding MBIDs. CAA image bytes
distinguish a definitive absence from a transport failure.

* Visualisation and retry level, summarised

| Level           | Has a visible failure state today?                               | Can retry here today?                 |
|-----------------+------------------------------------------------------------------+---------------------------------------|
| Whole run/page  | *No.* F1, F2, F3: truncation looks like completion               | No — rerun the whole fetch            |
| Table/sub-table | Rel: badge with failed count. ⏱: yellow retry/partial. CAA: none | Rel: yes. CAA: yes but see F5. ⏱: yes |
| Row             | Expand-RG: inline error text                                     | Yes (click ▶ again)                   |
| Cell            | Rel only: ⚠︎, hover ⟳                                             | Rel only: click                       |
| Column          | Release events: none. CAA metadata: none                         | No                                    |

*Every "can retry here" above means retry EVERYTHING at that level.* Nothing
in the script can currently retry just what failed, at any level except a
single Relationships cell. That is F7 as a behaviour and
[[Retry UI: one segmented control per table]] as an affordance.

The gaps sit at the two extremes: the page/run level (least visible, the most
data at stake) and the CAA and release-events columns (nothing visible at all).

* What could be automated, and how intrusive each is

Ordered by value over intrusiveness. "Silent" = the user notices nothing except
things now working.

1. *Shared transient classifier* (silent, tiny). One =_isTransientHttp(status)=
   covering 429, 502, 503, 504 and thrown errors, used by =_ws2GetJson()= and
   everything below, and an optional =Retry-After= reader capped at, say, 30 s.
   Removes F6 and is the prerequisite for the rest.
2. [DONE] *Stop recording transient failures as "no artwork"* (silent, small).
   Only 404/410 may write the zero and only they may go to IDB (F5). Also delete
   the IDB record in =_artRetryTable()=. Do this first: it is the only finding
   that persists.
   Shipped on =fix/caa-metadata-transient=, with one change: "may write the zero"
   was narrowed to the IDB write alone — see F5 above for why.
3. *Retry the HTML page fetch* (silent, small). 3 attempts with widening
   backoff (about 2 s, 5 s) inside =fetchHtml()= for transient statuses only,
   applied to all three call sites (F1-F3). Most 503s are bursts, so this
   alone should turn most of F1 into a non-event.
4. *Make a final HTML failure visible* (visible, small). When retries are
   exhausted, keep what was loaded but change the status line to
   "⚠️ Loaded N of M pages — page K failed (HTTP 503)" and do not count the
   failed page in =pagesProcessed=. Do not offer Save-to-Disk as "complete".
   No new controls. Fixes the deception without adding UI.
5. *Resume from the failed page* (visible, medium). A "↻ Load remaining pages"
   button that continues the loop from page K, appending to the existing
   accumulators. Worth doing only if the accumulators can be kept without a
   re-architecture; otherwise the honest answer is to rerun. Needs design
   before any code.
6. *Route release events through =_ws2GetJson()=* (silent, small), plus the
   failure state on the column header if it still fails (F4).
7. *One automatic follow-up pass for failed Relationships cells* (silent to
   nearly silent, small) after the Phase 2 queue drains and a cool-down of
   about 30 s, at most twice. The glyph states already exist, so the user sees
   ⚠︎ turn into icons. It needs a *circuit breaker*: stop after N consecutive
   failures, or MusicBrainz in a real outage gets hammered by a script that was
   meant to be polite.
8. *Table retry = failed cells only* (silent, small) — F7. Its UI is the
   "just what failed" zone in [[Retry UI: one segmented control per table]],
   which is where the real work is: the behaviour is a one-line filter, the
   affordance is not.

Deliberately NOT proposed: an automatic retry loop with no ceiling; a global
modal ("some requests failed"); any change to the pacing constants without a
measurement (the 1 request/second rule is MusicBrainz's, not ours to loosen).

Items 1-4 and 6 are non-intrusive and each is a few lines plus a test. Item 5
is the only one that adds UI. Item 7 is the only one with a rate-limit risk.

* Retry UI: one segmented control per table

Raised 2026-09-19. Not yet built; this is the design record, and item 8 / F7 is
the behaviour half of it.

** What exists today

Three separate buttons, all at table / sub-table level:

| Button                          | id                                    | Does                                                |
|---------------------------------+---------------------------------------+-----------------------------------------------------|
| CAA/EAA strip toggle            | =mb-caa-toggle-btn-{i}=               | show/hide the big-image strip; carries thumb + count |
| CAA/EAA retry                   | =mb-caa-toggle-btn-retry-{i}=         | =_artRetryTable()= — reload EVERYTHING, cache-busted |
| Relationships retry             | =mb-rel-retry-{i}=                    | =_relRetryTable()= — reload EVERYTHING, evicts IDB   |

Globals alongside them: =mb-caa-toggle-btn-global=,
=mb-caa-toggle-btn-global-retry=, =mb-rel-retry-global= (note the LAST one
breaks the pattern — it is not =mb-rel-retry-global-retry=).

*The two CAA buttons are already adjacent siblings*:
=_artCreateOrUpdateRetryButton()= ends in =toggleBtn.after(btn)=, and it bails
out entirely if the toggle is absent. So they are already one visual run that
merely looks like two controls. That is what makes this cheap.

** Proposed shape

One segmented pill per table, replacing the run of loose buttons. Zones left to
right, each its own click target:

#+begin_example
  CAA / EAA                                Relationships

  ┌─────┬─────┬─────┬───────┐              ┌────────┬─────┬───────┐
  │ ▶🖼 │  36 │  ⟳  │ ⚠⟳ 3 │              │ 🔗 84/91 │  ⟳  │ ⚠⟳ 7 │
  └─────┴─────┴─────┴───────┘              └────────┴─────┴───────┘
    (1)   (2)   (3)    (4)                    (1)      (3)    (4)

  (1) toggle   strip / column show-hide, carries the thumb (CAA) or done/total (Rel)
  (2) count    CAA only — see the open question below
  (3) all      reload everything, cache-busted (today's ⟳)
  (4) failed   reload ONLY what failed; ABSENT when nothing failed
#+end_example

Zone 4 is the new affordance and the point of the exercise. Zone 3 keeps
today's behaviour so "force a genuine refetch of a table I believe is stale"
does not disappear — it is a different intent from "recover the failures" and
both are legitimate.

** Why this is workable, and cheap

- *No DOM restructuring.* The CAA pair are already siblings; the pill is CSS
  plus one new segment. The Relationships button becomes a run of its own.
- *No wrapper element is needed*, so nothing has to be re-parented. Draw the
  run as one pill with the run-relative selector pair the sort group already
  uses — =:not(.mb-x + .mb-x)= for the first segment,
  =:not(:has(+ .mb-x))= for the last. =:first-of-type=/=:last-of-type= are
  WRONG here for the same reason CLAUDE.md gives for the sort group.
- *Existing ids survive.* =mb-caa-toggle-btn-{i}= and
  =mb-caa-toggle-btn-retry-{i}= stay on zones 1 and 3. This matters more than
  it looks: =#mb-caa-toggle-btn-global= is referenced by *eight live specs*
  plus =tests/support/measure-caa-metadata-404s.js=, and =.mb-caa-toggle-count=
  by two more. Renaming any of them turns a CSS change into a test migration.
- *There is an established visual language to join*, not invent: the
  =.mb-col-hdr-flex= family (six controls, one shared CSS rule) and the sort
  group's segmented pill. CLAUDE.md's "Column-header toggle family" section is
  the spec; a seventh member extends the selector lists rather than copying a
  block.

** Traps, in the order they will bite

1. *CAA HAS NO FAILURE STATE TO DRIVE ZONE 4. This is a hard prerequisite, not
   a detail.* Grep comes back empty for any transient-failure tracking on the
   art path. After F5, =ctx.countCache= holds =0= for *both* "the archive says
   this release has no artwork" (a 404, a fact) and "the request failed" (a
   503) — F5 deliberately kept the in-memory zero for both, and nothing
   distinguishes them afterwards. Zone 4 therefore needs a new session-scoped
   set of entity paths that failed transiently, written where F5 decided not to
   persist and cleared by =_artRetryTable()=. *Relationships already has this*
   (=data-rel-error= per cell, =_relCellLoadState()=, the 📊 "Load state"
   section), which is why the Rel half of this design is nearly free and the
   CAA half is not.
2. *Do not hide zone 4 on a count taken from the live DOM.*
   =_relTableProgress()= reads =table.querySelectorAll('tbody td.mb-rel-cell[data-mbid]')=
   — the LIVE tbody — and =runFilter()= REMOVES non-matching rows rather than
   hiding them. A zone that disappears at =failed === 0= would therefore vanish
   exactly when a filter excludes the failed rows, removing the only way back
   to them. This is the *identical* bug CLAUDE.md records for
   =_updateLengthMismatchButtons()= / =_updateLiveDateFlagButtons()=, and
   =_countLengthMismatchRows()= walking =_msSourceRows()= is the fix to copy.
   Count source rows, or keep the zone present and merely dimmed.
3. *Nested =<button>= is invalid HTML.* The segments are siblings, never
   nested — which they already are. The sort group uses sibling =<span>=s for
   the same reason.
4. *The sort group's "no wrapper, ever" rule does NOT transfer, and knowing why
   matters.* That rule exists because 19 specs locate sort icons as
   =locator('.sort-icon-btn', {hasText: '▲'})= and =hasText= is a SUBSTRING
   match, so a wrapper whose text is =⇅▲▼= wins =.first()=. These buttons are
   located by *id*, so a wrapper would be harmless here. It is still not needed
   — but do not cite the sort-group rule as a blanket prohibition, and do not
   dismiss it either.
5. *Per-zone CSS deltas must sit AFTER the shared rule*, same specificity, or
   source order silently decides. Three deltas in the existing family are
   load-bearing for exactly this reason; see CLAUDE.md.

** Open question — what does zone 2 DO?

Splitting the thumb and the count into two click targets was asked for, and the
thumb's action is obvious (toggle the strip). The count's is not. Two targets
that do the same thing is worse than one target, so zone 2 needs either a
distinct action or to be merged back into zone 1. Candidates, none chosen:

- a pure DISPLAY, not a control — merge back into zone 1, and let the count
  carry the failure state inline (=36= → =36 ⚠7=), which removes zone 4 as a
  separate thing entirely;
- scroll the strip to its first image / jump to the strip;
- open an artwork summary for the table.

*Recommendation*: decide this before any code. The "count carries the failure
state" variant is the smallest design and drops a zone, at the cost of making
the recovery action less discoverable.

** What this does NOT address

The page/run level, which is where the most data is at stake (F1-F3). A
per-table control cannot say "page 4 of 40 failed"; item 4's status line is
still the fix for that.

* Performance and compatibility notes

- Items 1-4 add no work on the success path (a status check that already
  exists). Retry backoff only spends time on a request that was already lost.
- Item 7's follow-up pass adds requests only after failures; the shared rate
  gate must cover it, as Step 36 requires of every Relationships source.
- =fetchHtml()= is used for the current-page shortcut only when the page is the
  current document; retry does not apply there.
- Test-framework impact to check before implementing: =tests/support/gmStubs.js=
  and =realNetworkGmXhr.js= (they stub network calls), any fixture that
  answers =**/ws/2/**= or serves HTML pages (a 503 sequence needs a stateful
  route), and =rel-column-collapse-toggle.spec.js= (it asserts exact request
  counts, which item 7 changes by design).

* Verification plan

Each finding needs a fixture spec with a route that answers 503, written to
fail before the fix and pass after (project rule; mutation-check it).

| Finding | Spec shape                                                                         | Guarantee it pins                                                                                  |
|---------+------------------------------------------------------------------------------------+----------------------------------------------------------------------------------------------------|
| F1      | HTML page 2 of 3 answers 503 (then 200 on the retry, and a variant that stays 503) | recovered run has all rows; failed run says "N of M" and =pagesProcessed= excludes the failed page |
| F2      | page-count request answers 503                                                     | not silently one page                                                                              |
| F5      | metadata request answers 503 for one release, 404 for another                      | 503 writes nothing to the =metadata= store; 404 writes a zero; retry then refetches                |
| F4      | =inc=release-rels= answers 503 once                                                | column still populates after the retry                                                             |
| F6      | 502, 504, 429 with and without =Retry-After=                                       | retried, and the wait honours the header up to the cap                                             |
| F7      | table with 3 failed of N cells                                                     | request count on table retry equals the failed count                                               |

The live check for a real burst is opportunistic: MusicBrainz 503s are bursty
and cannot be summoned on demand, so a fixture is the only reproducible route.
Do not measure with the live site and quote it as a rate.

* Open questions

1. Does Save-to-Disk after F1's truncated fetch write the partial set with no
   marker? (Trace =saveToDisk= from the =break= path.)
2. What does the artist-releasegroups main pass do when the pre-fetch set is
   short (F3)?
3. On the expand-RG error row, does one click re-arm the load or two? The
   =retrySetup= handler installs =loadOnce= on the /next/ mousedown, which also
   toggles the row.
4. Should the HTML retry share one budget across pages, so a genuinely down
   site fails fast instead of spending 3 attempts on each of 40 pages?
