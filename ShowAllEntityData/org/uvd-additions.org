* DONE new additions
** DONE i

1.)

At e.g. https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/works add a unique-values dropdown section for
the 'Attributes' column — one entry per e.g. bracketed string like e.g.

#+begin_example
ACAM ID
SUISA ID
BUMA/STEMRA ID
PRS tune code
etc.
#+end_example

See @debug/artist-works-initial.html for the raw initial HTML

2.)

a.)

Search the web for the anatomy of a "Catalog Number" assigned by a record label e.g.

#+begin_example
CBS S 65480
S 65480
65480
KC 31903
PC 4758
SOPL-248
C- 0710888
40-32210
DT 0017-1311
MHCP 721
88697287402
CDCBS 65480
CDCBS65480
138.645
746.225/2-032210
#+end_example

on a music medium

b.)


At e.g. https://musicbrainz.org/release-group/c497fc44-ddaf-3cce-a9b4-bfec958a0f3c re-implement the unique-values
dropdown section "Catalog info - Prefix" for the 'Catalog' column — one entry per the different kinds of prefixes

c.)

Make sure "S" is listed separately

#+begin_example
Stereo vs. Mono: In the 1960s and 1970s, labels often used an "S" prefix (as seen in CBS S 65480) to explicitly denote a stereo pressing versus its mono counterpart.
#+end_example

d.)

The "S" in "CBS S 65480" between "CBS" and "65480" is NOT highlighted when chhosing "S" from the drop down

3.)

At https://musicbrainz.org/cdstub/browse add a unique-values dropdown section for
the 'Title' column — one entry per

#+begin_example
Added <n> <date expression>
Modified <m> <date expression>
#+end_example

from each comment

#+begin_example
<span class="comment"><bdi>(Added 9 years ago, last modified 9 years ago)</bdi></span>
#+end_example

See @debug/top-cd-stubs-final.html for the raw initial HTML, as the comment ONLY exists on the final rendered page, not
on the initial page

4.)

also add tooltips for explanation of the prefixes if possible

** DONE make "Date" column adjustments

make the following adjustments to the unique value drop down menue for date and editor based columns:

1.)

add a new section for each unique year like e.g. "1984"

2.)

add a new section for each unique computed weekday like e.g "Monday"

3.)

Maybe reuse functions we already have in code for column extractors

4.)

also add the same date treatment to columns (which have a format which includes the time) in the following pagetypes

#+begin_example
https://musicbrainz.org/election/<id> - auto-editor-election-final.html - "Date" column
https://musicbrainz.org/elections - elections-final.html - "Start date", "End date" columns
https://musicbrainz.org/label/<MBID>/annotations - label-annotations-final.html - "Date" column
#+end_example

5.)

On https://musicbrainz.org/label/<MBID>/annotations the "Revision date" column doesn't currently have any unique value drop down sections

6.)

All the editor related columns on https://musicbrainz.org/elections, e.g.

#+begin_example
Cadidate
Proposer
1st seconder
2nd seconder
#+end_example

and on https://musicbrainz.org/election/473 the "Voter" column
should get the same unique value drop down sections like the "editor" column in https://musicbrainz.org/label/011d1192-6f65-45bd-85c4-0400dd45693e/annotations


* TODO "Tracks" column needs "Sum over mediums" in a "Total"-section

* TODO ISRC parser

The "ISRCs" column at e.g.
needs


* TODO "Barcode" column needs section handling

Music releases typically carry a couple of different, overlapping identifier systems. Here's the breakdown:

Barcode formats (product-level, printed as a scannable barcode)

UPC-A (Universal Product Code)

12 digits, standard in the US/Canada since the 1970s
Structure: 1 system digit + 5-digit manufacturer code + 5-digit product code + 1 check digit
Still used, but has largely been absorbed into EAN-13 for global compatibility

EAN-13 (European Article Number)

13 digits, the international standard (GS1) and what most releases use today, worldwide
A UPC-A code can be represented in EAN-13 simply by prepending a "0" — this is exactly what's happening in your example
Structure: GS1 prefix (country/registrant) + company prefix + item reference + check digit (last digit)

GTIN (Global Trade Item Number)

Not a separate physical format — it's the umbrella term GS1 uses for the whole family (UPC-A, EAN-13, EAN-8, GTIN-14, etc.). "GTIN" is what you'll see in metadata/distribution systems even when the printed barcode is EAN-13.
Your example: 0196587565725

This is a valid EAN-13 code:

Leading 0 → this is a UPC-A code padded into EAN-13 form. Drop the leading zero and you get 196587565725, a standard 12-digit UPC-A.
The last digit (5) is the check digit, calculated via a weighted mod-10 formula over the preceding 12 digits — I verified it and it checks out as internally consistent.
The digits after the leading 0 encode a GS1-assigned company/manufacturer prefix followed by a product reference number specific to that release (exact split depends on the length GS1 allocated to that registrant — not decodable without their registration data).

This kind of code identifies the release as a retail product (a specific album/single in a specific format — CD, vinyl, digital bundle, etc.). A different format or edition of the same album gets a different barcode.

Related but distinct codes in music
ISRC (International Standard Recording Code) — identifies an individual recording (a specific track), not the release. Format: CC-XXX-YY-NNNNN (country, registrant, year, designation). Embedded in metadata, not printed as a barcode.
Catalog number — the label's own internal reference (e.g. XL123), separate from any GS1 barcode.
ISWC (International Standard Musical Work Code) — identifies the underlying composition/song for publishing/royalty purposes, distinct from any particular recording.
GRid (Global Release Identifier) — used for digital releases in some distribution pipelines, less common now than relying on UPC/EAN + ISRC.

Quick summary: barcode (UPC-A/EAN-13/GTIN) = identifies the product you'd scan at a store or match in a distributor's system; ISRC = identifies the recording; ISWC = identifies the song itself.

* TODO instrument (as “feedback guitar”)

New "As" section in "Relationship types" column at
e.g. https://musicbrainz.org/instrument/63021302-86cd-4aee-80df-2270d54f4978/recordings

* "Description" column in "search" pages could have multiple lines

"Description" column in "search" pages could have multiple lines
e.g. https://musicbrainz.org/search?query=Born+to&type=instrument&limit=25&method=indexed

#+begin_example
<td data-mb-rest-bg="rgb(242, 242, 242)" style="background: rgb(242, 242, 242);">Not to be confused with zummara or other double clarinets with similar names.<br> For the multiple bore free reed created in 1880, see Martinshorn.<br> For the similarly named single-reed woodwind, see <a href="/instrument/e3511ed1-3d73-4340-880f-c8ccc8eda11c">chalumeau</a>.<br> For the Japanese descendant named from Portuguese, see <a href="/instrument/07b91470-b63e-41e4-88e6-11c4acac064f">charumera</a>.<br> For the Catalan shawm, see <a href="/instrument/dffe56f5-cd75-4392-ad31-1c3abf2b8e37"> tible</a>.<br> For the Italian ciaramella, see <a href="/instrument/814816db-3368-4889-a8cd-aa34bd1043f0">piffero</a>.<br> For the bagpipe, see <a href="/instrument/21d5d4ec-f8ef-44dc-b02f-e564de057dcc"> xeremies</a>.<br> European medieval and Renaissance conical bore double-reed woodwind, two types evolved; one from Middle-Eastern instruments and one from bagpipe chanters. A combination of both were used to develop the modern oboe.</td>
#+end_example

* "Length" column needs a section where it says that millisends are present and not "000"

"Length" column needs a section where it says that millisends are present and not "000"
e.g. https://musicbrainz.org/area/a36544c1-cb40-4f44-9e0e-7a5a69e403a8/recordings

* ISWC handling missing

ISWC handling missing

https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/works
https://musicbrainz.org/iswc/T-070.127.339-3

