* redesign of the action buttons
** DONE Decisions (2026-09-24) and what shipped

Options were put up as ASCII mockups and the chosen shape was *two menus*:
=📦 Data ▾= and =🛠 View ▾=, with =⚙️= and =❓= staying pinned. Items 1, 3, 4, 5
and 6 landed on branch =action-button-redesign=; item 2 followed on branch
=help-github-md=.

| item | decision                                                                                       | state |
|------+------------------------------------------------------------------------------------------------+-------|
|   1. | =↔️= / =👁️= glyph-only, in the h2 immediately before =#mb-filter-container=                    | DONE  |
|   2. | =❓= opens the GitHub page; *Shift-click still opens the in-script dialog*; hand-written =.md= | DONE  |
|   3. | =🎹= joins =🛠 View ▾=; =⚙️= stays pinned beside =❓=                                          | DONE  |
|   4. | Density / Statistics / Barcode into =🛠 View ▾=; Export into =📦 Data ▾=                       | DONE  |
|   5. | Save / Load are rows of =📦 Data ▾=, so they cost one slot between them                        | DONE  |
|   6. | =tests/support/toolbarMenu.js=; 9 call sites migrated; 2 new specs + 2 mutation lists          | DONE  |

Not in the original list, added because that page has the widest heading the
script renders: =artist-releasegroups=' four discography views became
=📀 Discography ▾=, whose button *names the active view*. The flat run could
only tint the active button, which told the user nothing until they had looked
at all four.

On item 2, the one deviation from the text below: the in-script dialog is kept
behind Shift-click rather than removed. Its quick-filter search is genuinely
useful and it is the only route that works with no network.

*Item 2, as built.* =ShowAllEntityData_HELP.md= is hand-written rather than
converted — the request says "probably needs a .md conversion", and a
transcription of 2448 lines of hand-laid-out plain text would have carried
across three years of accumulated resyncs along with their layout. It is about
a third of the length, organised by what the reader is trying to do, with the
reference detail in =<details>= sections. The =.txt= is retired; the mirror
keeps serving it until users have updated past the rename.

Two things the item did not ask for and got anyway, because the route to GitHub
made them necessary rather than optional:

  - the in-script dialog needed a Markdown renderer, since it used to drop the
    file into a =<pre>= and Markdown in a =<pre>= shows its own syntax;
  - =scripts/check-publish-ready.py= now fails on a companion file the
    published script fetches but the mirror LACKS, not only on one that
    differs. That is exactly the way this change could have shipped broken —
    script published, renamed help file not.

And one defect the *baselines* found rather than any test: the barcode row in
=🛠 View ▾= was the only menu row with no =data-mb-menu-hint=, though its
shortcut is real. Fixed here, since this branch re-drifts those baselines
anyway.

Mechanism, traps and test surface: see =CLAUDE.md='s "The h1 toolbar is two
pull-down menus plus two pinned buttons", "↔️ Resize and 👁️ Visible live in the
h2" and "❓ opens GitHub; Shift-❓ renders the same file in the page" sections,
and =DEBUG-NOTES.md='s 2026-09-24 entries.

** DONE Original request


Show me a couple of options/visualization for a redesign of the action buttons in the h1 header line of multi- (e.g. https://musicbrainz.org/artist/84c38d3a-3400-4c28-b988-90558bb6fae0)
and single-table (e.g. https://musicbrainz.org/artist/84c38d3a-3400-4c28-b988-90558bb6fae0/events) pageTypes buttons

1.)

The following two buttons

#+begin_example
<button id="mb-resize-btn"
<button id="mb-visible-btn"
#+end_example

should probably be rendered (as buttons with just a glypgh and the tooltip + keyboard handling intact) before the

#+begin_example
<span id="mb-filter-container"
#+end_example

like their siblings in the multi-table case in each h3 line before a sub-table

#+begin_example
<button id="mb-stf-Album-resize-btn"
<button id="mb-stf-Album-vis-btn"
#+end_example

2.)

The current "help button"/"help system"

#+begin_example
<button id="mb-stf-Album-vis-btn"
#+end_example

should also be completely redesigned. The end-user should be redirected directly to the GitHub help page, which probabyl
needs a .md conversion from the current .txt based format

3.)

I'm not sure how to best present the current "Keyboard Shortcuts" and "Settings Manager button

4.)

The buttons

#+begin_example
Density
Statistics
Export
Keyboard Shortcuts - maybe
Settings Manager - maybe
+ maybe future generic functionality 
#+end_example

should probably go into kind of a menu

5.)

The "Save to Disk" and "Load from Disk" buttons also occupy too much space I think

6.)

Take also into account possible changes to the testing framework


* DONE The fetch progress bar should be positioned after the last button in h1

Shipped in 9.99.1150: moved =mb-fetch-progress-wrap= to the end of
=_TOOLBAR_TAIL_ORDER= (was first), so it now trails =❓= instead of sitting
between the fetch buttons and =⏹ Stop=. See CLAUDE.md's "The h1 toolbar is two
pull-down menus plus two pinned buttons" table (row 9).

The fetch progress bar should be positioned AFTER the last button (Help) in the h1 header line

#+begin_example
@debug/fetch-progress-bar-new.html - old (wrong) behaviour, before this fix
@debug/fetch-progress-bar-old.html - even older pre-redesign behaviour, matches what shipped
#+end_example

* TODO resize handle

The following dialogs should get a resize handle on the lower right corner

#+begin_example
<div id="mb-shortcuts-help"
<div id="mb-app-help-dialog"
#+end_example

which should also be remembered (together with the whole position)


* TODO Remove "Show all " generically from all buttons 

#+begin_example
"Show  " - https://musicbrainz.org/label/c4f2cf49-b57c-4cc1-8061-f54400704ac4/annotations | https://musicbrainz.org/user/vzell/ratings/event/ | https://musicbrainz.org/user/vzell/ratings | https://musicbrainz.org/label/c4f2cf49-b57c-4cc1-8061-f54400704ac4/annotations
"(xxxxx) " - https://musicbrainz.org/report/LowDataQualityReleases?filter=1
"Show all " - everywhere else
#+end_example
* TODO more action button consolidation

- On e.g. https://musicbrainz.org/user/vzell/subscriptions/artist the button text in the five button should remove
"subscriptions" and instead render it as "Subscriptions" in bold in front of the buttons

vzell | Subscriptions: [Artist] [Collection] [Label] [Series] [Editor] | 

- https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f

Bruce Springsteen | RGs: [Artists] [Various Artists] |

- https://musicbrainz.org/report/LowDataQualityReleases?filter=1

Releases marked as having low data quality | [Unfiltered] [Subscribed Only]

- https://musicbrainz.org/user/vzell/tag/back%20scan%20missing - remove "Tag for Entities"

vzell’s tag “back scan missing” (see everyone’s) | [Upvoted] [Downvoted]

- https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/releases

Bruce Springsteen | Releases: [Artist] [Various Artist]

- https://musicbrainz.org/artist/70248960-cb53-4ea4-943a-edb18f7d336f/aliases

Bruce Springsteen | [Aliases] [Credits]

