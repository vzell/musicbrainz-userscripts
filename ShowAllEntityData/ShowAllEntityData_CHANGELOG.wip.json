[
  {
    "version": "WIP.3",
    "date": "2026-09-04",
    "sections": [
      {
        "label": "🐛 Fix",
        "items": [
          "Stop deleting MusicBrainz's own `treleases` class from release tracklists. `treleases` is genuine MusicBrainz markup there — a release page natively renders its Length column as `<th class=\"treleases\">` plus one `<td class=\"treleases\">` per track (verified in `tests/snapshots/release-tracks/raw.html`, which the Playwright harness captures with only this script loaded: 9 occurrences, and not one `jesus2099` string, plugin `title` or yellow `text-shadow` in the whole file). jesus2099's `RECORDING_LENGTH_COLUMN` merely reuses the same class name on the page types MusicBrainz does NOT mark (work, artist-relationships, place-performances), and never adds it alone — the same statements also set its own script name as the `title` and, on the header, a yellow `text-shadow`. New `_isJesus2099Treleases()` tests for exactly that co-occurrence, so a bare `treleases` is now recognised as MusicBrainz's and left alone.",
          "Three places keyed on the bare class and are now gated on that predicate: the WIP.1 artifact purge (which had started stripping it from the Length `<th>`), `applyColumnErasers()`'s `'jesus2099-any'` Strategy 3b (which had been emptying `class`/`style`/`title` on all eight Length `<td>`s of every release tracklist — a pre-existing defect, visible in the committed `release-tracks` snapshot baseline as `<td class=\"\">`), and `initTreleasesObserver()`'s dispatch (which fired `_repairTreleasesTd()` on our own freshly-rendered native cells, rebuilding them from plain `textContent` and so discarding any filter-highlight spans inside them). The predicate is conservative by construction: with no corroborating evidence the class is left in place, so the failure direction is \"a stale third-party class survives\", never \"native markup is deleted\"."
        ]
      }
    ]
  },
  {
    "version": "WIP.2",
    "date": "2026-09-04",
    "sections": [
      {
        "label": "🐛 Fix",
        "items": [
          "Sorting a \"Length\" column no longer treats MusicBrainz's own \"?:??\" unknown-duration placeholder as a zero-length recording. It was compared with `parseFloat(text.replace(/[^0-9.-]/g, '')) || 0`, which collapsed \"?:??\" to 0 and so floated every unknown-length row to the TOP of an ascending sort (descending already put them last, by the same accident). Unknown length is absent data, not zero length, so those rows are now pinned LAST in both directions — in single-column sorts and as a tie-breaker inside a Ctrl+Click multi-column chain alike."
        ]
      },
      {
        "label": "🔧 Refactor",
        "items": [
          "New explicit duration comparator for \"Length\" columns: `_parseDurationToMs()` parses `M:SS`, `M:SS.mmm` and `H:MM:SS[.mmm]` (right-padding a short fractional part, so \".5\" is 500 ms), and `_compareDurations()` orders on real milliseconds. The previous digits-only `parseFloat` path happened to order genuine durations correctly — seconds are always zero-padded, so \"5:05.146\" became 505.146 and stayed monotonic — but only by relying on that undocumented invariant.",
          "`createSortComparator()` and `createMultiColumnComparator()` now resolve a column's comparison kind through one shared `_sortColumnKind()` (`'duration'`/`'numeric'`/`'text'`) instead of disagreeing: the single-column path consulted the page definition's own `integerColumns` descriptors and only then a name heuristic, while the multi-column path used the raw header text alone and never looked at the page definition at all. `createSortComparator()`'s third argument changes from an `isNumeric` boolean to that kind string. Duration detection is data-driven — `integerColumns`' `align: ':'` descriptor already means \"colon-split duration\" — plus an exact `\"Length\"` name match, so a pageType that renders durations without declaring an `integerColumns` entry for them (`release-discids`' per-disc totals) sorts as a duration too. Alt+Click \"sort by column length\" still outranks the column's kind, durations included."
        ]
      }
    ]
  },
  {
    "version": "WIP.1",
    "date": "2026-09-04",
    "sections": [
      {
        "label": "🐛 Fix",
        "items": [
          "The final rendered table no longer carries any leftovers from jesus2099's userscripts. The reported case was the \"Length\" column header arriving as `<th class=\"treleases mb-original-column\" title=\"SUPER MIND CONTROL Ⅱ X TURBO\" style=\"text-shadow: yellow 0px 0px 2px; …\">` — jesus2099's \"mb. SUPER MIND CONTROL Ⅱ X TURBO\" mutates the native header and cells in place before this script scrapes them, and `makeTableSortableUnified()` rebuilds `th.innerHTML` without ever resetting `className`/`title`/`style`, so all three rode straight through into our own header. Same for every Length `<td>` and, on release pages, for the whole `jesus2099userjs81127*` tracklist family (`toolzone`/`editbutt`/`openEdits` hover buttons, plus markers stamped onto the recording title link and Title cell). New `purgeJesus2099Artifacts()`/`_stripJesus2099InTable()` now clean every rendered `table.tbl` — header row included — on both render paths.",
          "The purge also runs over the captured source rows in `groupedRows`/`allRows`, not just the live DOM. `runFilter()` re-renders by inserting `cloneNode(true)` copies of those rows and classes/attributes survive cloning, so cleaning only the live table would have pasted every marker back in on the next filter keystroke.",
          "Three dispositions, so nothing real is ever destroyed: jesus2099's cover-art icon family is skipped entirely (it already has its own deliberately gated handling in `applyColumnErasers()`'s Strategy 2 and `_stripTransientCellState()` — re-stripping it here would have reintroduced the artwork-loss regression documented in `debug/CAA-missing-doubled.org`); pure-decoration elements are removed outright; everything else keeps the element and loses only the marker class, because those markers sit on genuine MusicBrainz content (`jesus2099userjs81127recording` is stamped on the recording title anchor itself). An unrecognised future marker defaults to the safe \"strip, don't remove\" path. The plugin `title` and its yellow `text-shadow` are dropped from marker-carrying `<th>`/`<td>` only — native MusicBrainz puts meaningful `title`s on `<a>`/`<abbr>`, and a `text-shadow` with no jesus2099 marker belongs to an unrelated third-party script (30 such elements in `debug/therising.html` alone), so neither may be stripped blind."
        ]
      },
      {
        "label": "📝 Docs",
        "items": [
          "Scope note: the purge is confined to the `table.tbl` elements this script renders. Everything jesus2099 puts outside them — its sidebar/sub-header search links, pending-edit counters and its own toolbar (108-123 such markers per page in the sampled snapshots) — is left completely untouched, so the initial page keeps working exactly as before."
        ]
      }
    ]
  }
]
