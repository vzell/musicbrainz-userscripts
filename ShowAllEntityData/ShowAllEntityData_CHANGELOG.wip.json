[
  {
    "version": "WIP.6",
    "date": "2026-07-22",
    "sections": [
      {
        "label": "✨ Improve",
        "items": [
          "Ctrl+Click on an 'Annotation'-style cell's ▶/▼ collapse toggle now also expands (uncollapses) every nested wiki `<h2>` sub-heading inside that cell, instead of just expanding the cell's own text preview.",
          "Ctrl+Click on the 'Annotation' column's header collapse toggle now also expands every nested wiki `<h2>` sub-heading in every cell of the column, in addition to expanding all cells.",
          "Both toggles' tooltips now explain the Ctrl+Click behaviour — shown only when the cell/column actually contains a nested wiki sub-heading, so the hint doesn't appear on plain annotations with no headings to expand."
        ]
      }
    ]
  },
  {
    "version": "WIP.5",
    "date": "2026-07-22",
    "sections": [
      {
        "label": "✨ Improve",
        "items": [
          "Added `sa_annotation_h2_bg` / `sa_annotation_h2_color` settings for the text color of wiki-rendered `<h2>` sub-headings nested inside table cells (e.g. 'Annotation' column sub-sections like '== Known performances =='), separate from the existing page-level H2 section-header colors. Defaults (#e3f2fd background / #1565c0 text) are chosen to sit quietly against the grey/white alternating row backgrounds and to read as related to (rather than compete with) the standard blue hyperlink color that often appears in annotation text."
        ]
      }
    ]
  },
  {
    "version": "WIP.4",
    "date": "2026-07-22",
    "sections": [
      {
        "label": "🐛 Fix",
        "items": [
          "Fixed wiki-rendered `<h2>` sub-headings inside 'Annotation'-style column cells (e.g. '== Known performances ==') becoming permanently non-collapsible after the first sort or filter. renderFinalTable()/renderGroupedTable() replace table rows with cloneNode(true) copies on every sort/filter re-render, which copies the collapsed-h2 marker classes and icon but not the click listener — makeH2sCollapsible() (which normally sets these up) only runs after the initial full-page render and a disk-load, never after a sort/filter, so the cloned headings looked processed but were inert. A new `_rewireNestedTableH2Toggles()` re-attaches working click handlers to these nested headings after every sort/filter re-render, without touching page-level h2 sections (Genres, Tags, Relationships, …) or resetting any section's manually-toggled state."
        ]
      }
    ]
  },
  {
    "version": "WIP.3",
    "date": "2026-07-21",
    "sections": [
      {
        "label": "✨ Improve",
        "items": [
          "Made the collapsed-cell clamp height of 'Annotation'-style columns configurable via a new `sa_annotation_column_max_height_em` setting (default 5.6em, ~4 lines — the same value used in WIP.1), instead of a hardcoded CSS value."
        ]
      }
    ]
  },
  {
    "version": "WIP.2",
    "date": "2026-07-21",
    "sections": [
      {
        "label": "✨ Improve",
        "items": [
          "Added a new '📝 ANNOTATION COLUMNS' settings section: `sa_enable_annotation_collapse` (checkbox, default on) toggles the WIP.1 free-text collapse/height-clamp behaviour on/off for 'Annotation'-style columns, and `sa_annotation_column_max_width` (number, default 480) makes the auto-resize width cap configurable.",
          "The auto-resize width cap for these columns is now always active, independent of `sa_enable_annotation_collapse` — even with collapsing disabled, a long annotation no longer stretches the column to its unwrapped width."
        ]
      }
    ]
  },
  {
    "version": "WIP.1",
    "date": "2026-07-21",
    "sections": [
      {
        "label": "✨ Improve",
        "items": [
          "Annotation-style columns declared in `collapsableColumns` (report-detail's AnnotationsPlaces/AnnotationsReleases sub-reports, the instrument 'Annotations' report category) are now collapsible: free-text cells are height-clamped behind a ▶/▼ 'show more/less' toggle, sharing the same column-header and global mass-toggle buttons, filter-match highlighting, and expand-state persistence as existing list-type collapsable columns (Catalog#, Label, …).",
          "Auto-resize (↔️ Resize / Ctrl+R) no longer sizes these free-text columns to a paragraph's unwrapped single-line width — their measured width is now capped, matching the new height-clamped rendering.",
          "Tightened `collapsableColumns`' list-cell detection to only match a `<ul><li>` that is a direct child of the `<td>`, fixing a latent bug where a wiki-rendered bullet/numbered list embedded inside free-text annotation prose could be mistaken for a script-generated multi-row list and partially hidden."
        ]
      }
    ]
  }
]
