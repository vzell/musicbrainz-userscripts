[
    {
        "version": "WIP.1",
        "date": "2026-08-17",
        "sections": [
            {
                "label": "🐛 Fix",
                "items": [
                    "📊 unique-values dropdown: fixed \"Join phrases\" entries (e.g. \"&\") disappearing after closing and reopening the dropdown on a column with an active join-phrase filter. `_findCellJoinPhrases()` required the separator text to be a bare DOM text node, but `_highlightJoinPhraseMatch()` wraps that same text node in a `<span class=\"mb-column-filter-highlight\">` once the entry is checked — a re-scan on reopen (which runs without an intervening filter pass to clear the highlight first) then found nothing between the two credited artists. `_findCellJoinPhrases()` now also recognizes its own highlight-wrapper span in that position and reads its text content, so reopening the dropdown keeps showing join-phrase entries that still match the currently-filtered rows."
                ]
            }
        ]
    }
]
