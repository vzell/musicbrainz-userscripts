[
    {
        "version": "WIP.1",
        "date": "2026-09-01",
        "sections": [
            {
                "label": "🐛 Fix",
                "items": [
                    "A joined artist credit (e.g. \"Queen & David Bowie\") silently dropped any entity whose own link MusicBrainz wraps in `<span class=\"mp\">` — its native marker for an entity with open/pending edits — from the \"Entity info - Artist name\"/\"Entity info - Comment\" sections of the 📊 unique-values dropdown. `_findCellEntityRefs()` only recognized a credited name as a direct child of its shared `<bdi>` or containing its own nested `<bdi>`; it now resolves the governing `<bdi>` via an ancestor walk instead, so any incidental wrapper is transparent to it.",
                    "`_findCellEntityCommentParts()` also mis-attributed comments when a cell has more than one independently-commented entity sharing a `<bdi>` with no `<li>` boundary between them (the same \"Queen & David Bowie\" shape) — every entity after the first silently inherited the FIRST entity's own comment instead of its own. It now resolves each entity's own adjacent `.comment` span instead of taking the first match in the whole cell."
                ]
            }
        ]
    }
]
